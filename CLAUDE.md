# CLAUDE.md

@AGENTS.md

All project instructions live in **AGENTS.md** (the canonical, cross-tool agent file — the
line above imports it). This file exists only so Claude Code loads it; do not add project
guidance here — edit AGENTS.md instead.

One rule worth repeating for every session: **/ship (publish, Version-PR merge, deploy) is
always MK's decision — prepare, present, and wait for an explicit "yes proceed"; never
auto-ship.** (Canonical statement in AGENTS.md § Releasing.)
