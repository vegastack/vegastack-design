// Browser-mode (Playwright Chromium) global setup.
// jest-dom-style element assertions are provided natively by @vitest/browser's
// `expect.element(...)`; a11y assertions use axe-core via ./test/a11y.ts.
export {};
