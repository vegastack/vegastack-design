// @vegastack icon-sunset@0.6.0 sha256-8N3Abxd1hvjmtJXMfjcyoJCLs2ughsn6XtOtgxtL2Fw=

// Mirrored from lucide-animated (https://lucide-animated.com/r/sunset.json) — MIT.
// Generated by tooling/mirror-animated-icons.mjs — do NOT hand-edit; re-run the mirror to update.

"use client";

import type { Variants } from "motion/react";
import { motion, useAnimation, useReducedMotion } from "motion/react";
import type { HTMLAttributes } from "react";
import { useCallback, useImperativeHandle, useRef, useEffect } from "react";

import { cn } from "@vegastack/design";

export interface SunsetIconHandle {
  startAnimation: () => void;
  stopAnimation: () => void;
}

interface SunsetIconProps extends HTMLAttributes<HTMLDivElement> {
  size?: number | string;
  ref?: React.Ref<SunsetIconHandle>;
}

const ARROW_VARIANTS: Variants = {
  normal: {
    y: 0,
  },
  animate: {
    y: [0, 1, 0],
  },
};
const RAYS_VARIANTS: Variants = {
  normal: { opacity: 1 },
  animate: (i: number) => ({
    opacity: [0, 1],
    transition: { delay: i * 0.1, duration: 0.3 },
  }),
};

const SunsetIcon = ({
  onMouseEnter,
  onMouseLeave,
  className,
  size = "var(--icon-default)",
  ref,
  onPointerEnter,
  onPointerLeave,
  onPointerDown,
  onFocus,
  onBlur,
  ...props
}: SunsetIconProps) => {
  const arrowControls = useAnimation();
  const raysControls = useAnimation();
  const shouldReduceMotion = useReducedMotion();
  const runAnimation = useCallback(
    (
      control: typeof arrowControls,
      definition: Parameters<typeof arrowControls.start>[0],
      transitionOverride?: Parameters<typeof arrowControls.start>[1],
    ) => {
      if (shouldReduceMotion) {
        control.stop();
        return Promise.resolve();
      }
      return control.start(definition, transitionOverride);
    },
    [shouldReduceMotion],
  );
  const resetAnimation = useCallback(
    (
      control: typeof arrowControls,
      definition: Parameters<typeof arrowControls.start>[0],
      transitionOverride?: Parameters<typeof arrowControls.start>[1],
    ) => {
      if (shouldReduceMotion) {
        control.stop();
        control.set(definition);
        return Promise.resolve();
      }
      return control.start(definition, transitionOverride);
    },
    [shouldReduceMotion],
  );
  const isControlledRef = useRef(false);

  const startAnimation = () => {
    runAnimation(arrowControls, "animate");
    runAnimation(raysControls, "animate");
  };
  const stopAnimation = () => {
    resetAnimation(arrowControls, "normal");
    resetAnimation(raysControls, "normal");
  };
  useImperativeHandle(ref, () => {
    isControlledRef.current = true;
    return { startAnimation, stopAnimation };
  });

  useEffect(() => {
    if (shouldReduceMotion) stopAnimation();
  });

  const handlePointerEnter = (event: React.PointerEvent<HTMLDivElement>) => {
    onPointerEnter?.(event);
    if (!isControlledRef.current && event.pointerType !== "touch")
      startAnimation();
  };

  const handlePointerLeave = (event: React.PointerEvent<HTMLDivElement>) => {
    onPointerLeave?.(event);
    if (!isControlledRef.current && event.pointerType !== "touch")
      stopAnimation();
  };

  const handlePointerDown = (event: React.PointerEvent<HTMLDivElement>) => {
    onPointerDown?.(event);
    if (!isControlledRef.current && event.pointerType === "touch")
      startAnimation();
  };

  const handleFocus = (event: React.FocusEvent<HTMLDivElement>) => {
    onFocus?.(event);
    if (!isControlledRef.current) startAnimation();
  };

  const handleBlur = (event: React.FocusEvent<HTMLDivElement>) => {
    onBlur?.(event);
    if (!isControlledRef.current) stopAnimation();
  };

  return (
    <div
      className={cn(className)}
      onMouseEnter={onMouseEnter}
      onMouseLeave={onMouseLeave}
      onPointerEnter={handlePointerEnter}
      onPointerLeave={handlePointerLeave}
      onPointerDown={handlePointerDown}
      onFocus={handleFocus}
      onBlur={handleBlur}
      {...props}
    >
      <svg
        fill="none"
        height={size}
        stroke="currentColor"
        strokeLinecap="round"
        strokeLinejoin="round"
        strokeWidth="2"
        viewBox="0 0 24 24"
        width={size}
        xmlns="http://www.w3.org/2000/svg"
      >
        <motion.g
          animate={arrowControls}
          initial="normal"
          variants={ARROW_VARIANTS}
        >
          <path d="M12 10V2" />
          <path d="m16 6-4 4-4-4" />
        </motion.g>

        {[
          "m4.93 10.93 1.41 1.41",
          "M2 18h2",
          "M20 18h2",
          "m19.07 10.93-1.41 1.41",
          "M22 22H2",
          ,
        ].map((d, index) => (
          <motion.path
            animate={raysControls}
            custom={index + 1}
            d={d}
            initial="normal"
            key={d}
            variants={RAYS_VARIANTS}
          />
        ))}
        <path d="M16 18a4 4 0 0 0-8 0" />
      </svg>
    </div>
  );
};

SunsetIcon.displayName = "SunsetIcon";

export { SunsetIcon };
