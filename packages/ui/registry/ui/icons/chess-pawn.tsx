// @vegastack icon-chess-pawn@0.3.0 sha256-Nz7VG7tNG2QM8JsB3HzgrC0uUgtXsJzFVwxNT0C8rhg=

// Mirrored from lucide-animated (https://lucide-animated.com/r/chess-pawn.json) — MIT.
// Generated by tooling/mirror-animated-icons.mjs — do NOT hand-edit; re-run the mirror to update.

"use client";

import {
  motion,
  useAnimation,
  type Variants,
  useReducedMotion,
} from "motion/react";
import type { HTMLAttributes } from "react";
import { useCallback, useImperativeHandle, useRef, useEffect } from "react";

import { cn } from "@vegastack/design";

export interface ChessPawnIconHandle {
  startAnimation: () => void;
  stopAnimation: () => void;
}

interface ChessPawnIconProps extends HTMLAttributes<HTMLDivElement> {
  size?: number | string;
  ref?: React.Ref<ChessPawnIconHandle>;
}

const HEAD_VARIANTS: Variants = {
  normal: {
    x: 0,
    y: 0,
    scale: 1,
    rotate: 0,
    transition: {
      type: "spring",
      stiffness: 180,
      damping: 16,
    },
  },
  animate: {
    x: [0, -5, 5, 0],
    rotate: [0, -15, 15, 0],
    transition: {
      duration: 2.4,
      times: [0, 0.33, 0.66, 1],
      ease: "easeInOut",
    },
  },
};

const BODY_VARIANTS: Variants = {
  normal: {
    rotate: 0,
    transition: {
      type: "spring",
      stiffness: 260,
      damping: 16,
    },
  },
  animate: {
    rotate: [0, 5, 5, 5, 3, 0],
    transition: {
      duration: 1.8,
      times: [0, 0.08, 0.3, 0.52, 0.72, 1],
      ease: "easeInOut",
    },
  },
};

const ChessPawnIcon = ({
  onMouseEnter,
  onMouseLeave,
  className,
  size = "var(--icon-default)",
  ref,
  onPointerEnter,
  onPointerLeave,
  onPointerDown,
  onFocus,
  onBlur,
  ...props
}: ChessPawnIconProps) => {
  const headControls = useAnimation();
  const bodyControls = useAnimation();
  const shouldReduceMotion = useReducedMotion();
  const runAnimation = useCallback(
    (
      control: typeof headControls,
      definition: Parameters<typeof headControls.start>[0],
      transitionOverride?: Parameters<typeof headControls.start>[1],
    ) => {
      if (shouldReduceMotion) {
        control.stop();
        return Promise.resolve();
      }
      return control.start(definition, transitionOverride);
    },
    [shouldReduceMotion],
  );
  const resetAnimation = useCallback(
    (
      control: typeof headControls,
      definition: Parameters<typeof headControls.start>[0],
      transitionOverride?: Parameters<typeof headControls.start>[1],
    ) => {
      if (shouldReduceMotion) {
        control.stop();
        control.set(definition);
        return Promise.resolve();
      }
      return control.start(definition, transitionOverride);
    },
    [shouldReduceMotion],
  );
  const isControlledRef = useRef(false);

  const startAnimation = () => {
    runAnimation(headControls, "animate");
    runAnimation(bodyControls, "animate");
  };
  const stopAnimation = () => {
    resetAnimation(headControls, "normal");
    resetAnimation(bodyControls, "normal");
  };
  useImperativeHandle(ref, () => {
    isControlledRef.current = true;
    return { startAnimation, stopAnimation };
  });

  useEffect(() => {
    if (shouldReduceMotion) stopAnimation();
  });

  const handlePointerEnter = (event: React.PointerEvent<HTMLDivElement>) => {
    onPointerEnter?.(event);
    if (!isControlledRef.current && event.pointerType !== "touch")
      startAnimation();
  };

  const handlePointerLeave = (event: React.PointerEvent<HTMLDivElement>) => {
    onPointerLeave?.(event);
    if (!isControlledRef.current && event.pointerType !== "touch")
      stopAnimation();
  };

  const handlePointerDown = (event: React.PointerEvent<HTMLDivElement>) => {
    onPointerDown?.(event);
    if (!isControlledRef.current && event.pointerType === "touch")
      startAnimation();
  };

  const handleFocus = (event: React.FocusEvent<HTMLDivElement>) => {
    onFocus?.(event);
    if (!isControlledRef.current) startAnimation();
  };

  const handleBlur = (event: React.FocusEvent<HTMLDivElement>) => {
    onBlur?.(event);
    if (!isControlledRef.current) stopAnimation();
  };

  return (
    <div
      className={cn(className)}
      onMouseEnter={onMouseEnter}
      onMouseLeave={onMouseLeave}
      onPointerEnter={handlePointerEnter}
      onPointerLeave={handlePointerLeave}
      onPointerDown={handlePointerDown}
      onFocus={handleFocus}
      onBlur={handleBlur}
      {...props}
    >
      <svg
        fill="none"
        height={size}
        stroke="currentColor"
        strokeLinecap="round"
        strokeLinejoin="round"
        strokeWidth="2"
        viewBox="0 0 24 24"
        width={size}
        xmlns="http://www.w3.org/2000/svg"
      >
        <motion.g
          animate={bodyControls}
          initial="normal"
          style={{ transformBox: "view-box", transformOrigin: "12px 21px" }}
          variants={BODY_VARIANTS}
        >
          <path d="M5 20a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2v1a1 1 0 0 1-1 1H6a1 1 0 0 1-1-1z" />
          <path d="m14.5 10 1.5 8" />
          <path d="M7 10h10" />
          <path d="m8 18 1.5-8" />
        </motion.g>
        <motion.circle
          animate={headControls}
          cx="12"
          cy="6"
          initial="normal"
          r="4"
          style={{ transformBox: "fill-box", transformOrigin: "center" }}
          variants={HEAD_VARIANTS}
        />
      </svg>
    </div>
  );
};

ChessPawnIcon.displayName = "ChessPawnIcon";

export { ChessPawnIcon };
