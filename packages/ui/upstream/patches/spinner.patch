# component: spinner
# decisions: DOC-2, ICO-8
# hunks:
#   cn: `import { cn } from "cn"` becomes `@vegastack/design` — our npm layer owns `cn` (DOC-2).
#   ICO-8: the mark is `LoaderIcon` (lucide `loader`) in place of upstream's `Loader2Icon`, which
#          is an alias of `LoaderCircle`. One loader shape across the system, so this spinner, the
#          Toast and Sonner loading icons and `StatusIcon`'s `progress` are the same object
#          spinning. The `data-slot`, the `role="status"`, the `aria-label` and the
#          `size-4 animate-spin` recipe stay upstream's.
#   formatting: the repository formatter (prettier defaults: semicolons, trailing commas)
#               reflows the file; those hunks carry no decision of their own.
diff --git a/spinner.tsx b/spinner.tsx
index 4b753d2..65107e3 100644
--- a/spinner.tsx
+++ b/spinner.tsx
@@ -1,10 +1,16 @@
-import { cn } from "cn"
-import { Loader2Icon } from "lucide-react"
+import { cn } from "@vegastack/design";
+import { LoaderIcon } from "lucide-react";
 
 function Spinner({ className, ...props }: React.ComponentProps<"svg">) {
   return (
-    <Loader2Icon data-slot="spinner" role="status" aria-label="Loading" className={cn("size-4 animate-spin", className)} {...props} />
-  )
+    <LoaderIcon
+      data-slot="spinner"
+      role="status"
+      aria-label="Loading"
+      className={cn("size-4 animate-spin", className)}
+      {...props}
+    />
+  );
 }
 
-export { Spinner }
+export { Spinner };
