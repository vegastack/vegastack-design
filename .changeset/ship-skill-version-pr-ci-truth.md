---
---

📚 **Ship skill** — corrected a false claim about CI on the Version PR: runs on the changesets bot branch sit at `action_required` and never execute, so the publish is gated by `quality-gate` on main before and after the merge, not by checks on that PR.
