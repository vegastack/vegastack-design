---
"@vegastack/ui": patch
---

Record each registry part's `data-*` attributes and CSS variables in `component-contracts.json` (`dataAttributes`, AST-extracted and drift-checked) so the docs API tables and the agent markdown export list them.
