---
"@vegastack/design": patch
---

PageHeader gains `tabs` and `view` slots: a list page's view tabs sit on their own row directly under the title, with the layout switch pinned to its end. list-page-01 moves its Mine | Team scope (now default Tabs) and Grid | List toggle there.
