---
"@vegastack/ui": minor
---

Every control is on the surface ladder: hover climbs one rung and **every** control now has a
pressed step (previously only the solid primary Button did). Select's trigger hovered only in dark
and now hovers in both themes; the current sidebar row rests on `surface-3` so hovering it still
moves; ComparisonMatrix and PricingSection stop using `info` for promotion.
