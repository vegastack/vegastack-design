---
"@vegastack/ui": patch
"@vegastack/design": patch
---

🔧 Combobox's clear and chip-remove × no longer move when pressed (API-31). RecordLayout gains `RecordLayoutPanels`, `RecordTabCount` and `RecordLayoutMainSkeleton`; RecordChip gains `RecordChipMenu`, the flush picker popover a pill opens. RecordLayout picks the rail from its own width (container query, `useRecordLayoutWide`) and gains `RecordTabsRow` / `RecordTabsActions`.
