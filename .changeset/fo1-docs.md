---
---

📚 **Field, Checkbox, FieldInline, Textarea, Motion** — every page that described the error as
`role="alert"`, the shake as per-control, or the textarea focus treatment as a removed outline now
says what the code does. Textarea gains the `size` example it never had, NumberField gains its
invalid fixture and folds its "Scope" table into Usage, and Label documents `layout`.
[docs](https://design.vegastack.com/docs/components/field)
