---
"@vegastack/design": patch
---

The bundled design-system skill now documents the Button-as-link pattern: compose the anchor via
`render` and pass `nativeButton={false}`, matching Base UI's native-button contract.
