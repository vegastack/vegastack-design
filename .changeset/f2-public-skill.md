---
"@vegastack/design": patch
---

Refreshes the shipped design-system skill for Button's `variant × tone` matrix, the single
`xs · sm · md · lg` size vocabulary, and `IconButton` as the only icon-only path. No runtime code
changed.
