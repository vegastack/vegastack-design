---
---

📚 **Docs** — `AGENTS.md` is a rulebook again (150 lines): locked decisions as one-liners with dates, the three verification loops, and the four artefacts a PR carries. The CI history it used to carry moved to `docs/ledger/operator-review.md`, and the `component`, `review`, and `ship` skills now describe only machinery that exists.
