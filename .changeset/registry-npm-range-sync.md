---
---

Correct the npm dependency ranges every registry item declares so they track the packages actually
being published. No version bump: this fixes what the unpublished 0.3.0 release will contain, rather
than warranting a release of its own.
