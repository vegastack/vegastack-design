# design-lint over the registry + prettier over the staged set. ~4s, never a browser.
# The symlink filter (skills/* are symlinks) is why this is a script and not two shell lines.
node tooling/pre-commit.mjs
