# Generated by the registry pipeline with byte-stable JSON formatting and integrity metadata.
# Formatting these files creates drift that `pnpm registry:build` immediately reverses.
apps/docs/public/r/

# pnpm owns the lockfile's serialization, and it rewrites it on every install — so a formatted
# lockfile is drift that the next `pnpm add` immediately reverses, exactly like the registry output
# above. Without this, the pre-commit format gate fails after every dependency change.
pnpm-lock.yaml

# Same rule, applied to the surfaces `tooling/sync-component-derived.mjs` and the token build own
# outright. Formatting them is drift that `pnpm design:derived` / the token build immediately
# reverses, so prettier and `design:derived:check` would each undo the other with whichever ran last
# "winning". These files all declare themselves generated in their first lines.
#
# NOT ignored, deliberately: `packages/ui/component-contracts.json` AND `packages/ui/registry.json`
# are hand-maintained AUTHORITIES rather than outputs, so they are formatted like any other source
# file. (G1-b weighed ignoring registry.json — it is the input to a generator, and the pre-commit
# hook reflows it for any branch that stages it. Ignoring it would make the churn permanent instead
# of ending it: nothing would ever bring the file to a canonical shape, and every batch would keep
# rediscovering the same diff. `prettier --check .` in `pnpm lint` ends it once, repo-wide.)
*.generated.ts
*.generated.tsx
*.generated.json
docs/ledger/component-matrix.md
packages/design-tokens/src/tokens.ts
docs/research/design-md-audit/audit-register.json
docs/research/design-md-audit/audits/coverage.json

# Immutable third-party research snapshots are byte-for-byte evidence. Their manifests, not the
# repository formatter, own their serialization.
docs/research/design-md-audit/sources/
docs/research/design-md-references/Vercel_DESIGN.md
