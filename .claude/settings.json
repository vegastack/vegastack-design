{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "//": "A hook cannot call a model, so the model has to be handed the facts. These two events read .gates/last-failure.json and inject a compact digest, so a blocked commit or push is already in context on the next turn with no copy-paste. tooling/gates-digest.mjs prints nothing when there is no failure, which is why this is safe to run on every prompt.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node tooling/gates-digest.mjs"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node tooling/gates-digest.mjs"
          }
        ]
      }
    ]
  }
}
