// @vegastack icon-grip@0.7.0 sha256-T+D/Nu0UzZ6iOOQ0IAEP/3sHAtbl4+UZCOgFhp9b4qs=

// Mirrored from lucide-animated (https://lucide-animated.com/r/grip.json) — MIT.
// Generated by tooling/mirror-animated-icons.mjs — do NOT hand-edit; re-run the mirror to update.

"use client";

import type { Variants } from "motion/react";
import { createAnimatedIcon, type AnimatedIconHandle } from "@vegastack/design/create-animated-icon";

const VARIANTS: Variants = {
  normal: { opacity: 1, transition: { duration: 0.25 } },
  animate: (index: number) => ({ opacity: [1, 0.3, 0.3, 1], transition: { delay: index * 0.07, duration: 1.1, times: [0, 0.2, 0.8, 1] } }),
};

export type GripIconHandle = AnimatedIconHandle;

export const GripIcon = createAnimatedIcon({
  name: "GripIcon",
  elements: [
    { tag: "motion.circle", cx: 19, cy: 5, r: "1", variants: VARIANTS, initial: "normal", custom: 0 },
    { tag: "motion.circle", cx: 19, cy: 12, r: "1", variants: VARIANTS, initial: "normal", custom: 1 },
    { tag: "motion.circle", cx: 12, cy: 5, r: "1", variants: VARIANTS, initial: "normal", custom: 2 },
    { tag: "motion.circle", cx: 19, cy: 19, r: "1", variants: VARIANTS, initial: "normal", custom: 3 },
    { tag: "motion.circle", cx: 12, cy: 12, r: "1", variants: VARIANTS, initial: "normal", custom: 4 },
    { tag: "motion.circle", cx: 5, cy: 5, r: "1", variants: VARIANTS, initial: "normal", custom: 5 },
    { tag: "motion.circle", cx: 12, cy: 19, r: "1", variants: VARIANTS, initial: "normal", custom: 6 },
    { tag: "motion.circle", cx: 5, cy: 12, r: "1", variants: VARIANTS, initial: "normal", custom: 7 },
    { tag: "motion.circle", cx: 5, cy: 19, r: "1", variants: VARIANTS, initial: "normal", custom: 8 },
  ],
  start: async ({ control, flags, run }) => {
    if (flags.isAnimating) return;
    flags.isAnimating = true;
    await run(control(), "animate");
    await run(control(), "normal");
    flags.isAnimating = false;
  },
  stop: async ({ control, flags, reset }) => {
    if (!flags.isAnimating) return;
    await reset(control(), "normal");
    flags.isAnimating = false;
  },
});
