// @vegastack icon-projector@0.8.2 sha256-hl5VG4cXyD5sVPUe593G5yxXTHrpnT2P3WgI51T3sxQ=

// Mirrored from lucide-animated (https://lucide-animated.com/r/projector.json) — MIT.
// Generated by tooling/mirror-animated-icons.mjs — do NOT hand-edit; re-run the mirror to update.

"use client";

import type { Variants } from "motion/react";
import { createAnimatedIcon, type AnimatedIconHandle } from "@vegastack/design/create-animated-icon";

const RAY_LINE_VARIANTS: Variants = {
  hidden: { pathLength: 0, opacity: 0 },
  animate: { pathLength: 1, opacity: 1, transition: { duration: 0.4, ease: "easeOut" } },
  visible: { pathLength: 1, opacity: 1 },
};

const PROJECTOR_BODY_VARIANTS: Variants = { normal: { scale: 1, y: 0 }, animate: { scale: [1, 1.08, 1], y: [0, -1, 0], transition: { duration: 0.8, ease: "easeInOut" } } };

export type ProjectorIconHandle = AnimatedIconHandle;

export const ProjectorIcon = createAnimatedIcon({
  name: "ProjectorIcon",
  elements: [
    { tag: "motion.path", d: "M5 7 3 5", variants: RAY_LINE_VARIANTS, initial: "visible", group: "path" },
    { tag: "motion.path", d: "M9 6V3", variants: RAY_LINE_VARIANTS, initial: "visible", group: "path" },
    { tag: "motion.path", d: "m13 7 2-2", variants: RAY_LINE_VARIANTS, initial: "visible", group: "path" },
    {
      tag: "motion.g",
      variants: PROJECTOR_BODY_VARIANTS,
      initial: "normal",
      group: "body",
      children: [{ tag: "circle", cx: "9", cy: "13", r: "3" }, "M11.83 12H20a2 2 0 0 1 2 2v4a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2v-4a2 2 0 0 1 2-2h2.17", "M16 16h2"],
    },
  ],
  groups: ["path", "body"],
  start: async ({ control, run }) => {
    (async () => {
      run(control("body"), "animate").catch(() => {
        // ignore when interrupted by a new animation
      });
      await run(control("path"), "hidden");
      await run(control("path"), "animate");
    })().catch(() => {
      // ignore when interrupted
    });
  },
  stop: ({ control, run }) => {
    (() => {
      run(control("body"), "normal").catch(() => {
        // ignore when interrupted
      });
      run(control("path"), "visible").catch(() => {
        // ignore when interrupted
      });
    })();
  },
});
