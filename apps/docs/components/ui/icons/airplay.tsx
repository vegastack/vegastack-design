// @vegastack icon-airplay@0.6.0 sha256-QdCm06S9GpMElhnvpX36trtH1Cxvu+nOk6B0Kvm8/sg=

// Mirrored from lucide-animated (https://lucide-animated.com/r/airplay.json) — MIT.
// Generated by tooling/mirror-animated-icons.mjs — do NOT hand-edit; re-run the mirror to update.

"use client";

import { easeInOut, easeOut } from "motion/react";
import { createAnimatedIcon, type AnimatedIconHandle } from "@vegastack/design/create-animated-icon";

const DURATION = 0.3;

const SCREEN_VARIANTS = {
  normal: { opacity: 1, pathLength: 1, pathOffset: 0, transition: { duration: DURATION } },
  animate: { opacity: [0, 1], pathLength: [0, 1], pathOffset: [1, 0], transition: { duration: DURATION * 2, ease: easeInOut } },
};

const TRIANGLE_VARIANTS = {
  normal: { scale: 1, opacity: 1, transition: { duration: DURATION } },
  animate: { scale: [0.6, 1.1, 1], opacity: [0, 1], transition: { duration: DURATION * 2, ease: easeOut } },
};

export type AirplayIconHandle = AnimatedIconHandle;

export const AirplayIcon = createAnimatedIcon({
  name: "AirplayIcon",
  elements: [
    { tag: "motion.path", d: "M5 17H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v10a2 2 0 0 1-2 2h-1", variants: SCREEN_VARIANTS },
    { tag: "motion.path", d: "M12 15l5 6H7z", variants: TRIANGLE_VARIANTS },
  ],
  start: async ({ control, run }) => {
    await run(control(), "animate");
    run(control(), "normal");
  },
  stop: ({ control, reset }) => {
    reset(control(), "normal");
  },
});
