---
title: Introduction
description: VegaStack Design — Base UI + Tailwind v4 design system, tokens, and a private registry.
audience: public
---

{/* COMPONENT_COUNT_INTRO:START — generated by tooling/sync-component-derived.mjs. DO NOT EDIT. */}

VegaStack Design is the internal design system: **design tokens** (public npm), **108 components**
(private shadcn registry, copy-in), a **live Fumadocs showcase**, and **agent-ready** metadata.

{/* COMPONENT_COUNT_INTRO:END */}

## Architecture

- **Tokens** — `@vegastack/design-tokens` ships `theme.css` (OKLCH `:root` + `.dark` + `@theme inline`) and a
  typed `{ light, dark }` model. One-file override: redefine `--primary` and every component repaints.
- **Components** — authored on [Base UI](https://base-ui.com) with CVA variants, `cn()` from
  `@vegastack/design`, and `data-*` state hooks. Configure the private registry first, then install
  with `shadcn add @vegastack/<name>` (copy-in).
- **Showcase** — this site. Every component page renders the **real shipped source** via a live preview.

## Foundations

Start with [Colors](/docs/foundations/colors) and [Typography](/docs/foundations/typography), then
[Icons](/docs/foundations/icons) and [Motion](/docs/foundations/motion).

## Components

{/* COMPONENT_COUNT_CATALOG:START — generated by tooling/sync-component-derived.mjs. DO NOT EDIT. */}

Browse the 108-component library, grouped by role — Buttons & Actions, Inputs & Controls, Overlays,
Menus & Commands, Navigation, Layout & Structure, Data Display, Feedback & Status, Content & Typography,
Chat & Communication, and Brand & Marketing. Start with [Button](/docs/components/button).

{/* COMPONENT_COUNT_CATALOG:END */}

## Utilities

CSS-only helpers that ship with `@vegastack/design-tokens` — [Shimmer](/docs/utilities/shimmer) and
[Scroll Fade](/docs/utilities/scroll-fade).

## Installation

Start with [Install From VegaStack Registry](/docs/install) before copying component commands.
