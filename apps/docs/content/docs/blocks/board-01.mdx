---
title: board-01
description: A board page — a FilterBar over Board, with lanes named by their count, a paged Backlog, linked cards with menus, and No matches.
audience: public
registry: board-01
preview: board01Demo
---

## What it is

A board page — a PageHeader over a FilterBar and Board, with lanes named by their count, a paged Backlog, cards that link to each task with their own menu, and a No matches state. It is a **block**: you pull it once with `shadcn add`, and then you own it —
nothing re-pulls it and no integrity hash tracks your edits, unlike the components it composes.

`BoardView` in `components/` holds the filters and the board; the page is the server-safe frame around it.

## Install

```npm
npx shadcn@latest add @vegastack/board-01
```

<RegistryInstallCallout />

This installs the block's own files plus the components it composes: `app-shell`, `avatar`, `board`, `button`, `data-table-parts`, `dropdown-menu`, `empty`, `filter-bar`, `page-header`.

## Preview

<ComponentPreview name="board01Demo" />

## Anatomy

- `AppShellPage size="full"` › `PageHeader` h1 "Tasks" with its "New task" action.
- `FilterBar` — search plus an Assignee facet (a radio menu), with the chosen assignee shown as a
  removable chip and "Clear filters" while anything is filtered.
- `Board` — each lane is a region named with its count ("Backlog, 6 tasks", via `countLabel`).
  The Backlog is paged: its `count` is the server's total and `loadMore` fetches the rest. Each
  card is a link to its task (`getItemHref`), named for moves (`getItemLabel`), with its own
  Edit / Archive items above Move in one ⋯ menu (`getItemActions`). An empty lane says so ("No
  tasks", or "No matching tasks" while filtering).
- **No matches** — when the filters hide every card, the board gives way to an `Empty` with "Clear
  filters".

## Filters and moves

The filters narrow what each lane **shows**; a move always lands in the full lane, which is why
`onMove` updates the unfiltered columns. Space on a focused card lifts it into keyboard move mode;
Enter follows its link.

## Files

| File                                 | Registry type        | Target                                   |
| ------------------------------------ | -------------------- | ---------------------------------------- |
| `board-01/components/board-view.tsx` | `registry:component` | `app/board-01/components/board-view.tsx` |
| `board-01/page.tsx`                  | `registry:page`      | `app/board-01/page.tsx`                  |

## Do / Don't

<DoDont
  do="Name each lane with its total, page long lanes, and link cards to their records."
  dont="Add a view switch nothing reads, or put buttons inside a card."
/>
