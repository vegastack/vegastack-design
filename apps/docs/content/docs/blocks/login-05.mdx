---
title: login-05
description: A simple email-only login page. Composed from button, field, input.
audience: public
registry: login-05
preview: login05Demo
---

## What it is

A simple email-only login page. It is a **block**: you pull it once with `shadcn add`, and then you own it —
nothing re-pulls it and no integrity hash tracks your edits, unlike the components it composes.

## Install

```npm
npx shadcn@latest add @vegastack/login-05
```

<RegistryInstallCallout />

This installs the block's own files plus the components it composes: `button`, `field`, `input`.

## Preview

<ComponentPreview name="login05Demo" />

## Files

| File                                 | Registry type        | Target                                   |
| ------------------------------------ | -------------------- | ---------------------------------------- |
| `login-05/components/login-form.tsx` | `registry:component` | `app/login-05/components/login-form.tsx` |
| `login-05/page.tsx`                  | `registry:page`      | `app/login-05/page.tsx`                  |

## Do / Don't

<DoDont
  do="Pull it once, then edit it freely — replace the sample data, the links and the copy with your own."
  dont="Treat it as an update-tracked component, or re-pull it and expect your edits to survive."
/>
