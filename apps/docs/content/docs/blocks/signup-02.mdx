---
title: signup-02
description: A two column signup page with a cover image. Composed from button, field, input.
audience: public
registry: signup-02
preview: signup02Demo
---

## What it is

A two column signup page with a cover image. It is a **block**: you pull it once with `shadcn add`, and then you own it —
nothing re-pulls it and no integrity hash tracks your edits, unlike the components it composes.

## Install

```npm
npx shadcn@latest add @vegastack/signup-02
```

<RegistryInstallCallout />

This installs the block's own files plus the components it composes: `button`, `field`, `input`.

## Preview

<ComponentPreview name="signup02Demo" />

## Files

| File                                   | Registry type        | Target                                     |
| -------------------------------------- | -------------------- | ------------------------------------------ |
| `signup-02/components/signup-form.tsx` | `registry:component` | `app/signup-02/components/signup-form.tsx` |
| `signup-02/page.tsx`                   | `registry:page`      | `app/signup-02/page.tsx`                   |

## Do / Don't

<DoDont
  do="Pull it once, then edit it freely — replace the sample data, the links and the copy with your own."
  dont="Treat it as an update-tracked component, or re-pull it and expect your edits to survive."
/>
