---
title: sidebar-07
description: A sidebar that collapses to icons. Composed from avatar, breadcrumb, collapsible, dropdown-menu.
audience: public
registry: sidebar-07
preview: sidebar07Demo
---

## What it is

A sidebar that collapses to icons. It is a **block**: you pull it once with `shadcn add`, and then you own it —
nothing re-pulls it and no integrity hash tracks your edits, unlike the components it composes.

## Install

```npm
npx shadcn@latest add @vegastack/sidebar-07
```

<RegistryInstallCallout />

This installs the block's own files plus the components it composes: `avatar`, `breadcrumb`, `collapsible`, `dropdown-menu`, `separator`, `sidebar`.

## Preview

<ComponentPreview name="sidebar07Demo" />

## Files

| File                                      | Registry type        | Target                                        |
| ----------------------------------------- | -------------------- | --------------------------------------------- |
| `sidebar-07/components/app-sidebar.tsx`   | `registry:component` | `app/sidebar-07/components/app-sidebar.tsx`   |
| `sidebar-07/components/nav-main.tsx`      | `registry:component` | `app/sidebar-07/components/nav-main.tsx`      |
| `sidebar-07/components/nav-projects.tsx`  | `registry:component` | `app/sidebar-07/components/nav-projects.tsx`  |
| `sidebar-07/components/nav-user.tsx`      | `registry:component` | `app/sidebar-07/components/nav-user.tsx`      |
| `sidebar-07/components/team-switcher.tsx` | `registry:component` | `app/sidebar-07/components/team-switcher.tsx` |
| `sidebar-07/page.tsx`                     | `registry:page`      | `app/sidebar-07/page.tsx`                     |

## Do / Don't

<DoDont
  do="Pull it once, then edit it freely — replace the sample data, the links and the copy with your own."
  dont="Treat it as an update-tracked component, or re-pull it and expect your edits to survive."
/>
