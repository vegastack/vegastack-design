---
title: Elevation
description: Flat bordered surfaces, one overlay shadow, and one opt-in primary-action finish.
audience: public
---

VegaStack is **flat by default**. Borders separate surfaces; shadow communicates either a real
overlay layer or the explicitly approved primary-action finish. There is no generic elevation ramp.

<ShadowScale />

## Resolved tokens

{/* DESIGN-TOKENS:elevation:START */}
{/* GENERATED by tooling/sync-design-md.mjs from resolved DTCG tokens. Do not hand-edit. */}
| Token | Light | Dark |
|---|---|---|
| `--shadow-overlay` | `0 4px 14px -4px oklch(0.13 0.002 75 / 0.1), 0 2px 4px -2px oklch(0.13 0.002 75 / 0.06)` | `0 8px 28px -6px oklch(0 0 0 / 0.48), 0 2px 8px -2px oklch(0 0 0 / 0.4)` |
| `--shadow-lit` | `inset 0 1px 0 0 oklch(1 0 0 / 0.12), 0 1px 2px 0 oklch(0.13 0.002 75 / 0.18), 0 2px 6px -2px oklch(0.13 0.002 75 / 0.14)` | `inset 0 1px 0 0 oklch(1 0 0 / 0.35), 0 2px 6px -2px oklch(0 0 0 / 0.5)` |
{/* DESIGN-TOKENS:elevation:END */}

## The two roles

- **`shadow-overlay`** — portaled popovers, menus, tooltips, dialogs, sheets, and similar surfaces
  that genuinely float above the page.
- **`shadow-lit`** — an opt-in top-light/ambient finish for approved **primary actions only**. It is
  not a card or container elevation and never creates a shadow ladder.

Cards, panels, rows, inputs, and resting controls use `border-border` without a drop shadow. Sticky
surfaces gain a separating border when they detach from adjacent content; they do not acquire an
invented elevation tier.

## Equal-elevation nesting

Do not nest a bordered card inside another bordered card at the same visual elevation. Group the
content with spacing, a divider, or a deliberately different surface role. When a nested region is
necessary, its border/radius must communicate hierarchy rather than duplicate the outer container.

## Stacking bands

Use `z-(--z-raised)` for a local raise inside a component and `z-(--z-overlay)` for portaled
surfaces. Raw z-index ladders are forbidden. Sonner manages its documented toast layer above modal
content so a toast remains visible when emitted from a dialog.

<DoDont
  do="Use border-border for flat surfaces, shadow-overlay for real overlays, and shadow-lit only for approved primary actions."
  dont="Create shadow-sm/md/lg tiers, elevate cards, or nest equal-elevation cards."
/>
