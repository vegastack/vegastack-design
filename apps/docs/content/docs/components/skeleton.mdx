---
title: Skeleton
description: A pulsing placeholder that holds the shape of content while it loads — one div, sized and shaped by utility classes.
audience: public
registry: skeleton
status: stable
since: 0.1.0
a11y: decorative placeholder
preview: skeleton
---

## Install

<InstallSteps name="skeleton" />

## Usage

```tsx
import { Skeleton } from "@/components/ui/skeleton";

<Skeleton className="h-[20px] w-[100px] rounded-full" />;
```

<ComponentPreview name="skeleton" file="components/preview/skeleton.tsx" />

## Examples

### Avatar

Give the media block the avatar's own size and radius so the layout does not jump when the image
arrives.

<ComponentPreview
  name="skeletonAvatar"
  file="components/preview/skeleton.tsx"
/>

### Card

Skeletons sit inside the real container. Keep the card, the header and the content; replace only
what is still loading.

<ComponentPreview name="skeletonCard" file="components/preview/skeleton.tsx" />

### Text

Stack line skeletons at the line height of the copy they stand in for, and cut the last one short —
a full-width final line reads as a block, not as text.

<ComponentPreview name="skeletonText" file="components/preview/skeleton.tsx" />

### Form

Mirror the label/control rhythm of the form so the fields land where the skeleton put them.

<ComponentPreview name="skeletonForm" file="components/preview/skeleton.tsx" />

### Table

Repeat one row shape for as many rows as the first page will hold. Keep the count fixed — a
skeleton whose row count moves is a second layout shift.

<ComponentPreview name="skeletonTable" file="components/preview/skeleton.tsx" />

### RTL

The placeholder is direction-agnostic; sizing comes from the utilities you pass, and the
surrounding flex or grid flips with the document.

<ComponentPreview name="skeletonRtl" file="components/preview/skeleton.tsx" />

## API Reference

<ApiTable path="lib/api-props.ts" name="SkeletonProps" />

## Accessibility

- A skeleton is decorative: it carries no role, no text and no accessible name, so screen readers
  skip it rather than announcing a wall of empty boxes.
- Announce the loading state once, on the region that owns it — `aria-busy="true"` on the container,
  or a live region for a longer wait — rather than on each placeholder.
- The pulse is `animate-pulse`, and the global reduced-motion reset in `base.css` stops it for
  anyone who asks for less motion. The placeholder stays visible.
- The fill is `bg-muted`, which is a surface rather than text, so it carries no contrast obligation
  of its own.

<StatesTested name="skeleton" />

## Do / Don't

<DoDont
  do="Size each skeleton to the element it replaces, so the real content lands without a layout shift."
  dont="Render skeletons for an empty result — an empty state explains what happened; a skeleton implies data is coming."
/>

## Deviations

Upstream's file plus `packages/ui/upstream/patches/skeleton.patch`. Every hunk:

- **DOC-2** — `cn` is imported from `@vegastack/design`. Nothing else differs: the element, the
  `data-slot` and the `animate-pulse rounded-md bg-muted` recipe are upstream's.
