---
title: Transcript
description: A timestamped, speaker-labelled transcript of a recording that follows playback, seeks from each line, and searches with highlighted matches.
audience: public
registry: transcript
status: stable
since: 0.18.0
a11y: labelled scroll region with a list of lines
preview: transcript
---

## Install

<InstallSteps name="transcript" />

## Usage

```tsx
import {
  Transcript,
  TranscriptList,
  TranscriptSearch,
} from "@/components/ui/transcript";

<Transcript
  aria-label="Meeting transcript"
  segments={segments}
  speakerName={(id) => people[id].name}
  currentTime={time}
  onSeek={(seconds) => seekTo(seconds)}
  className="h-96"
>
  <TranscriptSearch />
  <TranscriptList />
</Transcript>;
```

Each segment is `{ id, start, end?, speaker, text }`, with `start` and `end` in seconds and the
segments sorted by `start`. Each turn is a header — the time, the speaker's coloured dot and name
— with the text full width below it, the same layout on a phone and a desktop. The row playing at `currentTime` is marked current, and while the
transcript is following it stays centred. A reader who scrolls pauses following, and "Back to
current line" brings it back.

Long transcripts mount progressively: the first `batchSize` turns (100) at once and the rest in
idle-time batches, with the current line and the current search match always mounted before
anything scrolls to them.

## Scope

- **Owns:** the current line, following it, pausing on a user scroll, seek buttons, search with
  `<mark>` highlights, speaker colours and chips, progressive mounting, and the empty and loading
  states.
- **Does not own:** playback or the clock. Pass `currentTime` from your player, and seek it in
  `onSeek`.
- **Compose with:** [`AudioPlayer`](/docs/components/audio-player) or
  [`VideoPlayer`](/docs/components/video-player) through their `mediaRef`. Scrolling is
  [`MessageScroller`](/docs/components/message-scroller)'s engine.

## Anatomy

`Transcript` is a compound component. Every exported part, with the `data-slot` it renders
(generated from the canonical source):

<Anatomy name="transcript" />

## Examples

### Current line and seek

Each timestamp is a button named "Play from 0:15" that calls `onSeek` with the segment's start.
Here it moves a local clock, so clicking a line makes it the current one.

Rows opt out of `MessageScrollerItem`'s `content-visibility: auto`, so every row is laid out at
mount. A skipped row reports an estimated height, and a follow or search jump measured against that
estimate lands past its line once the real rows render; laying them all out costs once, and a
`currentTime` tick still re-renders at most two rows.

<ComponentPreview name="transcript" file="components/preview/transcript.tsx" />

### With audio

Read `currentTime` from the player's `<audio>` element on `timeupdate`, and set it in `onSeek`.
The transcript follows the playing line until you scroll it yourself.

<ComponentPreview
  name="transcriptWithAudio"
  file="components/preview/transcript.tsx"
/>

### Search

`TranscriptSearch` wraps each match in `<mark>`. Enter and Shift+Enter, or the
arrow buttons, move between matches. Each move scrolls the match into view,
pauses following, and announces its position ("2 of 5"). Pass `query` and
`onQueryChange` to control the query yourself, for example to keep it in the URL.

<ComponentPreview
  name="transcriptSearch"
  file="components/preview/transcript.tsx"
/>

### Speakers and rename

`TranscriptSpeakers` renders one chip per speaker, with the dot colour their turns use. Pass
`onSpeakerRename(id, name)` to the Transcript and each chip gets a ✎ button: it turns into a field
where Enter or leaving the field saves and Escape cancels. Store the name and return it from
`speakerName`. Put the search and the chips in one toolbar row above the list.

```tsx
<Transcript
  aria-label="Meeting transcript"
  segments={segments}
  speakerName={(id) => names[id] ?? id}
  onSpeakerRename={(id, name) => renameSpeaker(id, name)}
>
  <div className="flex flex-wrap items-center gap-2">
    <TranscriptSearch className="flex-1" />
    <TranscriptSpeakers />
  </div>
  <TranscriptList />
</Transcript>
```

<ComponentPreview
  name="transcriptSpeakers"
  file="components/preview/transcript.tsx"
/>

### Read-only

Without `onSeek` the timestamps are plain text and no row has a button. Without `currentTime`
no line is current.

<ComponentPreview
  name="transcriptReadOnly"
  file="components/preview/transcript.tsx"
/>

### Loading and empty

`loading` shows skeleton lines and marks the list busy. With no segments and nothing loading,
the list shows `emptyState` ("No transcript yet" by default).

<ComponentPreview
  name="transcriptStates"
  file="components/preview/transcript.tsx"
/>

## API Reference

<ApiTable
  path="../../packages/ui/registry/ui/transcript.tsx"
  name="TranscriptProps"
/>

### TranscriptSearch

<ApiTable
  path="../../packages/ui/registry/ui/transcript.tsx"
  name="TranscriptSearchProps"
/>

### TranscriptSpeakers

<ApiTable
  path="../../packages/ui/registry/ui/transcript.tsx"
  name="TranscriptSpeakersProps"
/>

### TranscriptList

<ApiTable
  path="../../packages/ui/registry/ui/transcript.tsx"
  name="TranscriptListProps"
/>

## Accessibility

- The list is a scroll region named by `aria-label` and reachable with <kbd>Tab</kbd>. Its lines
  are a `list` of `listitem`s. The current line has `aria-current="true"` and starts with a
  visually hidden "Now playing".
- The line is not a live region, so playback never talks over the recording. The one live region
  announces search positions ("1 of 3", "No matches").
- A seek button's name, "Play from 0:15", contains its visible time. Time updates never move
  focus.
- When "Back to current line" scrolls back, focus moves to the list so it does not fall to the
  page.
- Speaker chips are a `list` named "Speakers"; each rename button is named `Rename {name}`, and the
  rename field returns focus to it when it closes. Speaker colour is never the only cue: every turn
  names its speaker.
- Follow and search scroll smoothly, and instantly under `prefers-reduced-motion`.

| Key                                                                                              | Action                                                          |
| ------------------------------------------------------------------------------------------------ | --------------------------------------------------------------- |
| <kbd>Tab</kbd>                                                                                   | Move to the search field, the list, and each line's seek button |
| <kbd>Enter</kbd> / <kbd>Space</kbd>                                                              | Play from the focused line's timestamp                          |
| <kbd>↑</kbd> <kbd>↓</kbd> <kbd>Page Up</kbd> <kbd>Page Down</kbd> <kbd>Home</kbd> <kbd>End</kbd> | Scroll the list; pauses following                               |
| <kbd>Enter</kbd> (in search)                                                                     | Next match                                                      |
| <kbd>Shift</kbd>+<kbd>Enter</kbd> (in search)                                                    | Previous match                                                  |

<StatesTested name="transcript" />

## Do / Don't

<DoDont
  do="Use Transcript for the text of a recording, with currentTime from the player and onSeek wired back to it."
  dont="Build a recording's transcript from MessageScroller and Message — a chat thread pins to its newest message and has no current line."
/>

<DoDont
  do="Use Timeline for a record of events, such as an activity feed or an audit trail."
  dont="Use Transcript for events that are not speech, or announce every line as it plays."
/>
