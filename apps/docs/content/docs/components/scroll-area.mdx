---
title: Scroll Area
description: Augments native scroll functionality for custom, cross-browser styling — a viewport, an auto-hiding scrollbar, and a corner.
audience: public
registry: scroll-area
status: stable
since: 0.1.0
a11y: focusable scrollable region
preview: scrollArea
---

## Install

<InstallSteps name="scroll-area" />

## Usage

```tsx
import { ScrollArea, ScrollBar } from "@/components/ui/scroll-area";

<ScrollArea className="h-[200px] w-[350px] rounded-md border p-4">
  Your scrollable content here.
</ScrollArea>;
```

`ScrollArea` renders the viewport, one vertical `ScrollBar` and the corner itself, so a vertical
scroll area is a single element. Give it a bounded height — a container that can grow never scrolls.

<ComponentPreview name="scrollArea" file="components/preview/scroll-area.tsx" />

## Anatomy

<Anatomy name="scroll-area" />

## Examples

### Composition

`ScrollBar` is the one part you compose, and only for the horizontal axis. Everything you pass as a
child lands inside the viewport, so the horizontal bar is written beside the content rather than
outside the component.

```text
ScrollArea
└── ScrollBar
```

<ComponentPreview
  name="scrollAreaComposition"
  file="components/preview/scroll-area.tsx"
/>

### Horizontal

Add `ScrollBar orientation="horizontal"` for horizontal scrolling, and make the content wider than
the area — `w-max` on an inner flex row, with `whitespace-nowrap` on the area itself.

<ComponentPreview
  name="scrollAreaHorizontal"
  file="components/preview/scroll-area.tsx"
/>

### RTL

Wrap the subtree in `DirectionProvider direction="rtl"`. The vertical bar sits on the inline start —
`border-s` on the scrollbar is logical — and the viewport scrolls from the right.

<ComponentPreview
  name="scrollAreaRtl"
  file="components/preview/scroll-area.tsx"
/>

## Playground

Pick the axis the content overflows on and read off the one composition step that follows: a
vertical area is `ScrollArea` alone, and either horizontal setting adds the `ScrollBar`.

<ScrollAreaPlayground />

## API Reference

`ScrollArea` and `ScrollBar` add no props of their own — each accepts everything the corresponding
[Base UI Scroll Area](https://base-ui.com/react/components/scroll-area) part accepts (`Root`,
`Scrollbar`). `ScrollBar`'s `orientation` is `"vertical"` by default and takes `"horizontal"` for the
other axis; the root also takes Base UI's `Root` props, plus `className`, `ref` and `render`.

## Accessibility

- The viewport is a tab stop exactly when it can scroll (A11Y-6). Base UI sets
  `tabIndex: hidden.x && hidden.y ? -1 : 0`, so a keyboard-only reader can reach overflowing content
  with arrow keys, and an area whose content fits adds no empty stop to the tab order.
- Give a scroll area an accessible name — `aria-label`, or `aria-labelledby` pointing at the heading
  above it — so a screen-reader user hears what the region holds before entering it.
- Focus is the global 2px `:focus-visible` outline from `base.css`. Upstream's ring glow and its
  `outline-none` are removed (FOC-1, FOC-6): the viewport is the one focusable part of this
  component, so suppressing the outline would leave it with no affordance at all.
- The scrollbar and its thumb are pointer affordances; every scroll they perform is reachable from
  the keyboard through the viewport itself.
- Native scrolling is preserved throughout — wheel, trackpad, touch and the platform's own
  scroll-into-view all behave as they do on an ordinary overflow container.

<StatesTested name="scroll-area" />

## Do / Don't

<DoDont
  do="Use a ScrollArea for a bounded region with its own overflow — a list, a log, a code block — and give it an accessible name."
  dont="Wrap the page or a document in one; the browser's own scrollbar is what readers and assistive tech expect at that level."
/>

## Deviations

Upstream's file plus `packages/ui/upstream/patches/scroll-area.patch`. Every hunk:

- **FOC-1, FOC-6** — the viewport's focus glow and outline suppression go: `outline-none`,
  `focus-visible:ring-[3px]`, `focus-visible:ring-ring/50` and `focus-visible:outline-1`. The
  viewport IS a tab stop (see A11Y-6), so `outline-none` would leave the one focusable part of this
  component with no affordance.
- **A11Y-6** — NO HUNK: the engine already implements it. Base UI's `ScrollArea.Viewport` sets
  `tabIndex: hiddenState.x && hiddenState.y ? -1 : 0`, which is exactly "a scroll viewport is a tab
  stop only when it can actually scroll". The pre-reset component computed the same thing itself with
  `useOverflow`; that code is gone because the engine beat it to it. `scroll-area.test.tsx` asserts
  both directions, so the day Base UI makes the viewport unconditionally tabbable this row stops
  being satisfied and the test says so.
- **DOC-2** — `import { cn } from "cn"` becomes `@vegastack/design`; our npm layer owns `cn`.
- **DOC-1** — prettier reflow plus the `registry-stamp.mjs` provenance header the three-copy model
  needs; no decision of its own.
