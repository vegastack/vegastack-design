---
title: Progress Indicator
description: A compact circular pie-fill progress indicator (0–100%) — a server-safe SVG glyph in circle or squircle shapes.
audience: public
preview: progressIndicator
---

import { story } from "@/components/stories/progress-indicator.story";

## Installation

```npm
npx shadcn@latest add @vegastack/progress-indicator
```

<RegistryInstallCallout />

## Usage

```tsx
import { ProgressIndicator } from "@/components/ui/progress-indicator";

<ProgressIndicator value={60} />;
```

<ComponentPreview
  name="progressIndicator"
  file="components/preview/progress-indicator.tsx"
/>

The `ProgressIndicator` is the **radial** counterpart to the linear [`Progress`](/docs/components/progress) bar. Reach for it when a tiny determinate percentage glyph fits better than a full-width bar — inline next to a label, in a dense list row, or beside a metric.

It is **server-safe** — pure SVG driven entirely by the `value` prop (no hooks, no `'use client'`, no DOM measurement). The arc is drawn with `stroke-dasharray`, so it renders identically on the server and the client.

## Playground

Adjust the glyph's value, size, and shape — the pie wedge sweeps to each new value — then copy the generated JSX.

<ProgressIndicatorPlayground />

## Explorer

Every `ProgressIndicator` prop, auto-generated from its TypeScript types — the exhaustive counterpart to
the curated playground above.

<story.WithControl />

## Examples

### Values

`value` is clamped into `[0, max]` (default `max` is `100`) and the fill grows clockwise from 12 o'clock.

<ComponentPreview
  name="progressIndicatorValues"
  file="components/preview/progress-indicator.tsx"
/>

### Shapes

`shape` controls the outline: a true `circle` ring or a rounded-square `squircle`.

<ComponentPreview
  name="progressIndicatorShapes"
  file="components/preview/progress-indicator.tsx"
/>

### Sizes

`xs` (14px), `sm` (16px), `default` (20px), and `lg` (24px) — mapped to the `size-*` token scale.

<ComponentPreview
  name="progressIndicatorSizes"
  file="components/preview/progress-indicator.tsx"
/>

### Variants

Use `variant="inline-value"` when the percentage should be readable beside a compact indicator, or
`variant="contained-value"` for a larger circular indicator with the percentage centered inside and
progress drawn on the border ring only.

<ComponentPreview
  name="progressIndicatorVariants"
  file="components/preview/progress-indicator.tsx"
/>

### Colors

The SVG inherits `currentColor`, so the track and fill follow the root's text color (`text-primary` by default). Recolor it with any semantic `text-*` token via `className` — never a hardcoded hex.

<ComponentPreview
  name="progressIndicatorColors"
  file="components/preview/progress-indicator.tsx"
/>

### Custom scale

Pass `max` to track progress on an arbitrary scale — the announced `aria-valuenow` is normalized to a percentage (`3 / 5` reports as `60`):

<ComponentPreview
  name="progressIndicatorCustomScale"
  file="components/preview/progress-indicator.tsx"
/>

```tsx
<ProgressIndicator value={3} max={5} aria-label="Step 3 of 5" />
```

### Motion

A `value` change sweeps the fill wedge instead of jumping to the new position — the fill's
`stroke-dasharray` transitions on `duration-base`/`ease-standard`, matching the linear
[`Progress`](/docs/components/progress) bar's `transition-[width]`.

<ComponentPreview
  name="progressIndicatorSweep"
  file="components/preview/progress-indicator.tsx"
/>

### Shape × value matrix

Both `shape` outlines across the full fill range — the squircle's pie wedge reads against its rounded-square frame just as the circle's does against its ring.

<ComponentPreview
  name="progressIndicatorShapeValueMatrix"
  file="components/preview/progress-indicator.tsx"
/>

### Segments mode

Pass `segments={n}` to render a dash row instead of the radial glyph — `round(value/max × n)`
bars filled. Use it for step counts (checklists, setup progress), not smooth percentages.

<ComponentPreview
  name="progressIndicatorSegments"
  file="components/preview/progress-indicator.tsx"
/>

## API Reference

<AutoTypeTable
  path="../../packages/ui/registry/ui/progress-indicator.tsx"
  name="ProgressIndicatorProps"
/>

## Accessibility

- Renders `role="progressbar"` with `aria-valuenow` / `aria-valuemin` / `aria-valuemax`. The value is normalized to a `0–100` percentage regardless of `max`.
- A default `aria-label` (`"{percent}% complete"`) is always present — pass a more descriptive `aria-label` (e.g. `"Step 3 of 5"`) when the surrounding context needs it.
- The inner `<svg>` is `aria-hidden` — the accessible state lives on the root element, so screen readers announce one progressbar, not a decorative graphic.
- Color is token-driven (`currentColor`); contrast follows whichever semantic `text-*` token you apply, so it inherits the theme's AA-compliant pairings.

| Attribute                         | Purpose                                                       |
| --------------------------------- | ------------------------------------------------------------- |
| `role="progressbar"`              | Exposes the element as a progress indicator to assistive tech |
| `aria-valuenow`                   | Current value as a `0–100` percentage                         |
| `aria-valuemin` / `aria-valuemax` | Bounds of the announced scale (`0` and `100`)                 |
| `aria-label`                      | Accessible name (defaults to `"{percent}% complete"`)         |

## Do / Don't

<DoDont
  do="Use ProgressIndicator for a compact, determinate percentage glyph — inline beside a label, metric, or list row."
  dont="Use it for an indefinite wait with no measurable value — use a Spinner there instead."
/>
