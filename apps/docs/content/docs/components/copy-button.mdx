---
title: Copy Button
description: Copy a value to the clipboard with transient check feedback — built on Button.
audience: public
registry: copy-button
status: stable
since: 0.1.0
a11y: native button + status announcement
preview: copyButton
---

## Install

<InstallSteps name="copy-button" />

## Usage

```tsx
import { CopyButton } from "@/components/ui/copy-button";

<CopyButton value="npx shadcn add @vegastack/button" />;
```

## Examples

### Copy feedback

A `ghost` / `sm` icon button that writes `value` to the clipboard and swaps the
`Copy` icon for a `Check` for ~1.5s. Pair it with the value it copies — an API
key, an invite link, a code snippet — and use `onCopied` to surface a toast.

<ComponentPreview name="copyButton" file="components/preview/copy-button.tsx" />

### Visible label

Set `showLabel` when the action needs stronger discoverability. The current
`copyLabel` or `copiedLabel` appears beside the icon, and the one `Button` switches from the
square `size="icon-sm"` to the text-bearing `size="sm"`.

<ComponentPreview
  name="copyButtonLabels"
  file="components/preview/copy-button.tsx"
/>

### With a toast

`onCopied` fires after a successful clipboard write — use it to confirm the action
with a toast or to log an analytics event. The transient check feedback is handled
internally, so `onCopied` is purely for your side effects.

<ComponentPreview
  name="copyButtonWithToast"
  file="components/preview/copy-button.tsx"
/>

```tsx
<CopyButton
  value={apiKey}
  onCopied={() => toast.add({ type: "success", title: "API key copied" })}
/>
```

### States

Idle shows the `Copy` icon. After a successful copy the icon swaps to a `Check`
tinted `text-primary` and the accessible label flips from `Copy` to `Copied`
for `timeout` ms (default 1500), with `data-copied` set for that window. `disabled`
is forwarded straight to the underlying button. The copied state is owned internally
and only appears after a real write, so the middle cell below is live — click it.

<ComponentPreview
  name="copyButtonStates"
  file="components/preview/copy-button.tsx"
/>

### Variants

`CopyButton` defaults to `variant="ghost"` and forwards every
[`Button`](/docs/components/button) variant unchanged — `default`, `outline`, `secondary`,
`ghost`, `destructive` and `link`. There is no `tone` axis: it went with the hand-written Button in
Batch 2 of the shadcn reset.

<ComponentPreview
  name="copyButtonVariants"
  file="components/preview/copy-button.tsx"
/>

### Sizes

`size` is forwarded too. Without a visible label the control is icon-only, so it takes an ICON
tier — `icon-xs · icon-sm · icon · icon-lg` — and defaults to `icon-sm`; with `showLabel` it
defaults to the text tier `sm`. An explicit `size` always wins. Upstream's `xs` / `sm` / `lg` are
text tiers with horizontal padding, so they would render a wide box around a lone glyph.

<ComponentPreview
  name="copyButtonSizes"
  file="components/preview/copy-button.tsx"
/>

## Playground

Try the forwarded variant and size axes, the label toggle, and the disabled state — the
copy action is live — then copy the generated JSX.

<CopyButtonPlayground />

## API Reference

`CopyButton` forwards supported [`Button`](/docs/components/button) presentation props
(`variant`, `size`, `className`, `disabled`, …) and owns the clipboard action, button
type, accessible labels, and icon children.

<ApiTable
  path="../../packages/ui/registry/ui/copy-button.tsx"
  name="CopyButtonProps"
/>

## Accessibility

- Renders a native `<button type="button">`; keyboard <kbd>Enter</kbd> / <kbd>Space</kbd> activate it.
- The accessible name switches from `Copy` to `Copied` on success so screen readers announce the result; the icon is decorative (`aria-hidden`).
- Inherits the underlying `Button`'s focus treatment: the global 2px `:focus-visible` outline in the `ring` ink (FOC-1), never `outline: none`. No variant adds a ring of its own — `design-lint`'s `no-focus-ring-glow` rejects one.
- A failed clipboard write (denied permission or insecure context) leaves the button in its default state instead of showing a false success.

| Key                                 | Action                         |
| ----------------------------------- | ------------------------------ |
| <kbd>Tab</kbd>                      | Move focus to the button.      |
| <kbd>Enter</kbd> / <kbd>Space</kbd> | Copy `value` to the clipboard. |

<StatesTested name="copy-button" />

## Do / Don't

<DoDont
  do="Place the CopyButton next to the value it copies and pass onCopied to confirm the action with a toast."
  dont="Use it for long-form content a user should read or edit — copy buttons are for short, opaque values (keys, links, IDs)."
/>
