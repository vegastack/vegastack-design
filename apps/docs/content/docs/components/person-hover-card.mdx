---
title: Person Hover Card
description: Overlapping 24px avatars with a compact hover card per person (a 32px avatar, the name and a muted email) and a "+N" count; the stack lists everyone.
audience: public
registry: person-hover-card
status: stable
since: 0.23.19
a11y: named buttons, hover card on hover and focus, tap on touch
preview: personHoverCard
---

## Install

<InstallSteps name="person-hover-card" />

## Usage

```tsx
import { AvatarStack } from "@/components/ui/person-hover-card";

<AvatarStack people={participants} label="Participants" />;
```

`AvatarStack` stacks up to `max` (5) 24px avatars — overlapping, each ringed in the background
colour — with the rest counted in a "+N". Hovering an avatar previews that person: a compact 240px
card with a 32px avatar and, beside it, the name over a smaller muted email (always shown when
known). The stack is one button: click, tap or <kbd>Enter</kbd> lists everyone in the same format.
Someone without an account (a free-text participant such as "Northwind FM leads") has no email:
their avatar shows initials and the card just the name.

<ComponentPreview
  name="personHoverCard"
  file="components/preview/person-hover-card.tsx"
/>

## Anatomy

<Anatomy name="person-hover-card" />

## Examples

### One person

`PersonHoverCard` wraps any trigger — usually a `PersonAvatar` — in a round ghost button named by
the person.

<ComponentPreview
  name="personHoverCardSingle"
  file="components/preview/person-hover-card.tsx"
/>

### The card

`PersonCard` is the card's body (`layout="card"`) and a compact list row (`layout="row"`).

<ComponentPreview
  name="personHoverCardCard"
  file="components/preview/person-hover-card.tsx"
/>

### Inactive member

`badge` on the person puts a status right after the name — a string such as "Inactive" is a small
muted outline badge. It shows in `PersonCard`, the hover card and the `AvatarStack` list. For a
table's person cell, compose `PersonAvatar` with the name and `PersonBadge`.

<ComponentPreview
  name="personHoverCardInactive"
  file="components/preview/person-hover-card.tsx"
/>

## API Reference

### AvatarStack

<ApiTable
  path="../../packages/ui/registry/ui/person-hover-card.tsx"
  name="AvatarStackProps"
/>

### PersonHoverCard

<ApiTable
  path="../../packages/ui/registry/ui/person-hover-card.tsx"
  name="PersonHoverCardProps"
/>

### PersonCard

<ApiTable
  path="../../packages/ui/registry/ui/person-hover-card.tsx"
  name="PersonCardProps"
/>

### PersonAvatar

<ApiTable
  path="../../packages/ui/registry/ui/person-hover-card.tsx"
  name="PersonAvatarProps"
/>

## Accessibility

- The stack is one button named "{label}: {names}"; it opens the full list, so keyboard and touch
  users reach every person without the hover previews.
- `PersonHoverCard` on its own is a round button named by the person; its card opens on hover and
  on keyboard focus, a tap toggles it, and <kbd>Esc</kbd> closes it.

| Key                                 | Action                                  |
| ----------------------------------- | --------------------------------------- |
| <kbd>Tab</kbd>                      | Move to the stack                       |
| <kbd>Enter</kbd> / <kbd>Space</kbd> | List everyone (or open a person's card) |
| <kbd>Esc</kbd>                      | Close the list or the card              |

<StatesTested name="person-hover-card" />

## Do / Don't

<DoDont
  do="Pass an email only for people with an account; a free-text participant is just a name."
  dont="Put actions in the hover card — it is a preview; link to the person's page instead."
/>
