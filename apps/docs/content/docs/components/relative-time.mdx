---
title: Dates & times
description: One Intl-based module for every date, time, duration, due label and day grouping — plus RelativeTime, DateTime, Duration and DueLabel. No date library.
audience: public
registry: relative-time
status: stable
since: 0.1.0
a11y: native time element
preview: relativeTime
---

## Install

<InstallSteps name="relative-time" />

## Usage

```tsx
import {
  DateTime,
  DueLabel,
  Duration,
  RelativeTime,
} from "@/components/ui/relative-time";
import { formatDate, formatRelative, groupByDay } from "@/lib/date-time";

<RelativeTime date={row.updatedAt} format="minimal" />; // "2m"
<DateTime date={task.createdAt} />; // "Sep 25"
<DueLabel date={task.dueAt} />; // "Overdue 2d"
formatRelative(row.updatedAt, { timeZone }); // plain string, server or client
```

The formatting functions live in `@/lib/date-time` and are plain — call them in server components,
route handlers, emails and client code alike. The components wrap them in a semantic `<time>` and
add the absolute time in a hover tooltip ("Sep 25, 2026 · 2:30 PM IST").

### Shared rules

- The year shows only when it is not the current year.
- 12-hour clock with no leading zero: "9:04 AM".
- Minimal units `s` `m` `h` `d` `w` `mo` `y`, no space before the unit.
- Calendar days use the viewer's time zone (`timeZone` option on every function).
- Anything under 45 seconds reads "now".
- Built on `Intl`, locale-ready; English for now.
- Set in the body font with proportional figures, like the text around them. Tabular figures are
  for live timers (the media player's clock), not for dates.

### Where to use what

| Surface                                  | Use                                                         | Reads                                               |
| ---------------------------------------- | ----------------------------------------------------------- | --------------------------------------------------- |
| Table columns (updated, last activity)   | `formatRelative` / `<RelativeTime format="minimal">`        | now · 2m · 3h · 5d                                  |
| Table columns (created, due, start date) | `formatDate` / `<DateTime>`                                 | Today · Mon · Sep 25                                |
| Detail pages, audit rows                 | `formatDateTime` / `<DateTime variant="datetime">`          | Sep 25 · 2:30 PM                                    |
| Hover tooltips                           | `formatTooltip` (built into every component)                | Sep 25, 2026 · 2:30 PM IST                          |
| Notifications, feeds                     | `formatRelative` + `groupByDay` headers                     | 2m under "Today"                                    |
| Sentences                                | `formatRelative({ style: "suffix" })`, `separator: "comma"` | 2m ago · Sep 25, 2:30 PM                            |
| Due chips                                | `formatDueLabel` / `<DueLabel>`                             | Overdue 2d · Due today · Due Sep 30                 |
| Durations (meetings, audio, SLAs)        | `formatDuration` / `<Duration>`; `clock` for players        | 1h 15m · 1:15:04                                    |
| Schedules, events                        | `formatDateRange`, `formatTimeOfDay`                        | Sep 25 · 2–3:30 PM                                  |
| Grouped lists (inbox, activity)          | `groupByDay`                                                | Today / Yesterday / This week / Last week / Earlier |
| API / JSON                               | ISO strings — never formatted text                          | 2026-09-25T09:00:00.000Z                            |

## Anatomy

`Dates & times` exports several parts. Every exported part, with the `data-slot` it renders
(generated from the canonical source):

<Anatomy name="relative-time" />

## Examples

### formatRelative

`now · 2m · 3h · 5d · 1w · 2mo · 1y · in 2d`. `style: "suffix"` gives "2m ago"; `style: "long"`
gives "2 minutes ago".

<ComponentPreview
  name="dateTimeRelative"
  file="components/preview/relative-time.tsx"
/>

### formatDuration

Input in seconds (or `unit: "milliseconds"`). At most two units; seconds drop once minutes exist.
`clock: true` reads "1:15:04".

<ComponentPreview
  name="dateTimeDuration"
  file="components/preview/relative-time.tsx"
/>

### formatDate and formatTimeOfDay

Today · Tomorrow · Yesterday · a weekday within ±6 days ("Mon", "Last Fri") · "Sep 25" ·
"Dec 20, 2026". `looseFuture: true` turns far-future dates into "in 2 weeks"; `absolute: true`
always prints the date.

<ComponentPreview
  name="dateTimeDates"
  file="components/preview/relative-time.tsx"
/>

### formatDateTime and formatDateRange

`formatDateTime` joins the day and time with a middle dot ("Today · 2:30 PM"); use
`separator: "comma"` inside sentences. `formatDateRange` collapses what the ends share.

<ComponentPreview
  name="dateTimeDateTimes"
  file="components/preview/relative-time.tsx"
/>

### formatDueLabel

Returns `{ label, tone }` — tone is `overdue`, `soon` (today/tomorrow) or `normal`.

<ComponentPreview
  name="dateTimeDue"
  file="components/preview/relative-time.tsx"
/>

### groupByDay

Buckets items into Today / Yesterday / This week / Last week / Earlier (weeks start Monday; empty
groups are dropped). Each group has a stable `key` and a `label`.

<ComponentPreview
  name="dateTimeGroups"
  file="components/preview/relative-time.tsx"
/>

### Components

`<RelativeTime>` auto-refreshes; `<DateTime>`, `<Duration>` and `<DueLabel>` render once. Each
shows the absolute time in a hover tooltip unless `title={false}`.

<ComponentPreview
  name="dateTimeComponents"
  file="components/preview/relative-time.tsx"
/>

### Time zone

Day boundaries follow the viewer. Write the browser zone to a `tz` cookie, read it on the server
with the org zone from app config as the fallback, and provide it to the tree:

```tsx
// app/layout.tsx (server)
import { cookies } from "next/headers";
import {
  TimeZoneProvider,
  TimeZoneScript,
} from "@/components/ui/relative-time";
import { TIME_ZONE_COOKIE, getTimeZone } from "@/lib/date-time";
import { config } from "@/config";

const timeZone = getTimeZone(
  (await cookies()).get(TIME_ZONE_COOKIE)?.value,
  config.orgTimeZone,
);

<body>
  <TimeZoneScript />
  <TimeZoneProvider timeZone={timeZone}>{children}</TimeZoneProvider>
</body>;
```

`TimeZoneScript` writes the cookie on load and again when the tab becomes visible, so a changed
zone reaches the next request. Client code reads the zone with `useTimeZone()`; server code passes
`timeZone` to each function.

`RelativeTime` renders a semantic `<time dateTime>` element and self-updates on a
timer while the date is recent, with the exact date and time in the hover. It uses the
platform-native `Intl` APIs — no `date-fns` or other date dependency.

### RelativeTime formats

Past and future instants, in the house compact form: `now` under a minute, then `19m ago`,
`3h ago`, `2d ago`, `3w ago`, `5mo ago`, `1y ago`; future dates read `in 2d`. No unit carries a
period — the old Intl short style (`19 min. ago`) is gone.

#### Format variants

`format` picks the form of the `ago` label: `suffix` (the default, `19m ago`), `minimal` for dense
tables and chips (`19m`), and `long` for sentences (`19 minutes ago`). The deprecated `unitStyle`
maps `long` to `format="long"` and everything else to the default.

<ComponentPreview
  name="relativeTimeFormatVariants"
  file="components/preview/relative-time.tsx"
/>

<ComponentPreview
  name="relativeTimeExamples"
  file="components/preview/relative-time.tsx"
/>

### Modes

`mode="ago"` (default) is duration-relative — `"2h ago"`, `"in 3d"`.
`mode="day"` is calendar-relative — `"today"`, `"yesterday"`, `"tomorrow"`, and an
absolute date (`"March 15"`, `"March 15, 2025"`) once a date is further out.

<ComponentPreview
  name="relativeTimeModes"
  file="components/preview/relative-time.tsx"
/>

### States

By default the absolute date-time is revealed in a Tooltip on hover or focus. Pass a
string to `title` for a custom label, or `title={false}` to drop the tooltip.

<ComponentPreview
  name="relativeTimeStates"
  file="components/preview/relative-time.tsx"
/>

### Tooltip delay

`tooltipDelay` (ms) sets how long the pointer must hover before the absolute-date
tooltip opens. It defaults to the app-wide tooltip delay (`TIMINGS.tooltipOpenDelayMs`, 250ms),
so it matches every other tooltip. Ignored when `title={false}`.

<ComponentPreview
  name="relativeTimeTooltipDelay"
  file="components/preview/relative-time.tsx"
/>

### Localization

`locale` localizes what `Intl` words: the `mode="day"` labels and dates and `format="long"`. The
compact `ago` form (`2h ago`) is the house form everywhere, so a dense column reads the same in
every locale. Omit `locale` to follow the runtime locale.

<ComponentPreview
  name="relativeTimeLocale"
  file="components/preview/relative-time.tsx"
/>

### Standalone label

Inside a sentence the relative words read lower-case ("due today"). A label that stands on its own
takes `capitalize` ("Today"). With `title={false}` it has no tooltip, so it is plain inline text at
the line's height rather than a 24px pointer target.

<ComponentPreview
  name="relativeTimeStandalone"
  file="components/preview/relative-time.tsx"
/>

### Fixed time zone

`timeZone` (an IANA name) decides which calendar day an instant falls on, and formats the server's
first render and the tooltip in the same zone — so a server in UTC and a reader in IST agree on what
"today" is. Omit it to follow the runtime's zone.

<ComponentPreview
  name="relativeTimeTimeZone"
  file="components/preview/relative-time.tsx"
/>

### Day with time

In `mode="day"`, `withTime` appends the time of day ("Today, 10:00 AM"). `formatOptions` shapes
the absolute date shown beyond yesterday and tomorrow — `{ day: "numeric", month: "short" }` reads
"6 Jan" in `en-IN` — and the year is added only when the date is in another year.

<ComponentPreview
  name="relativeTimeWithTime"
  file="components/preview/relative-time.tsx"
/>

### Live updates

While the date is recent and `now` is left unset, the component reads the live clock
and refreshes on an adaptive timer (every 10s under an hour old, every minute under a
day, then static). The example below is intentionally **live** — it seeds a timestamp
30s in the past and climbs as you watch. Pass `refresh={false}` to opt out.

<ComponentPreview
  name="relativeTimeLive"
  file="components/preview/relative-time.tsx"
/>

### Determinism

The displayed value depends on the current time. For tests, SSR snapshots, or
Storybook, pass a fixed `now` (epoch ms) to render deterministically — this also
disables the refresh timer.

```tsx
<RelativeTime date={ts} now={Date.UTC(2026, 0, 15, 12, 0, 0)} refresh={false} />
```

## Playground

Switch the formatting mode and the instant being rendered, then copy the generated JSX.

<RelativeTimePlayground />

## API Reference

<ApiTable
  path="../../packages/ui/registry/ui/relative-time.tsx"
  name="RelativeTimeProps"
/>

### Server rendering

An uncontrolled instance renders the **absolute** date (`"Mar 15, 2025"`) on the server and
on the hydration render, then swaps to the relative label once the client clock exists.
There is no empty frame and no layout jump.

The reason it cannot simply render the relative label is that `"2h ago"` needs
`Date.now()`, which the server cannot reproduce — so the two renders would disagree and
React would warn. Deriving the first paint from the target instant alone keeps both sides
byte-identical. Pass `now` to make the output fully deterministic and skip the swap.

## Accessibility

- Renders a semantic `<time dateTime="…">` so the machine-readable ISO timestamp is
  always present for assistive tech, even as the visible text updates.
- When the absolute-date Tooltip is enabled, the `<time>` is made focusable
  (`tabindex="0"`) so keyboard users can reveal it; `:focus-visible` shows a 2px ring
  (`outline-ring`) — never `outline: none` alone.
- Color is inherited from context — the component never encodes meaning in color, so
  the relative string stands on its own.

| Key            | Action                                                     |
| -------------- | ---------------------------------------------------------- |
| <kbd>Tab</kbd> | Move focus to the timestamp (when the tooltip is enabled). |
| <kbd>Esc</kbd> | Dismiss the open absolute-date tooltip.                    |

`focusable` controls whether the timestamp takes a tab stop to open its absolute-date
Tooltip. It defaults to `true` standalone, and to whatever an enclosing
`TruncationFocusProvider` sets — `false` for a grid or list host, where 50 rows would
otherwise mean 50 extra tab stops on top of that host's own roving focus. The
machine-readable `dateTime` attribute is there either way.

<StatesTested name="relative-time" />

## Do / Don't

<DoDont
  do="Pick the function from the where-to-use table — formatRelative for activity, formatDate for dates, formatDateTime for detail rows, formatDueLabel for due chips, formatDuration for lengths."
  dont="Call toLocaleString, hand-roll 'minutes ago', or add a date library — and never put formatted text in API/JSON output."
/>
