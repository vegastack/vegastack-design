// The generated authority includes every routable MDX page plus the root page. The generator is
// part of design:derived so a new/removed docs route cannot silently disappear from selected VRT.
export { VRT_PAGE_ROUTES } from "./page-routes.generated";
