# EXECUTION — audit 2026-09-07, fix batches

Live board for epic #31. Updated after every state change by the orchestrator (branch
`audit/execution-board`). Waves and protocol: `HANDOFF-PROMPT.md` §2. Decisions: `00-decisions.md`
(final). Counts to drive to zero: `99-change-list.md` §Counts.

Started 2026-09-07 from `main` @ `6f11a4bc`. `@vegastack/ui` 0.6.0 (workspace) · `@vegastack/design`
0.3.2 (npm). No pending changesets on `main` at start.

## Progress snapshot (refreshed at every state change)

_Last refresh: 2026-09-09 01:50 IST (session 3)._ **Completion (effort-weighted): ≈ 60% done · 40% pending** (7 merged · 13 code-complete unmerged · 6 in progress · 4 not started).

| batch              | issue           | PR                    |      own commits | stage                                                                   |
| ------------------ | --------------- | --------------------- | ---------------: | ----------------------------------------------------------------------- |
| F1 (+ follow-up)   | #32             | #55, #59              |                — | **merged**                                                              |
| Do1-a              | #48a            | #54                   |                — | **merged**                                                              |
| I1                 | #46             | #57                   |                — | **merged** `f1d7d2fb`                                                   |
| F2                 | #33             | #60                   |                — | **merged** `8ce8de4d`                                                   |
| Di1                | #42             | #62                   |                — | **merged** `42aa455b`                                                   |
| T2                 | #38             | #63                   |                — | **merged** `f8ca47ca`                                                   |
| O1                 | #40             | #66                   |               15 | finisher running (18 dirty files mid-regeneration recovered)            |
| N1 · C1 · Mk1 · D1 | #43 #44 #47 #34 | —                     |   6 · 4 · 2 · 15 | finishers running → rebase, verify, PR                                  |
| M1 · M2 · T1 · Fo1 | #35 #36 #37 #39 | #65 · #64 · #61 · #67 | 8 · 15 · 12 · 16 | PRs open, all CONFLICTING after WP0/WP1 → rebase once WP2 lands         |
| G1-a               | #49a            | #53                   |               15 | rebuild plan says close unmerged, carry TG-07/TG-08 into WP3 — Needs MK |
| D2 · O2 · P1       | #50 #41 #45     | —                     |        — · 4 · 3 | code-complete, stacked; D2 after D1, O2/P1 after O1                     |
| Do1-b · G1-b · D3  | #48b #49b #51   | —                     |                — | not started (dependency-bound; G1-b re-scoped by the rebuild)           |
| Version PR         | —               | #56                   |                — | MK publish gate — never merged by an agent                              |

### Parallel effort: verification rebuild (`docs/plans/2026-09-08-verification-rebuild.md`)

| WP  | what                                                 | state                                                       |
| --- | ---------------------------------------------------- | ----------------------------------------------------------- |
| WP0 | Linux runners online (`gates`, `gates2`)             | **merged** #68 `0935d502`                                   |
| WP1 | geometry contracts under vitest                      | **merged** #69 `aded7838`                                   |
| WP2 | `pnpm verify` + CI executes the browser lane         | finisher running — adopted from the dead rebuild session    |
| WP3 | delete the attestation stack (incl. `apps/docs/vrt`) | after the audit batches land                                |
| WP4 | ungit the generated docs files                       | one unpushed commit `5f082211`, parked until the batch wave |
| WP5 | changelog per version                                | not started                                                 |
| WP6 | rulebook and skills                                  | not started                                                 |

## Process revision (MK, 2026-09-07 17:10) — development first, one verification round at the end

- Implementers still run `pnpm gates:push` + the issue's acceptance commands before their PR.
- A batch PR merges as soon as CI is green with `receipt-guard` and `verify` actually executed. **No per-batch reviewer wait.**
- Dependent waves start on the dependency's merge SHA immediately.
- One consolidated end round: area-partitioned reviewers re-execute every issue's acceptance criteria against `main`
  (tokens/foundations · forms/overlays · data/media · navigation/composition · docs · tooling/deps), one fix pass, then
  `pnpm gates:ship`, state probe (light + dark), counts scan, final report. The G1-a reviewer already running is kept;
  its findings go into the fix round.
- Unchanged: publish, deploy and the D3 Changesets/pnpm sub-PR merge stay MK's decisions.

## Board

| issue   | batch                                                | wave | agent                                             | branch                             | PR  | reviewer verdict | merge SHA | counts moved         |
| ------- | ---------------------------------------------------- | ---- | ------------------------------------------------- | ---------------------------------- | --- | ---------------- | --------- | -------------------- |
| #32     | F1 surface tokens                                    | 1    | f1-impl                                           | `audit/f1-surface-tokens`          | —   | —                | —         | —                    |
| #34     | D1 deps security/alignment                           | 1    | d1-impl → d1-impl-2 (continuation, same worktree) | `audit/d1-deps-security-alignment` | —   | —                | —         | —                    |
| #49 (a) | G1-a gate fixes (no lint rules)                      | 1    | g1a-impl → g1a-impl-2 (Opus)                      | `audit/g1a-gate-fixes`             | #53 | reviewing        | —         | TG-01…09, TD-6, TD-7 |
| #48 (a) | Do1-a docs chrome + agent export (no page migration) | 1    | do1a-impl                                         | `audit/do1a-docs-chrome-export`    | —   | —                | —         | —                    |
| #33     | F2 Button variant × tone                             | 2    | —                                                 | `audit/f2-button-variant-tone`     | —   | —                | —         | —                    |
| #35     | M1 media players                                     | 3    | —                                                 | `audit/m1-media-players`           | —   | —                | —         | —                    |
| #37     | T1 tables and grids                                  | 3    | —                                                 | `audit/t1-tables-grids`            | —   | —                | —         | —                    |
| #39     | Fo1 forms                                            | 3    | —                                                 | `audit/fo1-forms`                  | —   | —                | —         | —                    |
| #40     | O1 overlays                                          | 3    | —                                                 | `audit/o1-overlays`                | —   | —                | —         | —                    |
| #42     | Di1 display leaves                                   | 3    | —                                                 | `audit/di1-display-leaves`         | —   | —                | —         | —                    |
| #46     | I1 icons factory                                     | 3    | —                                                 | `audit/i1-icons-factory`           | —   | —                | —         | —                    |
| #41     | O2 toasts                                            | 4    | —                                                 | `audit/o2-toasts`                  | —   | —                | —         | —                    |
| #38     | T2 chips/announcer/pagination                        | 4    | —                                                 | `audit/t2-chips-announcer`         | —   | —                | —         | —                    |
| #36     | M2 rich text/toolbars                                | 4    | —                                                 | `audit/m2-rich-text-toolbars`      | —   | —                | —         | —                    |
| #43     | N1 navigation/layout                                 | 4    | —                                                 | `audit/n1-navigation-layout`       | —   | —                | —         | —                    |
| #44     | C1 composition/feedback                              | 4    | —                                                 | `audit/c1-composition-feedback`    | —   | —                | —         | —                    |
| #45     | P1 pickers/drag                                      | 4    | —                                                 | `audit/p1-pickers-drag`            | —   | —                | —         | —                    |
| #47     | Mk1 marketing/hooks/block                            | 4    | —                                                 | `audit/mk1-marketing-hooks-block`  | —   | —                | —         | —                    |
| #50     | D2 deps Fumadocs/lucide/axe/Playwright/recharts      | 5    | —                                                 | `audit/d2-deps-fumadocs-lucide`    | —   | —                | —         | —                    |
| #48 (b) | Do1-b page-canon migration                           | 5    | —                                                 | `audit/do1b-page-canon`            | —   | —                | —         | —                    |
| #49 (b) | G1-b lint rules + contract-lane geometry             | 6    | —                                                 | `audit/g1b-lint-rules`             | —   | —                | —         | —                    |
| #51     | D3 majors (4 sub-PRs; last stops before merge)       | 7    | —                                                 | `audit/d3-*`                       | —   | —                | —         | —                    |

## Wave 1 file ownership (no sibling touches another's files)

- **F1** — `packages/design-tokens/**`, `packages/design/src/**` (the `surfaceInteractive`/`fillInteractive`
  recipes + forced-colours fallback), `packages/ui/registry/ui/*.tsx` hover-site migration + sidebar tokens
  (+ their tests), `design.md` §Surfaces/§Hover geometry, `apps/docs/content/docs/foundations/colors.mdx`
  (+ the `ColorPalette` specimen it uses), `skills/internal/component/references/tokens.md`,
  `skills/public/**` token vocabulary, `/CHANGELOG.md`, `.changeset/*`.
- **D1** — every `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `apps/docs/next.config.*`,
  `AGENTS.md` §Sanctioned dependency exceptions only, `date-picker.tsx:30` comment only, `design.md`
  §Sanctioned engines line, `docs/ledger/operator-review.md` (append), `/CHANGELOG.md` `📦`.
- **G1-a** — `tooling/**` (gate fixes TG-01…TG-09, `tooling/lib/fs.mjs`, receipt v2, `verify-turbo-inputs.mjs`),
  `.github/workflows/**`, `turbo.json`, `.npmrc`, `.husky/**`, `AGENTS.md` §Verification ladder /
  §Locked decisions (receipt) prose, `skills/internal/gates/**`, `skills/internal/review/**` (gate prose only —
  not `lint-rules.md` rule additions). **Not** `tooling/content-lint.mjs` beyond the fail-closed root fix,
  **not** `tooling/design-lint.mjs` beyond TG-08 motion anchoring, **not** `tooling/verify-docs-export.mjs`.
- **Do1-a** — `apps/docs/**` except `content/docs/components/*.mdx` bodies and `foundations/colors.mdx`;
  `apps/docs/source.config.ts` (frontmatter schema), `lib/source.ts` (agent export), `components/mdx.tsx`,
  `components/*` chrome, `app/**` layouts/CSS; new `tooling/verify-docs-export.mjs` and
  `tooling/verify-docs-base-mirror.mjs`; `packages/ui/component-contracts.json` (`dataAttributes` field
  additions only) + `pnpm design:derived` outputs; `design.md` §Docs canon; `docs/ledger/*` append.

## Counts (from `99-change-list.md`, before → now)

Rerun after every wave: `04-cross-cutting.md` §2 scan + `probe-states.mjs --all` (light and `--dark`).

| count                                     |  before | after w1 | after w2 | after w3 | after w4 | after w5 | after w6 |                      target |
| ----------------------------------------- | ------: | -------: | -------: | -------: | -------: | -------: | -------: | --------------------------: |
| `size="default"` call sites               |      10 |          |          |          |          |          |          |                           0 |
| cva `default` size keys                   |      31 |          |          |          |          |          |          |                           0 |
| `motion-reduce:`                          |      17 |          |          |          |          |          |          |                           0 |
| raw numeric sizes                         |     182 |          |          |          |          |          |          |                   allowlist |
| fixed widths                              |       8 |          |          |          |          |          |          |                           0 |
| physical direction utilities              |      52 |          |          |          |          |          |          |     allowlist (sheet sides) |
| `text-xs` non-mono                        |       7 |          |          |          |          |          |          |                           0 |
| restated focus                            |       7 |          |          |          |          |          |          |                           0 |
| `ring-N`                                  |      10 |          |          |          |          |          |          |                           0 |
| `hover:bg-*` literals                     |      33 |          |          |          |          |          |          |                           0 |
| non-overlay `shadow-*`                    |       7 |          |          |          |          |          |          |                           0 |
| hand-rolled icon buttons                  |      18 |          |          |          |          |          |          |                           0 |
| `rounded-full` overrides                  |      48 |          |          |          |          |          |          |  avatars/dots/progress only |
| `role="status"` hand-rolled               |      24 |          |          |          |          |          |          | ≤1 per announcing component |
| seq announcers                            |       3 |          |          |          |          |          |          |                           0 |
| matchMedia copies                         |       6 |          |          |          |          |          |          |                           0 |
| inline ref merges                         |      10 |          |          |          |          |          |          |                           0 |
| needless `"use client"`                   |      12 |          |          |          |          |          |          |                           0 |
| `100dvh` magic                            |       7 |          |          |          |          |          |          |                           0 |
| `(no own props)` rows                     |     138 |          |          |          |          |          |          |                           0 |
| JSX tags in agent markdown                |     260 |          |          |          |          |          |          |                           0 |
| probe `active-same-as-hover` (light/dark) | 268/270 |          |          |          |          |          |          |                           0 |
| probe `hover-invisible` (light/dark)      | 149/135 |          |          |          |          |          |          |                      0 real |
| probe `focus-clipped` (light/dark)        |   19/19 |          |          |          |          |          |          |                           0 |
| probe `hover-touches-border` (light/dark) |   13/14 |          |          |          |          |          |          |                           0 |

## Needs MK

1. **F1 · bordered elements hover by fill, not by border.** P1's rule says cards/inputs/outline buttons hover by changing the border; issue #32's Do-list §3 mapped those sites to `surfaceInteractive` (fill wash), and a 1px hairline change is below the state probe's visibility threshold, so F1 shipped the fill. Options: (a) keep the fill (consistent with the issue text and the probe acceptance; recommended), (b) border-only hover on bordered controls — a one-line change in F2's Button recipe. Recorded in `operator-review.md`. F2 proceeds with (a) unless told otherwise.
2. **F1 · status inks re-tuned.** `destructive/success/warning/info-text` moved 0.015–0.04 L (darker light / lighter dark) because adding a pressed rung to soft fills put the old inks at or below AA on their own pressed surface (light `info-text` 4.50:1 on hover). System-wide but AA-gated; no decision required unless MK dislikes the shift.

3. **I1 · two acceptance thresholds missed by a margin that is data, not duplication.** Measured 12,823 lines (target <12,000) and −1.57 MB on `public/r` (target ≥2 MB); the residual is upstream path geometry + Motion variant data. Recommendation: accept the measured numbers (the targets were estimates).
4. **I1 · public type change** `AnimatedIconComponent` host `HTMLDivElement → HTMLSpanElement` (inline-flex host, per the issue) and eight `@deprecated` handle aliases removed — both consistent with the no-shims mandate; `@vegastack/design` takes a minor. Informational.
5. **I1 · upstream lucide-animated is at 467 icons; the pinned count is 439**, so `mirror --refresh` is blocked until the 28 new icons are adopted or explicitly excluded. Recommendation: separate issue after the epic (registry, contracts, counts all move).
6. **I1 · icon-gallery route split deferred** — a dynamic import was already measured not to help (one client-reference manifest per catch-all route); a real route segment touches the route-scope authority (G1's lane). Recommendation: fold into G1-b's brief (orchestrator will).
7. **I1 · `motion` is now an optional peer of `@vegastack/design`** (only needed for the `create-animated-icon` subpath; root entry stays RSC-safe). Informational unless MK objects to Motion appearing in the public package's peer list.

8. **T1 · combined LOC reduction is 249, target ≥300.** 457 lines removed, 208 added back as documented shared API (`data-table-parts`, `table-scroll-region`, `use-overflow`); no further duplication found. Recommendation: accept the measured number (the target was an estimate).

9. **I1 · `MotionConfig reducedMotion` override is one-way.** The factory reduces motion when the OS preference is on OR `MotionConfig reducedMotion="always"`; `"never"` cannot un-reduce, because Motion's default context is `"never"` and indistinguishable from no provider. Recommendation: accept (a page can never accidentally re-enable motion for a user who asked for less).

10. **`packages/ui/test/` is not type-checked** (F2 #60 finding; defect 5 there was a rotted contrast fixture). Adding `"test"` to the package `include` surfaces ~12 pre-existing errors in sibling files. Recommendation: route to **G1-b** as a fail-closed fix (add the include, fix the errors) — no MK decision needed unless you object.
11. **AGENTS.md "no component writes its own `hover:bg-*`"** (F1 wording) is now narrower than the system: Button paints its own family through tone vars, exactly as `surfaceInteractive` does for the ladder. `design.md` §Components already states the mechanism. Recommendation: amend the AGENTS.md sentence to "…except a component that IS the recipe for its family (Button tones, the surface recipes)". Owner if approved: G1-b (with the lint rule).
12. **D7 `aria-disabled` on Button/IconButton/SplitButton** keeps disabled controls in the tab order; consumers asserting `element.disabled` need a one-line change (called out in the changelog). FYI, no decision needed.
13. **`:active` still paints on a disabled Button** (pointer events restored by D7). Options: (a) accept, consistent with "hover-on-disabled allowed" (batch 1); (b) `not-aria-disabled:` guard on all six recipes. Recommendation: (a).

- **2026-09-08 17:00 IST** — I1 #57 merged `f1d7d2fb`. Every sibling PR then turned CONFLICTING on generated files/ledgers/§Numbers. Response: a single merge-train conductor agent (`briefs/conductor.md`, `scratchpad/merge-chain.md`) rebases, re-sweeps once, stacks the next PR on the previous head so its receipt survives the squash, and merges on green CI with `receipt-guard` executed.
- **2026-09-08 15:50–16:40 IST** — Opus session limit (429) killed all 12 running agents mid-sweep; the remote sweeps kept running on the boxes. Results on resume: Di1, T2, I1, T1(rebased), Mk1 sweeps PASSED at their current heads (receipts pulled by the resumed agents); M1, O1, M2 unit-lane failures; Fo1, N1, D1 unit+contracts failures (stale F2-era assertions the common cause); C1 still sweeping. All 12 relaunched 16:45 with `briefs/resume-after-429.md` (reuse a passing box result; never a second concurrent sweep). P1 code-complete, waits for O1; O2 likewise.

14. **Di1 widened the reduced-motion reset** (`animation-delay: 0s` / `transition-delay: 0s` in `@vegastack/design-tokens`, minor changeset) instead of keeping `staggered-text-reveal`'s per-component carve-out. Recommendation: accept — one global rule beats a per-component exception, and it is what `motion-reduce:` deletion (D-rule) implies.
15. **`CommandShortcut` not converted to `Kbd`** (B2-07 defers it; `command.tsx` records palette hints as plain muted text). Recommendation: keep as is; revisit only if MK wants Kbd in the palette.

16. **T2 geometry calls** (operator-review 2026-09-08): the 28px `sm` chip leaves a 2px inset around its 24px remove control where §Hover geometry asks ≥4px; `FilterChip`/`ComboboxChip` go `rounded-md` → `rounded-full`; `Tag` grows 20→28px. Recommendation: accept all three (the 24px floor outranks the inset rule at `sm`; one chip silhouette; Tag joins the ladder).
17. **`verify-component-contracts.mjs` hard-codes the registry inventory count**, so every component batch edits a gate script. Recommendation: G1-b derives the expected count from `registry.json` and keeps only the reconciliation checks.

18. **Sweep economics** (measured 2026-09-08): every batch is classified global, so each sweep is the full 110-route contract lane (~10 min idle, up to 35 min with two lanes per box); a rebase voids the receipt; ~38 sweeps for 14 batches. Two proposals for G1-b need a yes/no: (a) regenerated-only diffs classified by authority inputs → scoped lane; (b) a receipt carry for rebases whose non-generated diff is empty. Neither is needed to finish this epic (the merge train + one lane per box already cut it to one sweep per PR).

19. **M1 API calls** (PR #65 body): a fourth `Slider` variant `bare` the issue did not name; the volume panel deliberately not portaled; two `⚠️ Breaking` changelog entries. Recommendation: accept `bare` (it is the media-chrome recipe, not a new visual), accept non-portaled volume (it must stay inside the player's theme scope).

20. **G1-a (#53) closes unmerged.** The verification rebuild's §4 says #53 conflicts with WP2–WP3 wholesale and that only its TG-07 (`tooling/lib/fs.mjs`) and TG-08 (motion pairing) should be carried into WP3. #53 is green, CONFLICTING, and represents 15 commits of finished work. Recommendation: **accept** — close it unmerged with a comment naming the two carried items, and put them in WP3's scope. The rest of it hardens a gate stack WP2/WP3 delete. Needs an explicit MK yes before the close.

21. ~~**WP2 adopted mid-flight by the audit orchestrator.**~~ **WITHDRAWN 02:25 IST — the premise was wrong and no decision is needed.** The rebuild session was never dead: it opened WP4 PR #70 at 02:06 and WP2 PR #71 at 02:07, and its WP2 commits land at 01:59–02:00, about four minutes _before_ the finisher this session launched. What this session read at 01:50 as an abandoned worktree was that session's live working tree between commits. The duplicate finisher has been stopped; it committed nothing and left the tree clean at `418a9d55`, identical to #71's head. WP2 and WP4 are the rebuild session's, and it has been messaged to that effect. Cost: one wasted agent and ~20 minutes. Lesson recorded below.

22. **Focus-indicator coverage will disappear silently unless someone decides otherwise.** The contract lane's focus check has been unable to fail since 2026-07-25 — it runs under `forcedColors: "active"`, where Chromium paints its own ≥2px ring, so deleting the design system's `:focus-visible` rule leaves all 864 checks green (recorded in `bugs.md`). WP3 of the rebuild deletes that check as a documented no-op. Deleting a known-vacuous gate is right; the problem is that **the repo would then have no automated focus-indicator coverage at all**, and AGENTS.md § Accessibility still promises a visible `:focus-visible`. Options: (a) add a real focus-indicator assertion to WP1's geometry lane, measuring the system's own ring rather than forced-colors'; (b) accept review-only coverage and say so explicitly in `bugs.md` and `design.md`. Recommendation: **(a)** — it is a few assertions inside a lane that already mounts every fixture, and AA focus visibility is a WCAG 2.2 commitment this system makes in writing. Either way it needs a decision before WP3 merges, and WP3 belongs to the rebuild session, not this one. Written into `briefs/g1b.md` so it cannot be lost.

## Log

- **2026-09-09 03:25 IST — MK: HOLD. No merges, no rebases, no new sweeps.** Directive: hold all audit merges; do not touch PR #71 or #70; let in-flight sweeps finish, then wait. MK will send word when `main` contains WP2 **and** WP4; rebase onto `main` only after that message, **never** onto the rebuild branches. Full snapshot for resumption in `$OPS/HOLD-STATE.md` (every audit branch, head, dirty count, worktree, and PR/check state). Rationale accepted from the options put to MK: with every open PR conflicting, landing 12 batches costs ~3h of mostly-serial sweeps under the receipt topology versus ~30-40 min in parallel once WP2 removes receipts and WP4 removes the generated-file conflict engine. **Every sweep run from now is wasted by construction**, because anything swept today must be rebased onto post-WP2/WP4 `main` regardless.
- **2026-09-09 03:25 IST — agents stood down.** N1, Mk1, P1 and O2 stopped. N1/Mk1/P1 had already rebased themselves onto merged O1 before the directive arrived, so **#73, #74 and #75 are MERGEABLE** and will still need one more rebase after WP2/WP4. O2 was stopped mid-sweep — its contracts lane completes on the box uncollected; inspect `agent-a89ebf2958f1d7aa3` (1 dirty file) before resuming it. C1 was left running because its sweep was in flight per the directive; PR **#76** is open and CONFLICTING at `b8b99f88`. Nothing is lost — every batch's work is committed in its worktree.
- **2026-09-09 03:20 IST — D1 shipped: PR #72, fully green, NOT merged** (head `e0048fb5`, based on `54c5cb68`). `verify` pass 14m7s · `verify-linux` pass 1m32s · `receipt-guard` pass 25s. `pnpm audit` **47 → 1** (0 critical, 0 high; the remaining one is a Windows-only esbuild path via `tsup`). AGENTS.md § Sanctioned dependency exceptions now lists `react-day-picker` as the fifth headless primitive and adds a **Theme engine** class for `next-themes` — **MK should confirm that wording binds future batches the way intended**. D1 confirms the cost of the O1 merge timing concretely: O1 landed ~30 s after D1 rebased, voiding its first receipt and forcing a second full sweep (111/888 then 112/896 checks). Its sonner toast-leak root fix survived both rebases as `63fd36d`. **D2's stacking base `203b2229` no longer exists on D1's branch**; D2 must still rebase `--onto origin/main 203b2229` (the object survives) after D1 merges, never onto D1's current head.

- **2026-09-09 03:05 IST — `briefs/g1b.md` written (re-scoped).** G1-b had no brief and roughly half its original scope was written against machinery WP2/WP3 delete. KEEP: the twelve design-lint rules, the hover-fill-without-active-rung rule, `packages/ui/test/` into the tsconfig `include` (now urgent — WP1's geometry lane lives there and is currently not type-checked at all), deriving the inventory count in `verify-component-contracts.mjs` from `registry.json` (O1 just proved this fails open: `components: 111` against `totalRegistryItems: 559`), and a token-reference lint. DROP: receipt v2, the generated-only receipt carry, the verify-only hook mode, contract-lane scope classification and the `TITLE_SUFFIXES` gap, the Playwright worker cap — all of it is work against deleted code. VERIFY-THEN-DECIDE: whether WP1's per-fixture iteration closes the `.first()`-only probing hole, and whether its fixed-`now` mount closes the `relative-time` target-floor race; the brief forbids leaving `bugs.md` claiming defects that no longer exist. New Needs-MK #22 raised out of this work.

- **2026-09-09 02:55 IST — merging O1 on green triggered the conflict cascade the merge train exists to prevent. Audit merges are now PAUSED.** Within minutes of #66 landing, N1 **#73**, D1 **#72** and the peer session's WP2 **#71** all flipped MERGEABLE → CONFLICTING. The pre-approval to "merge a batch PR on green CI" is real, but it is not the whole protocol: `briefs/conductor.md` and `finish-after-f2.md` warn that a squash merge conflicts every sibling through the same generated surfaces (`public/r/*.json`, the integrity manifest, `component-contracts.json`, `component-matrix.md`, AGENTS.md §Numbers), and that a rebase moves the tree hash and **voids the receipt** — so every conflicted sibling now owes a fresh ~10-15 minute browser sweep. With twelve batches left that is on the order of a dozen sweeps to land work that is already written and green. The train amortises this only for branches STACKED on one another; N1/C1/Mk1/D1 were each based on plain `main`, so nothing protected them. Merging O1 did not create the cascade — it is structural, and any first merge starts it — but merging while five siblings were mid-sweep made it maximally expensive. **Policy from here: no audit PR merges until either WP2 lands (receipts disappear; a PR needs only green CI on the Linux runners, and rebases stop costing sweeps) or an explicit stacked merge train is sequenced. In-flight finishers still complete and open their PRs; those PRs will be green-but-behind and get rebased in one pass.**
- **2026-09-09 02:55 IST — the coupling with the rebuild, stated plainly.** WP2 removes the receipt, so a rebase stops costing a sweep. WP4 ungits the four generated docs files that cause substantially all of these conflicts — it removes the conflict engine itself. Both are the peer session's, both are currently red on `verify`, and my O1 merge conflicted WP2 #71; the peer has been told, with an apology and the note that `gates`/`gates2` are being held idle for its CI. Until one of those lands, every audit merge costs the remaining batches a sweep each, which is the single largest remaining cost in this epic.

- **2026-09-09 02:40 IST — O1 MERGED (#66 → `54c5cb68`).** CI verified independently before merging, not taken from the agent's report: `verify` 19/19 steps, `receipt-guard` 7/7, `verify-linux` 9/9, all at head `3127e0aa` matching the PR. The sweep ran 896/896 contract checks over 112 routes on gates3 in 530s with no flakes. Recovery detail worth keeping: the predecessor's 18 uncommitted files were all legitimate batch work, and two were real source fixes — `component-contracts.json` and `verify-component-contracts.mjs` carried `components: 111` against `totalRegistryItems: 559`, which does not add up (112 + 439 + 7 + 1 = 559); `floating-surface` is the 112th component. Registry is now **112 components / 559 items**. O1 was written and verified against Base UI **1.6.0** — D1's 1.8 bump has not landed — so Drawer and the rejected Tooltip-`role` finding (B3-10) want re-verification once D1 merges. Five Needs-MK items are in #66's body: `floating-surface` shipped as a registry item rather than a `@vegastack/design` export; `AlertDialogContent intent` deleted rather than wired; `context-menu.tsx` 291 lines against the brief's <150 (134 are code, the rest is the JSDoc `verify-public-api-docs` mandates); Combobox non-modal while Popover/Select are modal (evidenced D12 exception).
- **2026-09-09 02:45 IST — P1 (#45) and O2 (#41) launched**, unblocked by O1's merge. Both replay `--onto origin/main` off O1's old head. O2 must additionally verify its own scope: its predecessor was killed with a test as the last commit, so completeness is not assumed.
- **2026-09-09 02:35 IST — runner defect found by O1 and fixed as `remote-gates-v4.sh`.** v3 printed a receipt-freshness verdict ONLY when the remote command exited 0 — but `pnpm gates:push` exits nonzero on a first sweep, because the receipt is not yet in HEAD, so the COMMON path produced no verdict at all and agents guessed. v3 also swallowed `.gates/` copy-back failures into `2>/dev/null`, leaving the previous run's receipt in place looking current; O1 nearly committed a stale one. v4 fails loudly (exit 2) if the copy-back fails and verifies the pulled receipt against the local tree on every gates command whatever the exit code, keeping `exit 3` for the "tree moved during a successful run" case only. `_common.md` now mandates v4 and forbids committing a receipt without either a `✓` line or a hand-run `verify-gate-receipt.mjs`. **Process note:** the fix was first written into v3 in place while four agents were mid-run on that script — bash reads a script by byte offset, so that could have corrupted a running instance. Caught within ~90s; v3 was restored byte-identical from backup (verified with `cmp`) and the fix shipped as a new file. The three in-flight sweeps were confirmed alive afterwards. Never edit a script in place while agents are executing it; write a new path.

- **2026-09-09 02:25 IST — correction: the rebuild session was alive; this session's WP2 finisher was a duplicate and has been stopped.** Three mistakes compounded. (1) The clock: this session wrote "09:10 IST" on the board and in the epic-#31 comment from assumption rather than from `date`; the real time was ~01:50. (2) The inference: an agent worktree holding uncommitted work with HEAD at `origin/main` was read as abandoned, when it is exactly what a live session's tree looks like between commits — the 01:32 restart notice made the wrong reading feel confirmed. (3) The check not run: `mcp__ccd_session_mgmt__list_sessions` would have shown "Project development bottlenecks analysis" (cwd `vegastack-design`, PR #68) active seconds earlier, and `gh pr list` at launch time would have shown #70/#71 about to appear. **Rule from here: before adopting any work another session may own, check `list_sessions` for a live peer in the same cwd and `gh pr list` for its branches — worktree state alone never establishes abandonment.** No damage: the stopped agent committed nothing, the tree is clean at `418a9d55` == PR #71 head. The peer has been sent a deconfliction message handing WP2/WP4 back and naming the one place our work touches (WP2 early helps the batches; WP3 must land after them).
- **2026-09-09 02:20 IST — batch wave progress.** D1 **PR #72** open (`verify-linux` green), O1 **#66** rebased and now MERGEABLE (`verify-linux` green), N1 pushed `5a71ceca` and Mk1 `72d491ff` (PRs pending), C1 rebased to `63ded168` — its first sweep hit the known cold-worktree Vite dep-optimizer flake and is re-running once. Sweeps occupy gates3/4/5 (load 15–22); gates and gates2 held idle for CI.
- **2026-09-09 02:05 IST — rebase trap recorded in `briefs/_common.md`** (found by C1): taking main's side on a file whose _only_ visible conflict is a provenance hash silently drops your batch's earlier edits to that file. C1 lost its `Empty bordered` → `variant="dashed"` migration across `board.tsx`, `dropzone.tsx`, `dashboard-01/page.tsx`; `typecheck` caught the code but two stale docs references survived. The brief now requires `git log --oneline origin/main..HEAD -- <file>` before resolving either side wholesale.

- **2026-09-09 01:50 IST — session 3 opens.** Session 2 ended in a Claude Code process restart that killed every agent; so did the session driving the verification rebuild. Cleanup done: the orphaned T1/C1 sweeps on `gates`/`gates2` were killed, all five box lanes are free and idle, worktrees pruned. Four of the five dirty audit worktrees held nothing but a modified `.gates/receipt.json`; only O1 had real work in flight (18 files, mid "regenerate after rebase onto Di1").
- **2026-09-09 01:50 IST — sequencing change: WP2 goes before the merge train.** WP2's worktree turned out to be substantially written but entirely uncommitted (`pnpm verify` / `verify:release`, `tooling/verify.mjs`, `workspace-clean.mjs`, `pre-commit.mjs`, `tooling/test/`, rewritten `ci.yml`/`release.yml`/`deploy.yml`, deleted `.husky/pre-push` and `.gates/`), with nobody driving it. The plan's own ordering constraints only require WP0 before WP2 and WP1 before WP3, so landing WP2 now is not a reorder — and it is what converts thirteen stacked receipt sweeps and a serialised merge train into "rebase, green CI on the Linux runners, merge". A finisher has adopted it: commit, measure the §5 timings, produce the §5 negative proofs, PR, green. WP3–WP6 stay sequenced after the batches. Every batch finisher is told to re-check `origin/main` for a `verify` script and an absent `.husky/pre-push` immediately before it would dispatch a sweep, and to skip the sweep and the receipt entirely if WP2 has landed.
- **2026-09-09 01:50 IST — wave launched (6 Opus agents):** WP2, plus finishers for O1 (#66, recover the 18-file regeneration), N1, C1, Mk1 and D1 (rebase → static gates → PR). Sweeps, where still needed, are pinned to gates3/gates4/gates5 only — `gates` and `gates2` are now GitHub Actions runners under WP0 and their capacity is reserved for CI.

- **2026-09-08 18:25 IST** — MK provided three more Ryzen boxes (gates3/4/5). Onboarded in ~10 min each (keys, sudo, Node 24, pnpm, mirror, Playwright incl. WebKit, `button` contract acceptance 8/8 on all three). Runner now schedules ONE lane per box across five boxes (measured: two lanes per box gained nothing and caused most unit-lane flakes). Queue of ~8 sweeps now drains in parallel.

- 2026-09-07 · board created; wave 1 launched (F1, D1, G1-a, Do1-a).
- 2026-09-07 · D1 implementer yielded mid `gates:push` (cold-worktree Vite dep-optimizer reload storm in the unit lane + a suspected duplicate `prosemirror-model` after the tiptap bump). No commits yet. Handed the same worktree/branch to a continuation agent with that diagnosis; lesson applied to later briefs: "do not yield while a gate is running".
- 2026-09-07 · all five wave-1 sub-agents (F1, D1×2, G1-a, Do1-a) were killed by the Fable session rate limit mid-work (uncommitted diffs preserved in their worktrees; D1 has one commit). Per MK, sub-agents now run on **Opus 5 (high reasoning)**; the orchestrator stays on Fable. Relaunched all four as continuation agents in the same worktrees/branches.
- 2026-09-07 16:20 · D1 continuation stopped again mid-gate (step 3 applied, uncommitted). Relaunched with an explicit poll-and-wait rule (background gate + foreground pgrep loop). TD-5 resolved by measurement: pnpm 11 writes `minimumReleaseAgeExclude` itself; explicit floor is now strict. style-dictionary held at 5.5.2 (5.5.3 changes token CSS → F1's territory; security hop was 5.5.1).
- 2026-09-07 16:35 · Harness lesson: an implementer's "completed" notification that reads "waiting on the gate" is a yield, not an exit — the agent re-wakes when its background gate finishes. My 4th D1 continuation therefore overlapped the 3rd for a few minutes (one commit subject got crossed; amended, tree unaffected). The 3rd agent stood down on its own; the 4th owns `audit/d1-deps-security-alignment` (steps 1–3 committed green: b3d0d425, 2b4f40bf, cbc30501; step 4 bumps applied, consume round-trip running). Rule from here: relaunch only on `failed` or proven inactivity.
- 2026-09-07 16:55 · G1-a PR #53 opened (8 commits, head 1b8c7b1c). `receipt-guard` executed and passed on CI run 34114469157. Implementer found and fixed two defects in the inherited work (a negative-fixture anchor broken by folding a ci.yml step into `pnpm lint`; an `import { ROOT }` that landed inside a template literal — caught by `release:preflight`, not `lint`). Reviewer launched. Note for MK: after merge, `main`'s committed receipt is schema 1 and must be re-minted with `pnpm gates:ship` before the next deploy (TD-1).
- 2026-09-07 17:25 · MK: maximise parallelism; Codex (gpt-5.6, high reasoning) for background review delegation, findings validated by the orchestrator before acting. Codex runtime verified ready (codex-cli 0.153.4). Launched F2 (on F1's branch head, rebases on F1 merge) and I1 (independent) ahead of wave 1 finishing.
- 2026-09-07 17:45 · #53 (G1-a) CI green: `verify` + `receipt-guard` executed. **Merge held on purpose**: G1-a moves the receipt schema to v2, so anything merged after it needs a rebase + browser-gate rerun for a v2 receipt. Wave-1 merge order: F1 → D1 → Do1-a → rebase G1-a (tooling-only) → merge G1-a. Then `pnpm gates:ship` re-mint on `main` (MK/orchestrator).
- 2026-09-07 18:00 · #53 reviewer verdict CHANGES (head 1b8c7b1c): high = two stale headers in `gates.mjs` (count literal; claims `tooling/**` is a turbo input); medium = `release-classify` prints "quality-gate skipped" though unconditional, promoted audit probes exit 0 with "0 probed" when the export is missing (fail-open), README "768 contracts"; low = missing bugs.md entries, epic inventory row, changelog wording, a 28th ROOT derivation, `histogram.mjs` raw readdir, dated slug in `out-dir.mjs`. Forged-receipt tests confirmed the guard discriminates (valid ship receipt accepted, four bad ones rejected). Fix agent launched on the same branch.
- 2026-09-07 18:20 · Do1-a PR #54 opened (head 0e48ed55). Notable: the literal acceptance grep `<[A-Z]` → 0 is unachievable once fixture source is inlined; the gate measures JSX outside code (0 across 143 files). DC-05 (hero via ComponentPreview) moved the contract probe onto the hero fixture on all 110 routes and exposed three real 24px-target defects (timeline, data-grid, text-edit) — recorded in bugs.md for the owning batches; the probe key was kept off the hero so coverage matches main. `.first()`-only probing flagged for G1-b. Codex background review started (task-mtr6vmwx-8wd6xu, gpt-5.6-sol high).
- 2026-09-07 18:40 · #54 CI: `receipt-guard` passed, `verify` failed only at `changeset status` — `packages/ui/component-contracts.json` changed (dataAttributes) with no changeset. Orchestrator integration fix: `@vegastack/ui` patch changeset added in the Do1-a worktree, receipt re-minted, pushed to the same PR. Rule for later batches: any `packages/**` edit (including the contracts authority) needs a changeset.
- 2026-09-07 19:05 · Codex (gpt-5.6-sol) review of #54 returned CHANGES from a read-only sandbox (builds blocked; findings came from source-level probes and were validated by the orchestrator). Actionable here: (1) unknown MDX components silently dropped from the agent export despite a "fails the build" claim; (2) emitted-CSS gate neutralised by any narrower override; (3) docs-shell browser suite orphaned + missing portal/inert assertions; (4) Banner + per-page callout both render on 107 pages; (5–7) ledger/changelog/doctrine overclaims ("no page with neither", "every page on the canon", "four negative self-tests"); (8–9) comment fixes. Routed elsewhere: Button variant/size table defaults → F2; `.first()` fixture probing → G1-b; two Do1-b transitional aliases → Do1-b acceptance. Fix agent launched on the same branch.
- 2026-09-07 19:20 · F1 PR #55 opened (head 793ea983, 11 commits, 154 files). Residual probe flags are structural (Base UI opens popups on pointerdown so triggers never paint `:active`; drag handles take pointer capture; checked Checkbox/Switch/Radio hover → Fo1; hand-rolled icon buttons → F2). Two bugs recorded that the audit missed: a light-only token alias leaks the light value into `.dark` (fixed; rule: declare theme-split aliases in both sources); the toaster mirror is not covered by `registry:build` (fail-late → G1-b). Codex review started (write-enabled sandbox). Merge order unchanged: #55 first.
- 2026-09-07 19:35 · **F1 merged** (#55 → 9c33dfaf; CI verify + receipt-guard executed). Release workflow on main is changeset-bearing → a Version Packages PR will open; it stays unmerged (MK's publish decision). Launched T1 (#37) on the new main; other wave-3 batches wait for F2's size-key rename to avoid 26-file conflicts. F2 rebases onto main now.
- 2026-09-07 19:50 · #53 fixes pushed (97e2feda); CI 34126250332 green, both jobs executed. Extras: `tooling/verify-tooling-imports.mjs` (TypeScript binder over `tooling/**/*.mjs`, TS2304/2552 only — `node --check` provably misses the class) wired into `lint:repo`; workflow-security negative harness 18 → 20 mutations; probes now refuse a missing export or zero probed elements (exit 2); `verify-release-chain.mjs` no longer discards the consume step's stderr (an unreproduced under-load failure had produced a wall of green ticks with no reason). Still held for merge order.
- 2026-09-07 20:00 · Release run on main after F1 (34126284981) green: receipt-guard + quality-gate executed; `publish` correctly skipped; **Version Packages PR #56 opened** (changeset-release/main). It is MK's publish gate — the orchestrator never merges it; it will keep updating as batches add changesets.
- 2026-09-07 20:05 · MK: go faster. Pulled wave 3 forward: M1, Fo1, O1, Di1 launched on F2's committed head (2ac84d39) with the rule `git rebase --onto origin/main 2ac84d39` after F2's squash-merge, PR only after F2 lands. T2/N1/C1 (F1+F2 only) start as D1/Do1-a free their slots. Waves 5–7 and M2/O2/P1/Mk1 remain dependency-bound. Constraint noted: machine load ~64 makes each gate 15–40 min; concurrency capped at ~8 agents.
- 2026-09-07 20:35 · I1 PR #57 opened (head 79d84385). Fail-opens fixed: per-render reduced-motion effect in all 439 icons; a reduced-motion test that could not fail (Motion's `useReducedMotion` singleton) — factory now uses `useReducedMotionConfig` with a `reducedMotion="never"` negative control. Four upstream sources had drifted; re-pinned after review. Codex review started. Note: #57 touches `pnpm-lock.yaml` (motion for `@vegastack/design`) — D1 will rebase across it.
- 2026-09-07 20:40 · Second rate-limit cut (Opus session limit) killed D1, Do1-a-fix, T1, M1, Fo1, O1, Di1 mid-work; F2 survived (yielded on its final gate). Worktrees intact (T1 73 / Di1 50 / Fo1 21 / M1 12 uncommitted files; Do1-a fixes committed, unpushed; D1 7 commits). Briefs now live on disk (`scratchpad/briefs/*.md` + `_common.md`) so relaunches are one line. All seven relaunched at 20:50 with "wait for orphaned gate processes" + verification-budget rules (one `gates:push`, one docs build, touched routes only). I1 #57 CI failed on a pre-rebase changelog sha → repointed by the orchestrator; receipt re-mint in progress.
- 2026-09-07 21:00 · MK: Playwright workers (10–15) dominate CPU. Cause: 3–4 concurrent contract sweeps × 5 local workers + Vitest's 4 Chromium workers. Decision: browser lanes are serialised machine-wide via `/tmp/vega-browser-lane.lock` (in the shared brief for wave 4+); implementation stays parallel. Local Playwright worker cap (3) → G1-b. Full 110-route sweeps stay reserved for foundation-class changes (existing route-scope rule).
- 2026-09-07 21:35 · MK provided two LAN Ryzen boxes (Debian 13, 12T/14 GB) for browser-lane offload. Plan reviewed against code: `verifyReceipt` checks tree/contract SHA/Playwright pins/gate status only (no host check) → a Linux receipt is valid; three gaps flagged (per-worktree remote dirs + per-box lock for concurrency; HEAD parity via `git push` to a bare repo on the box instead of GitHub; a verify-only mode for the pre-push hook, → G1-b). Key auth + passwordless sudo installed; base provisioning running on both. Test run planned on F2's worktree before changing the mandate.
- 2026-09-07 21:50 · Codex review of #57 (I1): CHANGES. Validated + routed to a fix agent: (1) HIGH fail-open — `verify-animated-icons` accepts a hand-edited icon path (manifest hashes only checked for shape; nothing binds module data to upstream bytes) → per-icon generated-body hash + path/choreography self-test mutations; (2) "eight deprecated aliases" is inaccurate — seven deprecated + one rename (`ChevronsDownUpIconHandle` → `ChevronFirstIconHandle`), documented as such, no aliases re-added (mandate); (3) gallery card lost the touch trigger; (4) Motion 12's `useReducedMotion` never re-renders on a live OS change → matchMedia store via `useSyncExternalStore`; (5) line count claim wrong (12,951 not 12,823) — corrected, thresholds stay "Needs MK: accept". DC-16 gallery route split → new issue (orchestrator). Changelog sha already repointed. Push of #57 in flight (pre-push hook re-runs the ladder — the verify-only-hook gap is G1-b's).
- 2026-09-07 22:55 · Claude Code process restarted; all agents/background shells stopped (worktrees intact, three orphaned gate runs still finishing on the Mac). Relaunched: D1, Do1-a-fix, T1, M1, Fo1, O1, Di1, I1-fix, and an F2 finisher (F2's implementer was mid fourth gate attempt). Ryzen boxes: base provisioned (Node 24.20, pnpm 11.7); `main` + F2/I1 branches pushed to their bare mirrors (mirror pushes bypass the local pre-push hook — it is CI-only in purpose); clone + `pnpm install` + Playwright browsers + button-contract acceptance running on both.
- 2026-09-07 23:25 · Ryzen Phase 0 acceptance PASSED on both boxes (button contract 8/8; 135 s / 153 s incl. docs build). `remote-gates.sh` written (push HEAD to the box mirror, per-worktree `~/wt/<name>`, rsync dirty tree, per-box flock, run, rsync `.gates/` back, verify receipt covers the local tree). First real test: full `gates:push` of I1's tree on box 1, running. Known gap: `.husky/pre-push` has no verify-only mode, so a Mac-side push re-runs the ladder — the runner path will bypass the hook only after a verified remote receipt; G1-b adds `gates push --verify-only`.
- 2026-09-07 23:30 · **MK: switch all agents to the remote runner; the Mac executes only.** Remote runner proven: box 2 scoped contracts 16/16 (193 s incl. docs build); box 1 full ladder on I1's tree — unit 61 s, smoke 33 s (WebKit works on Linux), contracts sweep in progress. Runner v2 (`scratchpad/remote-gates-v2.sh`): mirrors origin/main + HEAD to the box's bare repo, fetches GitHub's public PR refs (squash-merged shas the changelog links), per-worktree `~/wt/<name>`, rsync of the dirty tree, per-box flock, least-busy box selection, results + receipt copied back and verified locally. Shared brief rewritten: no browser process on the Mac; push of a verified remote receipt uses `HUSKY=0` for that push only (G1-b adds `gates push --verify-only`). All nine agents stopped, Mac lanes killed, relaunched under the rule.
- 2026-09-07 23:50 · Remote full ladder (box 1, I1's tree): typecheck/lint pass, unit 61 s, smoke 33 s, contracts 880 executed in 890 s — 3 failures, all `relative-time` 320 px in 3 lanes: `scrollIntoViewIfNeeded: Element is not attached to the DOM` (the ticking fixture re-renders between `toBeVisible` and the scroll — a spec race, not reflow; same tree passed 880/880 on the Mac). Re-running the route to classify. **G1-b**: make the 320 px check resilient to a re-rendering fixture (re-resolve the locator / retry the scroll) and add the box's Linux fonts/locale to the spec's assumptions if they matter.
- 2026-09-08 00:05 · Codex review of F1 (merged): CHANGES — (1) pressed-step gaps on chip-remove, selected calendar day, selected Segmented/Tabs items, NumberField/Pagination hand-written recipes, three docs-chrome controls → routed to T2/P1/N1/Fo1/Do1-b + a G1-b lint rule ("hover fill without a sanctioned active rung fails closed"); (2) stale prose after F1 (`--alpha-surface-subtle` in two guides; design.md "solid" border, "seven-token ramp", secondary as card fill, `track`), an under-gated `media-foreground` text pair, and a specimen mislabelling opaque rungs as alpha twins → small follow-up PR launched now (`audit/f1-docfix`); (3) bordered-hover vs P1 remains Needs MK #1; the status-ink retune is confirmed justified (7 of 8 old pairs failed AA on `subtle-active`). Routed items tracked in `scratchpad/briefs/fix-round.md`, re-verified in the end round.
- 2026-09-08 00:15 · `relative-time` 320 px re-run twice on box 1: 8/8 both times (25 s). Classified: a locator race under full-sweep load (ticking fixture re-renders between `toBeVisible` and `scrollIntoViewIfNeeded`), not an environment defect. **Remote ladder proven end to end**: typecheck/lint, unit 61 s, smoke 33 s (three engines), contracts 880 executed in ~15 min on one box. G1-b hardens the 320 px locator.
- 2026-09-08 00:20 · **Do1-a merged** (#54 → d5c960a3). All 11 Codex findings fixed; docs-shell suite 32/32 and all 110 contract routes ran on a Ryzen box (receipt `host.platform: linux`, accepted by receipt-guard). Do1-b must remove the two transitional aliases (`AutoTypeTable: ApiTable`, slug-inferred `registry`). Release run on main watched. Wave-3 branches rebase across Do1-a's docs changes before their PRs.
- 2026-09-08 00:50 · Release run on main after Do1-a green (Version PR #56 updated). F1 follow-up (`audit/f1-docfix`, 8abf203f) complete: deleted-token references purged from guides/doctrine/skills; ramp is eight tokens (`subtle-active`); secondary = surface-1 over a transparent border; switch off-track = surface-3 (design.md contradicted itself); media-scrim text gate raised to 4.5:1 (measures 5.22, proven non-vacuous at 6:1); SurfaceLadder specimen now paints real alpha composites over three hosts. Blocked by the pre-existing `relative-time` 320 px race — reproduced on unmodified main (`contracts:all` fails 1–3 lanes, route passes 8/8 alone): the fixture's `setTimeout`→`setClock` tick lands inside `scrollIntoViewIfNeeded`'s stability wait. Decision: fix at the root in `contracts.spec.ts` (re-resolve + bounded retry of the scroll; assertions unchanged; `.first()` stays G1-b's). Every global-scope PR (F2 next) would otherwise hit it.
- 2026-09-08 01:00 · Third Opus rate-limit cut (reset 06:40). Worktrees intact: D1 14 commits, F2 10, T1 9, I1 10 (fix pushed, CI green), O1 20, Di1 19, F1-docfix 5 (incl. the spec race fix). D1 had added a speculative `RelativeTime` hydration change its own probe then disproved — the continuation reverts it and corrects the ledger.
- 2026-09-08 12:00 · Resumed. Boxes idle. Relaunched the critical path: F1 follow-up (spec race fix) → F2 → D1 → T1 → I1 fix; M1/Fo1/O1/Di1 relaunch when F2 merges (they only need the rebase + final sweep).
- 2026-09-08 12:25 · MK: use the boxes optimally, defer/batch evaluations, focus on execution. Measured: Mac load 4.8; box loads 8–18 of 12 with one sweep each (headroom in single-threaded phases). Changes: (1) runner v3 — two lanes per box, least-busy across four lanes; (2) verification budget — exactly one browser run per batch (the remote `gates:push`), no per-batch `gates:component`/probes/captures; the orchestrator batches probes + captures per wave on a box; (3) no per-PR Codex review — reviews move to the consolidated end round (per-PR fix loops were the second-largest time sink after rate limits). Merge on green CI unchanged; G1-a still merges last.
- 2026-09-08 12:30 · Pulled wave 4 forward: T2, N1, C1 launched on F2's committed head (eb4abb05) with the `rebase --onto` rule; their fix-round items (pressed steps on chip-remove/pagination/segmented/tabs, timeline hero 24 px) are in their briefs.
- 2026-09-08 12:50 · F1 follow-up **PR #59** open (doctrine/guides/skill reconciled; media-scrim text gate 4.5:1; SurfaceLadder specimen paints real alpha composites; 320 px contract check retries the scroll on a re-resolved locator — root cause corroborated: the fixture sits inside Fumadocs `<Tabs>` which can remount its panel). Full sweep green on box 2 incl. `relative-time`; receipt-guard passed; merge on `verify`.
- 2026-09-08 13:05 · **#59 merged** (065315d5). Branches opening PRs from here rebase onto it and get the resilient 320 px check for free.
- 2026-09-08 13:20 · MK: maximise implementation parallelism on the idle Mac; batch tests per phase. Dependency re-examination: O2 and P1 start on O1's committed head (floating-surface/panelSearch present), M2 and Mk1 on F2's head; all replay onto main with `--onto` after their bases merge. Deferred by real dependency: D2 (lucide rename sweep touches every component → after the component batches), D3 sub-PR 1 (on I1's factory, after D1+I1 merge), Do1-b, G1-b, D3 final. Twelve agents now in flight; tests = one remote sweep per batch, probes/captures once per wave.
- 2026-09-08 13:40 · N1 code-complete (6 commits; `selectedChipVariants` in alpha form so selected chips still hover/press; Tabs line wash 4 px off the rail; `persist`; `landmark` prop on shell parts). Holding its sweep for F2. Box 1 queue: D1 (running 35 min, old single lock) → F2 → T1; box 2: I1. F2's PR expected ~14:10.
- 2026-09-08 14:05 · I1 #57 ready: 7/7 Codex findings + an 8th stale count fixed; rebased onto 065315d5; CI 34202469808 green. Merge order: F2 → I1 → D1 → T1 (shared 0.7.0 changelog entry). Box note: one remote unit run hung at 2% CPU for 24 min on an idle box, killed and re-run (32 s) — watch for recurrence.
- 2026-09-08 14:20 · F2's sweep failed on a SECOND race path in the same fixture: `relative-time` re-renders during the 24 px target-floor probe (`control.evaluate(scrollIntoView)` path, which #59 did not harden). Finisher recorded it (5dc0bc0d) and re-ran on box 2. Fallback if it recurs: make the docs fixture deterministic (fixed `now`) AND harden the target-floor probe; G1-b keeps the general `.first()`/re-render resilience item.
