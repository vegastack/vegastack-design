# Mk1 · Marketing leaves, hooks and the dashboard block (#47) — `audit/mk1-marketing-hooks-block`

Base: F2's UNMERGED committed head `b7240267a2b863521612f272609c8f35a0d5b78f` (local). `git fetch origin && git checkout -B audit/mk1-marketing-hooks-block b7240267a2b863521612f272609c8f35a0d5b78f`; `pnpm install --frozen-lockfile`. Before the PR: wait for F2 to merge, then `git rebase --onto origin/main <your first commit>^`; also rebase across T1 (#37, the block's DataList `mobile: "merge"`), O1 (#40, `panelSearch`/`DialogContent size` for ShortcutOverlay), M2 (#36, `motion-dock`) and I1 (#46, `mergeRefs`-adjacent icon code) if they land first. Never open a PR containing another batch's commits.

Decisions FINAL: D29 (single-series chart = foreground ink — F1 added `--chart-single`), D30 (next-themes sanctioned; AGENTS.md text is D1's — do not edit AGENTS.md). Sources: `02-batch-09-marketing-hooks-icons.md` B9-03…B9-07, B9-09…B9-12; issue `issues/16-mk1-marketing-hooks-block.md`. Files: `packages/ui/registry/ui/{use-mobile,use-platform,animated-number,message-scroller,particle-field,pricing-section,logo-row,staggered-text-reveal,testimonial,settings-row,shortcut-overlay,chart}.tsx|ts`, `packages/ui/registry/blocks/dashboard-01/**`, `packages/design/src/index.ts` (`mergeRefs`), the nine files with inline `typeof ref === "function"` merges, `use-file-drop.ts` tests, `component-contracts.json`. Docs: React 19 `useSyncExternalStore`, `matchMedia`, IntersectionObserver, recharts 3.9, Base UI Dialog (installed).

## Scope (all of it)

1. `use-media-query.ts` registry hook on `useSyncExternalStore` with `serverFallback`; `usePrefersReducedMotion`, `useIsMobile` become one-liners over it; `usePlatform` uses it for the touch query; `useIsMobile` no longer starts `false` on the server (SSR renders the declared fallback). `mergeRefs` moves to `@vegastack/design` and replaces the nine inline merges (`grep -rn 'typeof ref === "function"' packages/ui/registry/ui` → 0) and the export hiding in `use-animation-replay`.
2. PlanCard highlighted → `surface-3` + alpha `primary` border + neutral/primary badge (F1 removed `info`; verify/finish); block empty state neutral `EmptyMedia`.
3. Block: KPI label `line-clamp-2` with the trend badge on the value row; `BreadcrumbTrail maxItems={2}` in the header; single-series chart on `--chart-single`; 1280 and 320 captures show full KPI labels and a one-line header (end-round capture; assert via a unit/contract test where possible).
4. LogoRow: marks `muted-foreground` → `foreground` on hover, no underline; logical `-ms-px border-s`; `grid-cols-[repeat(auto-fill,minmax(…))]` wall.
5. ParticleField reads `getComputedStyle(canvas).color` per frame (theme toggle test: pixel colour changes without remount); StaggeredTextReveal `whenVisible` (default on, IntersectionObserver); Testimonial `<q>`/CSS `quotes`; `SettingsSection` title `render`/`as`; ShortcutOverlay `DialogContent size="md"`, `--layout-overlay-max-height`, `panelSearch` input (O1's recipe is on your base).
6. Tests: `use-file-drop` unit suite (paste path, `accept`, directory traversal, `maxSize`); `use-platform`/`use-mobile` cases; previews per B9-12 (pricing, ruled-band, particle-field).
7. Doctrine `design.md` §Charts (single series = ink), §Marketing leaves (promotion colour) + `pnpm design:sync`; skills (`component` skill §4 responsive: container queries vs `useIsMobile`); changelog (`🧩 use-media-query`, `🔧`); changesets (`@vegastack/ui` minor, `@vegastack/design` minor for `mergeRefs`); ledgers.

Do NOT touch: T1's DataList/DataGrid internals (the block consumes them); O1/O2 overlays; M2's ActionBar/MessageScroller motion (you may only swap MessageScroller's `usePrefersReducedMotion` to the hook); I1's icons; N1's page-header/breadcrumb internals; `tooling/**` (except allowlist counts); docs chrome.

Acceptance: the two greps → 0; ParticleField theme-toggle test; `use-file-drop` suite; one remote `gates:push` incl. the block's contract route. PR title: `Mk1 · Marketing leaves, hooks and the dashboard block (#47)`, `Closes #47`.
