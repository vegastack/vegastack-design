# M2 · Rich text prose recipe, Base UI Toolbar, docked-control motion (#36) — `audit/m2-rich-text-toolbars`

Base: F2's UNMERGED committed head `b7240267a2b863521612f272609c8f35a0d5b78f` (local). `git fetch origin && git checkout -B audit/m2-rich-text-toolbars b7240267a2b863521612f272609c8f35a0d5b78f`; `pnpm install --frozen-lockfile`. Before the PR: wait for F2 to merge, then `git rebase --onto origin/main <your first commit>^`; also rebase across M1 (#35: code-block/tool-call-chip fixtures, media), Fo1 (#39: `text-edit` outline-none audit) and T2 (#38: FilterBar chips) if they land first — they touch files you touch; resolve, keep both. Never open a PR containing another batch's commits.

Decisions FINAL: D11 (overlay timings; docked controls 150 in / 100 out, no scale — amendment #7), the `prose` recipe (`04-cross-cutting.md` §1). Sources: `02-batch-04-media-rich-text.md` B4-09, B4-10, B4-11; `02-batch-08` B8-11; `02-batch-09` B9-08; motion register M-39/M-49 in `docs/ledger/`; issue `issues/05-m2-rich-text-toolbars.md`. Files: `packages/ui/registry/ui/{markdown-view,text-edit,code-block,action-bar,message-scroller,filter-bar,filter-bar-managed}.tsx` + tests/previews/MDX, `packages/design-tokens/src/utilities.css` (`motion-dock-in/out`), `packages/design/src/**` (if `prose.ts` lives there — decide: a `@vegastack/design` export vs a registry `lib` item; consumers who `shadcn add markdown-view` must get it, `verify-registry-deps` fails closed), `component-contracts.json`. Docs: Base UI Toolbar (installed version — `Toolbar.Root/Group/Separator/Button`, roving focus), tiptap 3.31 (editor class / `editorProps.attributes`), react-markdown components map, Tailwind 4.3.

## Scope (all of it)

1. `prose` recipe: class strings per element (h1–h4, p, ul/ol/li, code, pre, blockquote, table, hr, a, img) consumed by MarkdownView's components map and TextEdit's editor class; `pre` renders CodeBlock's surface classes. `grep -c "\[&_" text-edit.tsx` < 10.
2. Base UI `Toolbar` in TextEdit, ActionBar and FilterBar (roving focus, one tab stop, arrows); keep each public API. Unit test: Tab enters once, arrows move, Shift+Tab leaves; axe clean.
3. `motion-dock-in` / `motion-dock-out` utilities in `utilities.css` (translate + fade, 150 ms in `ease-emphasized`, 100 ms out `ease-exit`, no scale); ActionBar and MessageScrollerButton use them; MessageScrollerButton becomes `variant="outline"` with no inline override (F2's variants).
4. Docs: `text-edit.mdx` "Scope" into the canon position (content only); `code-block` headerless + long-line fixtures; `tool-call-chip` interactive + running fixtures (M1 may add the B4-11 fixtures too — check M1's branch `a86d5cc0…`; if present, rebase and keep theirs).
5. Doctrine `design.md` §Motion (docked controls; amendment #7), §Prose (one recipe) + `pnpm design:sync`; motion register M-39/M-49 updated in `docs/ledger/`; `component` skill §2 motion matrix (add the dock row); public skill; changelog (`🔧`, `🧩 prose` if a registry item); changesets (`@vegastack/ui` minor; `@vegastack/design-tokens` patch for the utilities; `@vegastack/design` if `prose.ts` exports there); ledgers.

Do NOT touch: M1's players/slider/attachment/image; Fo1's form leaves (text-edit's outline-none audit is Fo1's — coordinate by rebase); T2's chip/announcer parts of filter-bar (you own the toolbar wrapper); O1/O2; `tooling/**` (except allowlist counts); docs chrome.

Acceptance: `grep -c "\[&_" packages/ui/registry/ui/text-edit.tsx` < 10; MarkdownView and TextEdit render the same computed styles for h1/p/code (a unit test with the style-mirror technique, or the capture harness in the end round); one remote `gates:push` covering text-edit, markdown-view, action-bar, message-scroller, filter-bar, code-block, tool-call-chip. PR title: `M2 · Prose recipe, Base UI Toolbar, docked-control motion (#36)`, `Closes #36`.
