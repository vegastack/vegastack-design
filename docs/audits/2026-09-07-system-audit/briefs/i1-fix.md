# I1 fix task — PR #57 `audit/i1-icons-factory` (#46)

Worktree: `/Users/mk/projects/vegastack-design/.claude/worktrees/agent-a7f31d5f971cf5c04`. A `git push` with its pre-push gate may still be running there (started by the orchestrator to push a changelog-sha fix) — WAIT for it (`pgrep -f "agent-a7f31d5f971cf5c04/.*gates.mjs"`), then `git status`/`git log origin/audit/i1-icons-factory..HEAD` to confirm the branch is pushed and clean before you start. Codex review findings (validated by the orchestrator) to fix at the root:

1. **`tooling/verify-animated-icons.mjs` (high, fail-open)** — editing Bell's path string (`…3-9` → `…3-8`) leaves the verifier green: it only checks that manifest hashes look like SHA-256 and schema-checks whatever is in the module; nothing binds the generated module's data to the pinned upstream bytes. Fix: the mirror records, per icon, a deterministic hash of the canonical generated body (or the extracted data: svg + elements + choreography) in the manifest; the verifier recomputes it from the module and compares. Add path and choreography mutations to `--self-test` and prove they fail. Keep the count fail-closed (439).
2. **Removed public handle types (medium)** — the changelog/changeset say "eight deprecated aliases removed" but `ChevronsDownUpIconHandle` was the ORIGINAL non-deprecated interface for `chevron-first` (upstream misnamed its displayName) and `ChevronFirstIconHandle` was its deprecated alias. Under the mandate (no compatibility aliases) the removal of the seven deprecated aliases stands, and the eighth is a RENAME (`ChevronsDownUpIconHandle` → `ChevronFirstIconHandle`, exported symbol is authoritative). Document it accurately: `⚠️ Breaking`: seven deprecated aliases removed + one handle type renamed, with the old and new names. Do not re-add aliases. Record the reasoning in `operator-review.md`.
3. **`apps/docs/components/animated-icon-card.tsx` (medium)** — attaching the ref latches the icon into controlled mode, disabling its own touch `pointerdown`; the card replaces mouse-enter/leave and focus/blur but has no `onPointerDown`, so tap-to-play on touch has no driver. Fix: handle touch `pointerdown` on the card (pointerType rules as in the factory) and add a REAL `AnimatedIconCard` behaviour test (hover, focus, touch, reduced motion with a `reducedMotion="never"` negative control) instead of a hand-built button markup test.
4. **`packages/design/src/icons/create-animated-icon.tsx` (medium)** — Motion 12.42.2's `useReducedMotion()` is `useState(prefersReducedMotion.current)` with no subscription, so a mounted icon does not re-render when the OS preference changes; the "live change" claim is false. Fix: subscribe to `(prefers-reduced-motion: reduce)` via `useSyncExternalStore` (matchMedia change listener; SSR snapshot `false`) and combine with `MotionConfig`'s `reducedMotion` override (`"always"`/`"never"` win; `"user"` follows the store). Test a live media-query transition (stub `matchMedia` with a dispatchable listener) in addition to the static MotionConfig cases. Keep the effect deps correct.
5. **Numbers (medium)** — the changelog/PR claim 12,823 lines; HEAD measures 12,951. Correct every number in `CHANGELOG.md`, the changeset, `operator-review.md`, and post the corrected figures in a PR comment. Do NOT game the `<12,000` target by widening `printWidth`; keep 200 and keep the "Needs MK: accept the measured numbers" item (the byte target 1.56 MiB vs ≥2 MiB likewise).
6. **DC-16** — the gallery route split is now its own issue (see the orchestrator's PR comment / `gh issue list --search "DC-16"`); reference it in the PR body's "Left out" section. Keep `Closes #46`.
7. Changelog sha links — already repointed to `70fe6b2` by the orchestrator; make sure any new commits you link exist on the branch.
   Also: `useReducedMotionConfig` is not `@public`-documented in Motion; you are replacing it (item 4) — remove the import if unused; note the D3 (motion 13) compatibility in operator-review.

Finish: `node tooling/verify-animated-icons.mjs` + `--self-test` (now ≥12 mutations incl. path + choreography); `node tooling/mirror-animated-icons.mjs` regenerates byte-identically (network needed — if lucide-animated.com is unreachable, say so and verify against the committed manifest only); icon unit tests + the new card test + reduced-motion live-transition test (`pnpm --filter @vegastack/ui exec vitest run <files>`); `pnpm typecheck`; `pnpm lint`; `pnpm registry:build && git status --porcelain` clean; `pnpm design:derived && git status --porcelain` clean; `node tooling/changelog-lint.mjs`; `node tooling/sync-changelog.mjs`; ONE `pnpm gates:push` under the browser-lane lock (see `_common.md`), commit code + receipt; `git push` (the pre-push hook re-runs the ladder — that is the current topology; do not use `--no-verify`); confirm CI `receipt-guard` + `verify` executed and green (`gh run list --branch audit/i1-icons-factory`, `gh run view <id> --json jobs`; poll); `gh pr comment 57` with finding → fix → evidence. Final message = the comment + CI run id + job results. Do NOT merge.
