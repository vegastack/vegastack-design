# D2 · Dependencies: Fumadocs 16.15 family, lucide 1.41, axe-core 4.13, Playwright 1.63, recharts 3.10 (#50) — `audit/d2-deps-fumadocs-lucide`

Base: `origin/main` AFTER D1 (#34) and I1 (#46) have merged (D1 = Next 16.3/Base UI 1.8/shadcn 4.21; I1 = the icon factory + data modules the lucide regen must go through). Start only when both are on main: `git fetch origin && git checkout -B audit/d2-deps-fumadocs-lucide origin/main`; `pnpm install --frozen-lockfile`. Because the lucide rename sweep touches many component files, rebase onto `origin/main` right before the PR and expect to re-run the sweep once.

Decisions FINAL: none new; D1's sanctioned list holds. Sources: `01-deps.md` batches 5–6 (Fumadocs migration notes: Orama → the new search client, `hotKey: false`, Tabs `forceMount`, typescript cache, the `<main>` landmark change; lucide renames incl. `Trash` → `Trash2`; axe 4.13 new rules; Playwright 1.63 removes `Locator.ariaRef()`; recharts 3.10 Legend props); issue `issues/19-d2-deps-fumadocs-lucide.md`. Files: `apps/docs/**` (search, layouts, `source.config.ts`, `mdx.tsx` — Do1-a's export pipeline must keep working: `verify-docs-export` is your regression guard), every registry file importing a renamed lucide icon, `tooling/mirror-animated-icons.mjs` (`--refresh` against upstream lucide-animated is a separate MK decision — do NOT adopt the 28 new icons; regenerate the 439 modules through the factory generator), `tooling/lib/gate-receipt.mjs` (Playwright pin), `packages/ui/vitest*.config.ts`, `chart.tsx`, `pnpm-lock.yaml`. Read the official release notes for each exact target version (`query-docs`/`resolve-library-id`, `WebFetch`); record URLs.

## Scope

1. Fumadocs 16.15.8 family (`fumadocs-ui`, `fumadocs-core`, `fumadocs-mdx` 15.4, `fumadocs-typescript` 5.4, `@fumadocs/story` 1.3; twoslash stays 3.3.1): search rewrite, `hotKey: false` where the docs own ⌘K, Tabs `forceMount` audit, clear `.next/fumadocs-typescript` cache, verify the duplicate-`main` interaction with N1's `landmark` prop, `verify-public-api-docs` rerun, the agent export (`verify-docs-export`) still clean, `design-lint --docs-shell --emitted-css` still clean.
2. lucide-react 1.41: rename sweep; regenerate the animated-icon data modules through the I1 generator (`node tooling/mirror-animated-icons.mjs` without `--refresh`), `verify-animated-icons` green (15/15 self-test).
3. axe-core 4.13: triage new rules across routes (the one remote sweep + the unit axe suite); fix or document each new violation in `operator-review.md`.
4. Playwright 1.63: receipt pin updated; the three-engine suite runs on the boxes (`pnpm --filter @vegastack/ui test:all-browsers` — this is the ONE extra browser run this batch may take, on a box).
5. recharts 3.10: Legend prop migration; chart fixtures re-captured in the end round.
6. Changelog `📦`; changesets (`@vegastack/ui` minor if registry items change; `@vegastack/design`/`design-tokens` if their output changes); ledgers (axe triage; behaviour changes).

Do NOT touch: component behaviour beyond renames/API migrations; `design.md` doctrine (unless a dependency changes a documented behaviour — then say so); docs page bodies (Do1-b). Acceptance: `pnpm lint`, `pnpm typecheck`, unit + smoke + three-engine green (remote), `registry:build`/`design:derived` idempotent, both `SITE_VISIBILITY` builds, search works in the static export (manual check via the built `out/` + the existing search test). PR title: `D2 · Dependencies: Fumadocs 16.15, lucide 1.41, axe 4.13, Playwright 1.63, recharts 3.10 (#50)`, `Closes #50`.
