# T1 · Tables and grids (#37) — `audit/t1-tables-grids`

Worktree: `/Users/mk/projects/vegastack-design/.claude/worktrees/agent-a5ece1ab2a22fb18b`. Based on `origin/main` @ 9c33dfaf (F1 merged). F2 (#33, `variant × tone`, `size: default → md` across 26 files, `IconButton shape`) is in flight: name any size you add `md`; rebase onto `origin/main` before the PR (plain rebase — your base is main).

Decisions FINAL: D18 (cells wrap by default, `nowrap` opt-in per column, numeric/mono stay nowrap), TD-4 (scroll regions keep tabIndex with an inset ring). Sources: `02-batch-05-data.md` (B5-01, B5-02, B5-04, B5-07, B5-09…B5-13), `07-state-probe.md` SP-03 (cells), SP-05 (chart), `04-cross-cutting.md` §1 (`data-table-parts`), issue `issues/06-t1-tables-grids.md`. Files: `packages/ui/registry/ui/{table,data-list,data-grid,property-list,chart,comparison-matrix,truncated-text,terminal}.tsx` + tests/previews/MDX, `component-contracts.json`, `apps/docs/vrt/contracts.spec.ts`, `tooling/lib/route-scope.mjs`. Docs to read: TanStack Table v8 (not v9), TanStack Virtual 3.14, recharts 3.9, Tailwind 4.3 (`@container`, `wrap-anywhere`), Base UI installed version.

## Scope (all of it)

1. Table container: `role="region"`, `tabIndex=0` ONLY when scrollable (measure on mount + resize; share the `useOverflow` hook from `truncated-text.tsx` — extract to a hook file if needed), `aria-label` from the caption or a prop, inset focus ring (`focus-visible:-outline-offset-2`). DataList/DataGrid inherit; ComparisonMatrix and Terminal align to the shared hook.
2. Default wrap (`overflow-wrap: anywhere` on body cells, min column widths); `nowrap` per column via `DataListColumn.nowrap` / `DataGridColumn.nowrap`, default `true` for `align="end"` numeric and `mono` columns; delete the blanket `whitespace-nowrap`.
3. `data-table-parts` registry item (consumers who `shadcn add` data-list/data-grid must get it via `registryDependencies`; `verify-registry-deps` fails closed): `SortHeaderButton`, `SelectionCell`, `SelectAllHead`, `SkeletonRows`, `EmptyRow`, `useRowSelection`, `alignClass` — used by both; DataList stays presentational, DataGrid keeps its engines. Sort affordances use system `IconButton`/`Button`, not raw `<button>` (adjust `RAW_INTERACTIVE_EXEMPTIONS` counts in `tooling/design-lint.mjs` — fail closed both ways).
4. DataGrid `mobile` default `"merge"` + "N columns hidden" toolbar hint whenever revelation dropped anything; `columnPicker` prop (default `true`) in the toolbar trailing slot.
5. Header rows do not take the row hover; keep `containerProps` only; PropertyList label track `minmax(calc(var(--spacing)*20),max-content)` + `@container` stack below `@xs`; chart ticks `text-sm`, numerals `text-code-sm`; chart container `[&_svg:focus-visible]` inset outline (SP-05); `data-grid-cell` focus ring inset (SP-03). Fix the select-all/row-selection checkbox cell's 24px target floor (Do1-a recorded it in `bugs.md`) — the contract lane probes `::before` hit areas; axe `target-size` alone is not the arbiter.
6. Contract lane: wide-table (keyboard scroll), long-cell (wrap), 320px DataGrid no-data-loss fixtures → previews + contract records → `pnpm design:derived`.
7. Doctrine `design.md` §Tables, §DataGrid (amendments #10/#11) + `pnpm design:sync`; MDX pages for touched components (content only); skills if they name table behaviour; changelog (`🧩 data-table-parts`, `🔧`, `⚠️` DataGrid `mobile` default); `@vegastack/ui` minor changeset; ledgers.

Do NOT touch: F2's `button/icon-button/split-button`, other components' size maps; D1 deps; `tooling/**` gates (except `RAW_INTERACTIVE_EXEMPTIONS`); docs chrome/`mdx.tsx`; icons; T2's Chip/`use-announcer`/pagination (DataGrid's hand-rolled announcer stays); Di1 owns `"use client"`/`motion-reduce:` deletions and exposes a TruncatedText `focusable` context — consume it only if Di1 has merged; Mk1's dashboard block beyond what your API change forces.

Acceptance: axe no `scrollable-region-focusable` on table/data-list/data-grid/dashboard-01; `wc -l data-list.tsx data-grid.tsx` down ≥300 combined; 320px `dataGridEditable` capture shows merge or hint (read the PNG); `probe-states.mjs --routes table,data-list,data-grid,chart,property-list` light + `--dark` → 0 `focus-clipped`, 0 `focus-none` on svg; `pnpm gates:component table data-list data-grid property-list chart comparison-matrix data-table-parts`. PR title: `T1 · Tables and grids: focusable region, wrap default, shared parts, honest mobile (#37)`, `Closes #37`.
