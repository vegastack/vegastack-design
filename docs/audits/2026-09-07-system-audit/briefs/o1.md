# O1 · Overlays (#40) — `audit/o1-overlays` (XL)

Worktree: `/Users/mk/projects/vegastack-design/.claude/worktrees/agent-a17ed5bd58558f638`. Based on F2's UNMERGED head `2ac84d39`. Before the PR: wait for F2 to merge, then `git rebase --onto origin/main 2ac84d39` (or `--onto origin/main <your first commit>^`). Never open a PR containing F2's commits. Check whether D1 (#34, Base UI 1.8) has merged — the Drawer/Toolbar deltas matter; write against the INSTALLED Base UI version and note what changes at 1.8.

Decisions FINAL: D11 (timings measured from Vercel/Linear — values in `02-batch-03-overlays.md`'s D11 row), D12 (Popover/Select stay modal; scroll lock intended), D14 (24px dialog/alert-dialog/sheet; 16px popover/hover-card/toast), D15 (Sheet gets `size` AND moves to Base UI Drawer). Sources: `02-batch-03-overlays.md` in full (B3-01…B3-13), `02-batch-08` B8-04, `02-batch-09` B9-11, `07-state-probe.md` SP-02 (Command), `04-cross-cutting.md` §1, issue `issues/09-o1-overlays.md`. Files: `packages/ui/registry/ui/{popover,dropdown-menu,context-menu,select,combobox,hover-card,tooltip,navigation-menu,dialog,alert-dialog,sheet,command,emoji-picker,shortcut-overlay,sidebar}.tsx` + tests/previews/MDX, `packages/design/src/index.ts` (`FLOATING`, `TIMINGS`), `tooling/verify-portal-theme-scope.mjs` (12/12 inventory, fails closed — every portal keeps `useInternalThemeScope()`), `component-contracts.json`, `probe-overlays.mjs`. Docs: Base UI positioner/popup anatomy, `data-starting-style`, Drawer (swipe, snap points, virtual keyboard), Dialog.

## Scope (all of it)

1. `floating-surface` (decide: registry `lib` item declared in each overlay's `registryDependencies`, or a `@vegastack/design` export — `verify-registry-deps`/`verify-shadcn-consume` decide): `FloatingPositioner`/`FloatingPopup` over Base UI's parts with shared classes, `FLOATING` constants, D11 enter/exit motion classes (if D11 needs a duration the 3-step scale lacks, that is a token change + `design-tokens` changeset + `design.md` §Motion), the arrow; all eight overlays compose it. `context-menu` re-exports dropdown-menu's item parts with `ContextMenu.Root` (< 150 lines).
2. One `menuItemVariants` recipe for menu/select/combobox/command items (`surfaceInteractive`-based hover `surface-2`, `surface-3` active/selected, 4px inset floor: list `p-1`, item `rounded-md`).
3. Padding/width: `p-6` dialog/alert-dialog/sheet, `p-4` popover/hover-card (toast is O2's); widths from `--panel-width-*`; `--layout-overlay-max-height` replaces every `100dvh` calc; `DialogContent size: sm | md | lg | full`; Command sizes through the same prop.
4. `AlertDialogContent intent` drives the confirm button `tone` (F2) or is deleted — decide, record; Tooltip drops explicit `role`; `Popover`/`Select` document the scroll lock (D12) in JSDoc + MDX.
5. Sheet → Base UI `Drawer` (swipe, snap points, virtual keyboard) with `size: sm | md | lg | full`; the mobile Sidebar sheet follows (edit `sidebar.tsx` ONLY where it composes Sheet — N1 owns the rest).
6. `panelSearch` recipe (leading `Search` icon, no box, hairline below, `h-(--size-md)`) used by Command, Combobox panels, EmojiPicker, ShortcutOverlay.
7. Docs per B3-13 (content only); doctrine `design.md` §Overlays (amendment #7) + `pnpm design:sync`; `component` skill §2 motion row if the reference pattern changes; public skill overlay guidance; changelog (`🧩 floating-surface`, `🔧`, `⚠️` Sheet on Drawer, `🗑`); changesets; ledgers.

Do NOT touch: F2's button files; Fo1 form leaves (Fo1 owns Select trigger/Combobox input chrome — keep your `select.tsx`/`combobox.tsx` edits to the floating parts); M1 media; Di1's directive/`motion-reduce:` deletions and the two `Kbd` spots (`CommandShortcut`, `TooltipKbd`); T1/T2 tables/chips/pagination/announcer; O2 (`sonner.tsx`, toast, provider); N1 (sidebar except Sheet composition, tabs, breadcrumb, page-header, scroll-area); D1 deps; `tooling/**` (except `verify-portal-theme-scope.mjs` inventory and `OUTLINE_NONE_EXEMPT` if forced); docs chrome.

Acceptance: `grep -l "Positioner" packages/ui/registry/ui/*.tsx` → only `floating-surface` + thin wrappers; `wc -l context-menu.tsx` < 150; `probe-overlays.mjs` → every overlay's computed duration/easing equals D11 (quote the table); `probe-states.mjs --routes command,dropdown-menu,select,combobox,popover,context-menu,navigation-menu,hover-card,tooltip,dialog,alert-dialog,sheet,emoji-picker,shortcut-overlay` light + `--dark` → 0 `hover-touches-border`; Sheet swipe-to-close in touch smoke; `node tooling/verify-portal-theme-scope.mjs`; `pnpm gates:component` per overlay; `test:smoke`; `verify-shadcn-consume` (lib item installs with one overlay). PR title: `O1 · Overlays: floating-surface, D11 timings, Sheet on Drawer, panel-search (#40)`, `Closes #40`.
