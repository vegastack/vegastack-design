# T2 · One Chip primitive, one announcer hook, pagination landmark, filter fixes (#38) — `audit/t2-chips-announcer`

Base: F2's UNMERGED committed head `eb4abb05ab7dee83a2b27543a3b04ba45a0af900` (a local commit; 13 commits ahead of main incl. F2's rebase): `git fetch origin && git checkout -B audit/t2-chips-announcer eb4abb05ab7dee83a2b27543a3b04ba45a0af900`; `pnpm install --frozen-lockfile`. Before the PR: wait for F2 to merge (poll `git fetch && git log origin/main --oneline -5` every 2–3 min), then replay ONLY your commits with `git rebase --onto origin/main <your first commit>^` (F2 is squash-merged). Never open a PR containing F2's commits.

Decisions FINAL: D23 (`status` by default), the Chip primitive (`04-cross-cutting.md` §1), D1–D15 as recorded. Sources: `02-batch-05-data.md` B5-03, B5-05, B5-06, B5-08, B5-14; `04-cross-cutting.md` §1 (announcer copies) and §6; issue `issues/07-t2-chips-announcer-pagination.md`; `07-state-probe.md`. Files: `packages/ui/registry/ui/{tag-group,filter-bar,filter-bar-managed,combobox,chip-input,pagination,editable-cell,data-grid,comparison-matrix,copy-button,badge}.tsx`, `use-drag-reorder.ts`, `use-file-drop.ts` + tests/previews/MDX, `component-contracts.json`, `apps/docs/vrt/contracts.spec.ts` (target-size probe). Docs: Base UI (installed version) Combobox chips, `useRender`; WCAG 2.5.8; Tailwind 4.3.

## Scope (all of it)

1. `chip.tsx` registry item `chip`: `hue`, `size: sm | md`, `active`, `onRemove` → a real 24px remove `IconButton size="xs"` (hover + pressed via the F1 recipes — the fix-round item: `ComboboxChipRemove` and FilterChip's selected remove had no pressed step); `Tag`, `FilterChip`, `ComboboxChip`, ChipInput chips compose it via `render`; `Badge bordered` stays a badge. Contract lane: target-size probe on the `comboboxMultiple` fixture.
2. `use-announcer.ts` registry hook: `const { announce, Announcer } = useAnnouncer()`; the five copies use it (editable-cell, chip-input, data-grid, use-drag-reorder, use-file-drop; CopyButton too). One `role="status" aria-live="polite" aria-atomic` node per component.
3. Pagination: `<nav aria-label={label ?? "Pagination"}>` with no explicit role; docs name each instance; `PaginationPager` composes `IconButton variant="ghost" size="sm"` (fix-round: `pagination.tsx:102` hand-wrote the hover/active recipe — use the recipes).
4. ComparisonMatrix promoted column → `surface-2`/`primary` alpha (F1 already removed `info`; verify and finish); fix the FilterBar JSDoc default; previews/tests for property-list, comparison-matrix, tag-group per B5-14.
5. Doctrine `design.md` §Chips (one primitive, remove control 24px), §Live regions (one hook; amendment #8) + `pnpm design:sync`; docs pages (content only); skills (`component` skill §5 live-regions paragraph → the hook; public skill) → sync + skill-lint; changelog (`🧩 chip`, `🧩 use-announcer`, `🔧`, `⚠️` Tag/FilterChip internals); `@vegastack/ui` minor changeset; ledgers.

Do NOT touch: T1's `data-table-parts`/table internals beyond consuming them (DataGrid: only the announcer swap + chip usage); Fo1's form leaves (ChipInput: only its chip + announcer); O1's combobox floating parts (you own `ComboboxChip*` only); N1's `selectedChip` recipe (Tabs/Segmented/Toggle); Di1's Badge; Mk1's `use-media-query`/`mergeRefs`; `tooling/**` gates (except `RAW_INTERACTIVE_EXEMPTIONS` counts); docs chrome.

Acceptance: `grep -rn "seq: prev.seq + 1" packages/ui/registry/ui` → 0; `role="status"` ≤ 1 per announcing component; axe on the pagination page: no `landmark-unique`; contract-lane target-size check passes on combobox chips; `probe-states.mjs --routes tag-group,filter-bar,combobox,chip-input,pagination` (remote) → hover AND pressed on every chip and pager control; `pnpm gates:component chip tag-group filter-bar filter-bar-managed combobox chip-input pagination editable-cell data-grid comparison-matrix` (remote). PR title: `T2 · Chip primitive, use-announcer, pagination landmark, filter fixes (#38)`, `Closes #38`.
