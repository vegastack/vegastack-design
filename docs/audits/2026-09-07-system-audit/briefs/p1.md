# P1 · Pickers and drag: DatePicker layout, shared searchable select + geo-data, drag-item recipe, Board/Dropzone fixes (#45) — `audit/p1-pickers-drag`

Base: O1's UNMERGED committed head `01cfba888a92b6dbfc056c8f87a316a24b1ef0a4` (local; on F2's head; has `panelSearch` and `floating-surface`). `git fetch origin && git checkout -B audit/p1-pickers-drag 01cfba888a92b6dbfc056c8f87a316a24b1ef0a4`; `pnpm install --frozen-lockfile`. Before the PR: wait for F2 AND O1 to merge, then `git rebase --onto origin/main <your first commit>^`. Never open a PR containing F2's/O1's commits. Also rebase across T2 (#38, `combobox` chips) and M2 (#36, ActionBar) if they land first.

Decisions FINAL: D25 (react-day-picker sanctioned — AGENTS.md text lands in D1; do not edit AGENTS.md), D27 (shared searchable-select + `geo-data` lib item). Sources: `02-batch-08-pickers-drag.md` B8-02, B8-03, B8-05, B8-06, B8-07, B8-12; issue `issues/14-p1-pickers-drag.md`. Fix-round item (Codex/F1): `date-picker.tsx:317` selected day keeps `bg-primary` on hover with no pressed rung → use F2's `solid` recipe incl. `fillInteractive.primary`. Files: `packages/ui/registry/ui/{date-picker,country-select,region-select,region-select-data,board,sortable-list,dropzone,emoji-picker,combobox}.tsx`, `use-drag-reorder.ts`, `use-file-drop.ts` (+ new `searchable-select.tsx`, `geo-data.ts`, `drag-item.tsx`), `registry.json` (new `registry:lib` type for `geo-data` — `tooling/verify-registry-deps.mjs` must cover it; that is a tooling edit you may make, minimal), `component-contracts.json`, tests/previews/MDX. Docs: react-day-picker v10 (installed — check), Base UI Combobox `value`/`onValueChange` (installed version), Pragmatic DnD 2.x, react-dropzone 19.

## Scope (all of it)

1. DatePicker: triggers `w-full` (consumers constrain via parent; docs updated), caption as `grid-cols-[auto_1fr_auto]` (no padding hack), day buttons `icon-md`, selected day on F2's `solid` recipe with a pressed rung; fix the stale "v9" comment if D1 has not already (check `origin/main` and D1's branch — do not duplicate).
2. `searchable-select.tsx` registry item: Combobox preset with the Select-shaped trigger, O1's `panelSearch` input, check on the selected row, `w-(--anchor-width)` panel, optional clear `X` (an `IconButton`); selection via Base UI `value`/`onValueChange` only. `CountrySelect`/`RegionSelect` become thin wrappers.
3. `geo-data` registry item (`registry:lib`): `COUNTRIES`, `REGIONS`, `getCountryByCode`, `getRegions(country)`; both selects declare it; datasets removed from component files. `wc -c apps/docs/public/r/region-select.json` < 8 KB.
4. `drag-item.tsx` next to `use-drag-reorder`: exported `dragItemClasses` (or a `DragItem` slot) with drop-edge hairlines, lift dim, pending pulse; Board and SortableList consume it.
5. Board: `cursor-grab` only when `!pointerDisabled && !readOnly`; hover via F1 recipes; `columnMaxHeight` prop (CSS length; default from `--layout-overlay-max-height`).
6. Dropzone: the surface carries `data-dragging:outline-2 outline-primary/(--alpha-…)` (destructive for invalid) inside its `rounded-lg`; `Empty` reads it via `group-data-dragging/dropzone` (Di1's Empty API may have changed — check main/Di1's head `variant: plain | card | dashed`).
7. Fixtures: Board keyboard move + locked lane; SortableList menu path; Dropzone dragging state (static `data-dragging` prop for the lane); 320 px range picker fits.
8. Doctrine `design.md` §Form controls (no fixed-width controls), §Drag (one item recipe) + `pnpm design:sync`; docs pages (content only); skills; changelog (`🧩 searchable-select`, `🧩 geo-data`, `🔧`, `⚠️` Country/Region internals); `@vegastack/ui` minor changeset; ledgers.

Do NOT touch: O1's overlay parts beyond consuming them; T2's chip/announcer; N1's board viewport focus (N1 owns scroll-region focus — you own Board's cursor/hover/height); Mk1; `tooling/**` beyond `verify-registry-deps.mjs` for the new type; docs chrome.

Acceptance: region-select.json < 8 KB and `geo-data.json` carries the data once; keyboard Enter and pointer click select through one code path (unit test); one remote `gates:push`; `verify-shadcn-consume` covers the lib item. PR title: `P1 · Pickers and drag: DatePicker layout, searchable-select + geo-data, drag-item recipe, Board/Dropzone (#45)`, `Closes #45`.
