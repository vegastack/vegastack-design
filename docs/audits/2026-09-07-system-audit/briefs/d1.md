# D1 · Dependencies (#34) — `audit/d1-deps-security-alignment`

Worktree: `/Users/mk/projects/vegastack-design/.claude/worktrees/agent-a5fd49c9bbd42c51d`. Based on `origin/main` @ 6f11a4bc; main has since gained F1 (9c33dfaf) — rebase onto `origin/main` before the PR (`pnpm-lock.yaml` may conflict with I1's `motion` peer addition once #57 merges — resolve by re-running `pnpm install`, then commit the lockfile).

State: 7 commits (steps 1–3 + step-4 work + ledger entry `8162c046 docs(ledger): record the 15 Base UI results, four decisions…`), 3 uncommitted files, and possibly an orphaned `gates:push`/contract sweep from the previous agent (wait for it). Steps done: 1 security (vitest 4.1.11, tiptap 3.31.3, postcss 8.5.28, style-dictionary 5.5.2 — held there deliberately, 5.5.3 changes token CSS in F1's territory); 2 alignment + explicit strict `minimumReleaseAge` (TD-5 by measurement); 3 Next 16.3.4, two default flags removed, `agentRules: false`; 4 Base UI 1.8.0 + shadcn 4.21.0 + `@shadcn/react` 0.3.1 applied and the 15 verification points recorded. Decisions FINAL: D25 (react-day-picker sanctioned), D30 (next-themes sanctioned), TD-5. Sources: `01-deps.md` (batches 1–4, §Base UI, §shadcn, §Next), issue `issues/03-d1-deps-security-alignment.md`, `06-tooling-gates.md` TG-06.

## Remaining

- Verify steps 4–5 are complete: AGENTS.md §Sanctioned dependency exceptions lists `react-day-picker` (5th headless primitive, behind `date-picker`; "Exactly four… a fifth entry" rewritten to five + the rule for a sixth) and `next-themes` (theme engine, behind `provider`), MK sign-off 2026-09-07, D25/D30; `date-picker.tsx:30` stale "v9" comment fixed (confirm installed major); `design.md` amendment #13 line + `pnpm design:sync`; skills quoting the sanctioned list → `sync-package-skills` → `skill-lint`; `verify-shadcn-consume` passes with shadcn 4.21; `verify-shadcn-base` pins updated via its sanctioned path; MessageScroller `data-pending-scroll` handled; the shadcn `cn` decision recorded (locked: `cn()` from `@vegastack/design`); no `package.json` carries a top-level `registries` key.
- Changelog `📦`/`🛠` bullets under 0.7.0 + sync + lint; changesets (`@vegastack/ui` minor if registry item ranges changed; `@vegastack/design`/`design-tokens` if published output changed); `docs/ledger/bugs.md` (prosemirror duplication root cause) — check the ledger entry already committed covers everything.
- `pnpm audit` (0 critical/high — quote 47 → N); `pnpm lint`; `pnpm typecheck`; `pnpm --filter @vegastack/ui test`; `test:smoke`; `registry:build`/`design:derived` idempotent; `design:sync:check`; both docs builds allowed here (Next bump) — but only once each; `pnpm gates:push`; commit with receipt; rebase; push; PR.

Do NOT touch: F1/F2 component sources beyond what a Base UI delta forces; `tooling/**` gates (except `verify-shadcn-base.mjs` pins); docs chrome; AGENTS.md sections other than §Sanctioned dependency exceptions. PR title: `D1 · Dependencies: security, alignment, Next 16.3, Base UI 1.8 + shadcn 4.21 (#34)`, `Closes #34`. Body: per-package before → after table, the 15 Base UI results, decisions, docs URLs + versions, evidence, left-out items (D2/D3 scope).
