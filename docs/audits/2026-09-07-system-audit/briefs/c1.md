# C1 · Composition and feedback: Item role and type, polite alerts, NotificationBell replay, Empty variants (#44) — `audit/c1-composition-feedback`

Base: F2's UNMERGED committed head `eb4abb05ab7dee83a2b27543a3b04ba45a0af900` (a local commit; 13 commits ahead of main incl. F2's rebase): `git fetch origin && git checkout -B audit/c1-composition-feedback eb4abb05ab7dee83a2b27543a3b04ba45a0af900`; `pnpm install --frozen-lockfile`. Before the PR: wait for F2 to merge (poll `git fetch && git log origin/main --oneline -5` every 2–3 min), then replay ONLY your commits with `git rebase --onto origin/main <your first commit>^` (F2 is squash-merged). Never open a PR containing F2's commits.

Decisions FINAL: D23 (`status` by default; `alert` only for destructive/warning rendered after mount), D24 (Item title 14/500, description 12; `size="sm"` keeps 12/12). Sources: `02-batch-07-composition-feedback.md` B7-01…B7-10; issue `issues/13-c1-composition-feedback.md`. Files: `packages/ui/registry/ui/{item,timeline,alert,announcement-banner,notification-bell,onboarding-checklist,empty,collapsible,accordion,stepper,progress-indicator,sortable-list}.tsx` + tests/previews/MDX, `use-animation-replay.ts`, `component-contracts.json`. Docs: Base UI Collapsible/Accordion (installed version), WAI-ARIA live regions, React 19.

## Scope (all of it)

1. `ItemGroup` provides context; `Item` sets `role="listitem"` only inside it (or an explicit `role`); remove the Timeline workaround. axe: 0 `aria-required-parent` across item, timeline, sortable-list routes.
2. Alert: `role="status"` for `default|info|success`; `role="alert"` for `destructive|warning` ONLY when `live` (a `live` prop like `AttachmentDescription`); no live role for static content. AnnouncementBanner: no live role at load; `live` prop for runtime banners.
3. NotificationBell: `previousCount` in state; when `count` increases after mount set an `animate` flag cleared on `animationend` via `useAnimationReplay`; delete the key-remount trick. Test: pops exactly once when count increases, never on mount.
4. OnboardingChecklist composes `ProgressIndicator segments`; dismiss/collapse buttons → `IconButton variant="ghost" size="xs"` (F2 converted Alert/Banner/Onboarding dismiss controls — verify and finish).
5. `ItemTitle` `text-label` (14/500), `ItemDescription` `text-sm`; `size="sm"` 12/12.
6. `Empty variant: plain | card | dashed` replaces `bordered` + `surface`; `EmptyTitle` accepts `render`. Disclosure triggers (Collapsible, Accordion) hover with `surfaceInteractive`, no underline (F1 gave them an ink pressed step against the hairline — reconcile with §Hover geometry: a wash needs the 4px inset; choose and record). Stepper's navigable label uses `Button variant="link" tone="neutral"`.
7. Docs: move `alert.mdx` "Strip variant" and `empty.mdx` "Illustration & value tiers" into Examples; fixtures/tests per B7-10 (announcement-banner, onboarding-checklist, card, timeline). Fix-round item (Do1-a): the `timeline` hero fixture fails the 24px target floor — fix it here.
8. Doctrine `design.md` §Live regions (D23; amendment #8), §Lists (row type 14/500 + 12) + `pnpm design:sync`; skills (`component` skill §5 live-region policy); changelog (`🔧`, `⚠️` Empty API); `@vegastack/ui` minor changeset; ledgers.

Do NOT touch: T2's announcer hook internals; N1's tabs/sidebar/scroll; O1's overlays; Fo1's `field`/`field-inline`; Mk1's marketing leaves; `tooling/**` gates (except allowlist counts); docs chrome.

Acceptance: axe 0 `aria-required-parent` on item/timeline/sortable-list; unit tests: Alert static → `status`, destructive `live` → `alert`; NotificationBell pops once; `grep -rn "hover:underline" packages/ui/registry/ui` → only `link`-role components; `pnpm gates:component item timeline alert announcement-banner notification-bell onboarding-checklist empty collapsible accordion stepper` (remote); `probe-states.mjs --routes item,alert,empty,collapsible,accordion,stepper,timeline` light + `--dark` (remote). PR title: `C1 · Composition and feedback: Item role/type, polite alerts, NotificationBell replay, Empty variants (#44)`, `Closes #44`.
