# O2 · Toasts on Base UI Toast (#41) — `audit/o2-toasts`

Base: O1's UNMERGED committed head `01cfba888a92b6dbfc056c8f87a316a24b1ef0a4` (local; it sits on F2's head; has `floating-surface`, D11 motion classes, `panelSearch`, Sheet on Drawer). `git fetch origin && git checkout -B audit/o2-toasts 01cfba888a92b6dbfc056c8f87a316a24b1ef0a4`; `pnpm install --frozen-lockfile`. Before the PR: wait for BOTH F2 and O1 to merge (poll `git log origin/main --oneline -8`), then `git rebase --onto origin/main <your first commit>^`. Never open a PR containing F2's or O1's commits. Check the installed Base UI version (`packages/ui/package.json`): D1 (#34) moves it to 1.8 — if D1 has merged by the time you rebase, re-verify Toast against 1.8 docs; write against the version installed in your worktree and note the delta.

Decisions FINAL: D13 (migrate to Base UI Toast, keep our style, support every Base UI Toast feature), D14 (16 px toast padding), D11 timings (via O1's motion classes), D23 (live-region policy). Sources: `02-batch-03-overlays.md` B3-07; `02-batch-09` B9-02 (provider mounts sonner; tooltip delays have no home); issue `issues/10-o2-toasts-base-ui.md`; https://base-ui.com/react/components/toast (read the INSTALLED version's docs). Files: `packages/ui/registry/ui/{sonner,provider,tooltip}.tsx` (+ new `toast.tsx`), `packages/ui/src/**` provider mirror (the private package's Toaster mirror — `tooling/sync-toaster-mirror.mjs`), `registry.json`, `component-contracts.json`, tests/previews/MDX, `tooling/verify-toaster-mirror`/`sync-toaster-mirror.mjs`, `tooling/verify-portal-theme-scope.mjs` (Sonner host is in its inventory — update the reviewed inventory, it fails closed), `design-lint.mjs` (sonner z-index exception → delete), `packages/design-tokens` `z-overlay` description mentions sonner.

## Scope (all of it)

1. New registry item `toast` on Base UI Toast: `ToastProvider` (limit, timeout, swipe directions), `ToastViewport`, `Toast` with `title`, `description`, `action`, `close`, `type: default | success | warning | destructive | info`, promise toasts, custom `render`, stacking/expand on hover, keyboard (F6 / Escape), `useToastManager` re-exported as `useToast`/`toast()`.
2. Styling: `surface-2` on `card`, alpha border, 16 px padding, `shadow-overlay`, D11 timings via O1's motion classes (`motion-dock`-style enter/exit if M2's utility exists on your base — it will not; use O1's floating enter/exit classes and say so), tone strip via `StatusIcon`; standard focus ring; `useInternalThemeScope()` on the viewport (portal theme-scope inventory).
3. Provider: mount Base UI `ToastProvider` + viewport; `toaster` prop keeps its shape (`boolean | ReactNode`); set `Tooltip.Provider` `delay`/`closeDelay` from `TIMINGS` here.
4. Remove `sonner` everywhere: registry item deps, `packages/ui/package.json` (this IS a dependency removal — allowed for this batch; run `pnpm install`, commit the lockfile), provider `registryDependencies`, the toaster-mirror gate (repoint at the new item or delete it and its `pnpm lint` wiring — decide, record), the design-lint sonner z-index exception, the `z-overlay` token description, `design.md`/skills mentions of sonner. `grep -rn "sonner" packages apps --include=*.ts --include=*.tsx --include=*.json --include=*.mdx --include=*.md` → 0 (historical ledger/changelog mentions excepted).
5. Docs: `toast.mdx` rewritten (all types, promise, action, stacking, RTL, reduced motion; content only — Do1-b restructures); contract fixtures for each type; changelog `⚠️` (API change) + `🗑` (sonner) + `🧩 toast`; changesets (`@vegastack/ui` minor; `@vegastack/design` if `TIMINGS` moves).
6. Doctrine `design.md` §Feedback — toasts on Base UI Toast; live-region policy (D23) applies; `component` skill §2 "Toast motion is sonner-owned" sentence and §1 z-index "Sonner is the ONE documented exception" → rewritten; `tokens.md` z-index note; public skill toast guidance → sync + skill-lint; ledgers.

Do NOT touch: O1's overlays beyond consuming `floating-surface`; Fo1/T1/T2/N1/C1/M1/M2/P1/Mk1 files; `tooling/**` beyond the toaster mirror, portal inventory and the sonner lint exception; docs chrome.

Acceptance: sonner grep → 0; toast fixtures pass the contract lane (one remote `gates:push` — it is global because `provider` changes); Escape dismisses, F6 reaches the viewport (unit tests); axe clean; `node tooling/verify-portal-theme-scope.mjs`; `node tooling/verify-shadcn-consume.mjs` passes with the new item. PR title: `O2 · Toasts on Base UI Toast, sonner removed (#41)`, `Closes #41`.
