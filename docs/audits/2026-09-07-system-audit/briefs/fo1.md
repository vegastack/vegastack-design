# Fo1 · Forms (#39) — `audit/fo1-forms`

Worktree: `/Users/mk/projects/vegastack-design/.claude/worktrees/agent-adb77e930818ebe32`. Based on F2's UNMERGED head `2ac84d39`. Before the PR: wait for F2 to merge, then `git rebase --onto origin/main 2ac84d39` (or `--onto origin/main <your first commit>^`). Never open a PR containing F2's commits. If O1 (#40) merges first, rebase across it too (O1 owns `select.tsx`/`combobox.tsx` floating parts; you own the trigger/input `fieldControl` classes + hover — keep your edits in those two files confined to the trigger/input class strings).

Decisions FINAL: D3 (labels 12px — doctrine edit), D4 (helper text below), D5 (`Field` owns the invalid shake), D6 (remove `selection:*`), D7 (disabled keeps pointer events), D23 (`status` role for inline errors). Sources: `02-batch-01-form-leaves.md` (B1-01, B1-06, B1-07, B1-11…B1-14, B1-16…B1-18), `02-batch-08` B8-08/B8-09, `02-batch-09` B9-06, `07-state-probe.md` SP-02 (NumberField), SP-04 (Select/Checkbox hover), issue `issues/08-fo1-forms.md`. Files: `packages/ui/registry/ui/{field,input,textarea,number-field,otp-input,select,combobox,label,checkbox,radio-group,switch,field-inline,editable-cell,password-input,auto-save-input,chip-input,text-edit,spinner}.tsx` + tests/previews/MDX, `packages/design/src/index.ts` (F1's recipes — add `fieldControl` beside them), `packages/design-tokens/src/base.css` (F1's forced-colours fallback — extend, do not duplicate), `use-animation-replay.ts`. Docs: Base UI Field/CheckboxGroup/NumberField/Select/Input (installed version), Tailwind 4.3 `forced-colors:`/`user-invalid:`.

## Scope (all of it)

1. `fieldControl` recipe in `@vegastack/design` for Input, Textarea, NumberField, OTP, Select trigger, Combobox input: border, focus tint, invalid, disabled; forced-colours outline stays in `base.css`. Remove every `selection:*`.
2. `Field`: helper/description BELOW the control, error below that; `role="status"`; owns `useShakeOnInvalid` (remove per-component wiring in ChipInput, NumberField, …).
3. `Label`: `inline-flex`/`inline` by default; block variant kept.
4. `CheckboxGroup` registry item on Base UI `CheckboxGroup` (parent/indeterminate) — the eight files per the `component` skill §6 (+ `meta.json`, barrel, contract record).
5. `use-inline-edit.ts` registry hook (draft, commit, cancel, focus restore, double-commit guard) used by `FieldInline` and `EditableCell`; FieldInline hover → `surfaceInteractive`, error role → `status`.
6. PasswordInput toggle → `IconButton variant="ghost" size="xs"` (check F2's head — build on it), no `motion-pop-in`, no `hasToggledRef`; trailing-space class strings fixed. AutoSaveInput: `Spinner decorative` prop (add to Spinner if missing), `size-(--icon-default)`, drop the no-op wrapper.
7. NumberField segments: inner radius derived from the field (`rounded-[inherit]`-style) or `overflow-hidden` + inset ring so ± hover fills don't touch the border (SP-02); Select trigger hover border tint in BOTH themes; Checkbox/Radio/Switch unchecked AND checked hover border tint.
8. Tests/docs per B1-17/B1-18 (RTL prefix/suffix, error+disabled combos, group keyboard). Doctrine `design.md` §Forms + §Focus (amendments #4/#5) + `pnpm design:sync`; docs pages (content only); skills; changelog (`🧩 checkbox-group`, `🧩 use-inline-edit`, `🔧`, `⚠️` helper placement); changesets (`@vegastack/ui` minor, `@vegastack/design` minor); ledgers.

Do NOT touch: F2's button files; M1 (`slider`, `audio/video-player`, `attachment` — M1 owns AttachmentTrigger focus, `image`); O1 overlay floating parts, `command`, `dialog`, `sheet`, `popover`, menus, `tooltip`, `hover-card`, `navigation-menu`, `emoji-picker`, `shortcut-overlay`; Di1's directive/`motion-reduce:` deletions (leave directives alone even in your files), `progress/badge/kbd/status-icon/skeleton/truncated-text/relative-time`; T1 tables (EditableCell is yours — keep its API stable for DataGrid); T2's Chip primitive and `use-announcer` (ChipInput's chip + announcer stay; only remove its shake wiring); D1 deps; `tooling/**` (except `RAW_INTERACTIVE_EXEMPTIONS`/`OUTLINE_NONE_EXEMPT` if forced); docs chrome.

Acceptance: `probe-forced-colors.mjs` → visible focus indicator on every text-entry control; `grep -rn "selection:" packages/ui/registry/ui` → 0; `useShakeOnInvalid` imported only by `field.tsx`; `fieldControl` imported by ≥5 components; `probe-states.mjs --routes input,select,checkbox,radio-group,switch,number-field,field-inline,password-input,textarea,otp-input,checkbox-group` light + `--dark` → 0 `hover-invisible` on triggers/controls, 0 `hover-touches-border`; `pnpm gates:component` on every touched component; axe clean. PR title: `Fo1 · Forms: Field owns feedback, one field chrome, CheckboxGroup, use-inline-edit (#39)`, `Closes #39`.
