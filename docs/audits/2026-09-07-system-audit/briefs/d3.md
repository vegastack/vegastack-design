# D3 · Dependencies: majors (#51) — four PRs, the last stops before merge

> **2026-09-09: item 4 is superseded by `d3-4.md`.** This file predates the verification rebuild and
> its verification instructions are stale everywhere — treat items 1–3 as SCOPE ONLY and read
> `_common.md` for how anything is verified now. Item 4 in particular names five things that no
> longer exist; `d3-4.md` re-derives it against the current release topology.

Base for each sub-PR: `origin/main` AFTER D2 (#50) has merged. Branches: `audit/d3-1-mechanical-majors`, `audit/d3-2-tanstack-table-9`, `audit/d3-3-vitest-5`, `audit/d3-4-changesets-pnpm12`. Decisions FINAL: sanctioned-dependency boundaries in AGENTS.md; batch 10 is MK-gated (prepare, never merge). Sources: `01-deps.md` batches 7–10; issue `issues/20-d3-deps-majors.md`; read every target version's official migration guide (`query-docs`, `WebFetch`); record URLs.

1. **Mechanical majors** — `motion` 13 (only import site: I1's factory in `packages/design/src/icons/create-animated-icon.tsx`; the `@emotion/is-prop-valid` removal does not apply), `react-dropzone` 20 (`use-file-drop` only), `@atlaskit/pragmatic-drag-and-drop` 3 (+ per-function imports; `use-drag-reorder` only), `@testing-library/jest-dom` 7 (+ `@testing-library/dom` peer), `globals` 17. Each behind its boundary file; the consumers' unit suites + contract routes in the one remote sweep.
2. **TanStack Table 9** — `data-grid` only: `useTable` + `tableFeatures`, feature-slot row models, renames per `01-deps.md`; the APG keyboard layer is ours and must not change; virtualisation unaffected; every data-grid fixture in the contract lane.
3. **Vitest 5 + vitest-browser-react 2.3** — after D1's `@vitest/browser` override; gate report paths in `tooling/gates.mjs` re-verified (coordinate with G1-b if it has landed); browser-mode unit + axe suite green in Chromium, WebKit, Firefox (the three-engine run on a box).
4. **Changesets 3 + `changesets/action` v2 + pnpm 12** — one reviewed change touching `release.yml`, `tooling/gate-receipt-carry.mjs` (the version-bump carry), `.changeset/config.json` (`privatePackages`), `packageManager` and the minis' pnpm; `verify-workflow-security` + `verify-gate-receipt-negative` green; a dry-run of the release workflow's classify + receipt steps on the branch (`node tooling/release-classify.mjs`, `pnpm release:preflight` — this is the one batch allowed to run preflight); **open the PR and STOP — MK merges**.

Per PR: changelog `📦`; `operator-review.md` notes any behaviour change; one remote `gates:push` (2 and 3 also the three-engine suite on a box). PR titles: `D3-1 · Mechanical majors: motion 13, react-dropzone 20, pragmatic-dnd 3, jest-dom 7, globals 17 (#51)`, `D3-2 · TanStack Table 9 in data-grid (#51)`, `D3-3 · Vitest 5 + vitest-browser-react 2.3 (#51)`, `D3-4 · Changesets 3, changesets/action v2, pnpm 12 (#51) — MK-gated`. Only D3-4's body may say `Closes #51` (once the others are merged).
