# M1 · Media players (#35) — `audit/m1-media-players`

Worktree: `/Users/mk/projects/vegastack-design/.claude/worktrees/agent-a26bd900144579794`. Based on F2's UNMERGED head `2ac84d39` (F2 later added `40e2bab4`; F2's PR is not yet open). Before the PR: wait for F2 to merge (poll `git fetch && git log origin/main --oneline -5` every 2–3 min), then `git rebase --onto origin/main 2ac84d39` (or `--onto origin/main <your first commit>^`). Never open a PR containing F2's commits.

Decisions FINAL: D16 (no glass — overlay buttons are ghost IconButtons on a scrim), D17 (standard 2px outline on media controls), TD-4. Sources: `02-batch-04-media-rich-text.md` (B4-01…B4-08, B4-11), `02-batch-01` B1-15, `04-cross-cutting.md` §1, issue `issues/04-m1-media-players.md`. Files: `packages/ui/registry/ui/{audio-player,video-player,slider,attachment,image,code-block,tool-call-chip}.tsx` + tests/previews/MDX, `registry.json`, `component-contracts.json` (`crossBrowserSmoke`), `packages/ui/vitest.smoke.config.ts`. Docs: Base UI Slider/Popover (installed version), HTMLMediaElement, `<img loading decoding fetchpriority>`, Tailwind 4.3 `pointer-coarse:`/`(hover: none)`.

## Scope (all of it)

1. New registry item `media-player-controls` (shared controls + `useMediaShortcuts`: Space/K play, J/L seek, M mute, F fullscreen, arrows); `audio-player` and `video-player` compose it; `video-player` declares it in `registryDependencies`; `assignRef`/`getMediaDuration`/`clampTime` in one module (use `mergeRefs` from `@vegastack/design` only if it already exists — Mk1 owns adding it).
2. Overlay chrome on `--media-scrim` (gradient), `--media-scrim-strong` (volume pill), `--media-foreground` (ink); buttons `IconButton variant="ghost" shape="round"` on the scrim.
3. Delete `MEDIA_SOFT_FOCUS_CLASS`/`MEDIA_FOCUS_OFFSET_CLASS`; controls keep the centralised outline, inset inside `overflow-hidden` frames; forced-colours carve-out removed; `tabIndex=0` only on genuinely scrollable regions.
4. Audio: mute button + volume popover in wide and narrow layouts; seek thumb visible at rest under `(hover: none)` and on focus, hidden-until-hover on hover-capable devices only.
5. `Slider`: `variant: default | media | overlay`, `orientation`, `thumb: always | hover`, `marks`, `showValue`; players pass props; every `[&_[data-slot=slider-*]]` override deleted.
6. `AttachmentTrigger`: drop `outline-none`, inset ring. `Image`: `loading="lazy" decoding="async"` defaults + `eager` opt-out.
7. Docs: `video-player.mdx` static fixture with overlay open (`controlsVisible` prop, documented); `code-block`/`tool-call-chip` fixtures per B4-11; slider/audio/attachment/image pages (content only). Contract records → `pnpm design:derived`.
8. Doctrine `design.md` §Media (amendment #9) + `pnpm design:sync`; skills; changelog (`🧩 media-player-controls`, `🔧`, `⚠️` Slider props); `@vegastack/ui` minor changeset; ledgers.

Do NOT touch: F2 `button/icon-button/split-button`; Fo1 form leaves; O1 overlays (you consume Popover; do not edit it), `command`, `emoji-picker`, `dialog`, `sheet`; Di1's directive/`motion-reduce:` deletions, `progress/badge/kbd/status-icon/skeleton/truncated-text/relative-time`; T1 tables; I1 icons; M2 `text-edit/markdown-view/action-bar/message-scroller/filter-bar`; D1 deps; `tooling/**` gates (except `RAW_INTERACTIVE_EXEMPTIONS`/outline allowlists if forced); docs chrome.

Acceptance: dark-lane test: video scrim L < 0.3 and overlay icon ink L > 0.85 (compiled-CSS test); `grep -c "\[&_\[data-slot=slider" audio-player.tsx` → 0; `wc -l audio-player.tsx` < 600 (before/after); item in `registry.json` + contracts; `pnpm gates:component audio-player video-player slider attachment image media-player-controls code-block tool-call-chip`; `pnpm --filter @vegastack/ui test:smoke`; `probe-overlays.mjs`/`probe-media.mjs` same chrome both themes; `probe-states.mjs --routes audio-player,video-player,slider,attachment` light + `--dark`. PR title: `M1 · Media players: shared controls, theme-invariant chrome, Slider props (#35)`, `Closes #35`.
