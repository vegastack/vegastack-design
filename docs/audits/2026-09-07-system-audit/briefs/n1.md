# N1 · Navigation and layout: selected-chip recipe, tab hover inset, sidebar persist prop, scroll-region focus (#43) — `audit/n1-navigation-layout`

Base: F2's UNMERGED committed head `eb4abb05ab7dee83a2b27543a3b04ba45a0af900` (a local commit; 13 commits ahead of main incl. F2's rebase): `git fetch origin && git checkout -B audit/n1-navigation-layout eb4abb05ab7dee83a2b27543a3b04ba45a0af900`; `pnpm install --frozen-lockfile`. Before the PR: wait for F2 to merge (poll `git fetch && git log origin/main --oneline -5` every 2–3 min), then replay ONLY your commits with `git rebase --onto origin/main <your first commit>^` (F2 is squash-merged). Never open a PR containing F2's commits.

Decisions FINAL: D20 (keep all three Tabs variants; fix the hover wash touching the rail and every similar micro-defect), D21 (neutral filled star), D22 (sidebar cookie behind `persist`, on by default, can be turned off), TD-4. Sources: `02-batch-06-navigation-layout.md` B6-02…B6-11; `07-state-probe.md` SP-02 (tabs), SP-03 (clipped rings), SP-06 (active rows); issue `issues/12-n1-navigation-layout.md`. Files: `packages/ui/registry/ui/{tabs,segmented,toggle,toggle-group,sidebar,breadcrumb,page-header,scroll-area,message-scroller,board,app-shell,navigation-menu}.tsx` + tests/previews/MDX, `component-contracts.json`, `apps/docs/vrt/contracts.spec.ts`. Docs: Base UI Tabs/Toggle/ToggleGroup/ScrollArea (installed version), `document.cookie`, `@container`.

## Scope (all of it)

1. `selectedChipVariants` (shared recipe: muted track → `surface-2`, raised chip → `surface-3` with alpha hairline) used by Tabs `pill` and `chip`, Segmented, Toggle pressed, ToggleGroup pressed. Keep Tabs `line | pill | chip`. Fix-round item: selected items in `segmented.tsx` and `tabs.tsx` exclude hover/active via `not-data-pressed:*`/`not-data-[active]` — selected items must still step (hover surface-2 → pressed surface-3 or an ink step) so the probe's `active-same-as-hover` is 0.
2. Tabs `line`: hover wash is an inner pill inset 4px from the indicator rail, rounded; vertical variant mirrors on the inline-start rail; geometry added to the tabs contract fixture (SP-02).
3. Sidebar `persist: boolean` (default `true`) around the cookie write; document the cookie name and how to turn it off; `onOpenChange` unchanged.
4. `BreadcrumbPage` → `<span aria-current="page">`.
5. `SidebarTrigger` → `IconButton`; the page-header back link keeps being a real `<a>` wearing `iconButtonGeometry` (F2's rule — check F2's diff); logical margins (`-ms-*`).
6. ScrollArea viewport focusable only when scrollable (share T1's `useOverflow`), inset ring (`focus-visible:-outline-offset-2`); same for MessageScroller viewport, Board column viewports, `sidebar-rail`.
7. Shell previews render `AppShellContent`/`SidebarInset` with `render={<div role="region"/>}` (or the preview frame sandboxes them) so the docs page keeps one `<main>`; add the `landmark` escape to the components' API (DC-06's other half).
8. `--layout-header-height`, `--sidebar-width-mobile` (F1) replace the literals; neutral star (`fill-current text-foreground`); delete restated focus utilities; active row = `surface-3`, hover = `surface-2` (F1 did the sidebar rows — verify).
9. Docs/tests per B6-11 (navigation-menu keyboard/RTL/mobile fixtures, segmented sizes/disabled/icon fixture + dark capture, toggle previews; rename "SSR persistence" section).
10. Doctrine `design.md` §Selection (one raised-chip recipe), §Tabs cites §Hover geometry + `pnpm design:sync`; skills; changelog (`🔧`); `@vegastack/ui` minor changeset; ledgers.

Do NOT touch: O1's overlays and `sidebar.tsx` Sheet composition (rebase across O1 if it lands first); T1's tables; T2's chips; C1's `item/alert/empty/…`; Mk1's block/hooks; `tooling/**` gates (except allowlist counts); docs chrome.

Acceptance: `probe-states.mjs --routes tabs,segmented,toggle,toggle-group,sidebar,scroll-area,message-scroller,board,breadcrumb,page-header` light + `--dark` (remote) → 0 `hover-touches-border`, 0 `focus-clipped`, 0 `active-same-as-hover`; axe: no `landmark-no-duplicate-main` on app-shell/sidebar routes; no `role="link"` on BreadcrumbPage; `pnpm gates:component` for every touched component (remote); smoke for sidebar/tabs. PR title: `N1 · Navigation and layout: selected-chip recipe, tab hover inset, sidebar persist, scroll-region focus (#43)`, `Closes #43`.
