# F2 finish task — `audit/f2-button-variant-tone` (#33)

Worktree: `/Users/mk/projects/vegastack-design/.claude/worktrees/agent-aa23f9e1ef7ff3262`. The implementer finished the code (7 commits on top of `origin/main` 9c33dfaf; last: `40e2bab4 fix(ui): keep PageHeader's back affordance a link; export iconButtonGeometry`), 0 uncommitted, but was terminated before its final `pnpm gates:push` wrote a receipt and before pushing. `verify-gate-receipt` reports the committed receipt describes a different contract SHA — expected.

Implemented (verify by reading the diff, `git log`, `docs/ledger/bugs.md` + `operator-review.md` F2 entries, `CHANGELOG.md` 0.7.0, changesets): `Button` `variant: solid|soft|outline|ghost|link|cta` × `tone: neutral|destructive|success|warning|info` via `--btn-*` custom properties; `glass`/`finish`/`shadow-lit` and the seven colour-named variants deleted; `tone="destructive"` + `variant="solid"` is a compile error; sizes `xs·sm·md·lg` across the registry (no `default` size keys, no Button icon tier); `IconButton shape="square|round"` + exported `iconButtonGeometry(size, shape)` for anchor affordances; hand-rolled icon buttons replaced in Alert, AnnouncementBanner, Dialog, Sheet, Pagination, OnboardingChecklist, FilterBar, ColorPicker, EmojiPicker, PageHeader, CopyButton, MessageScroller, SplitButton; loading keeps width; `aria-disabled` without `pointer-events-none` (D7); docs, skills, doctrine, changelog, changesets, ledgers updated. Three defects found + fixed: loading button lost its accessible name; a permanent `display: contents` wrapper changed SVG hit-testing; tone vars via `--color-*` painted light ink in nested dark scopes.

## Your job

1. Sanity-check the branch against issue #33 (`issues/02-f2-button-variant-tone.md`) and `03-proposals.md` §P2 acceptance: `grep -rn 'size="default"\|default:' packages/ui/registry/ui/*.tsx` shows no size key named `default`; `grep -rn "<button\b"` only inside Base UI `render` defaults and the react-day-picker day button; `rounded-full` only on avatars/dots/progress/switch/radio/slider thumbs; Button page previews show the full matrix; `RAW_INTERACTIVE_EXEMPTIONS` counts consistent. Fix anything missing (small).
2. `git fetch origin && git rebase origin/main` (main = 9c33dfaf unless more merged — check).
3. Run: `node tooling/design-lint.mjs packages/ui/registry`; `pnpm typecheck`; `pnpm lint`; `pnpm registry:build && git status --porcelain` clean; `pnpm design:derived && git status --porcelain` clean; `pnpm design:sync:check`; `node tooling/changelog-lint.mjs` (every linked sha must exist on the branch AFTER the rebase — repoint any pre-rebase sha, then `sync-changelog`); `node tooling/audit/probe-states.mjs --routes button,icon-button,split-button,alert,dialog,pagination` (or the `docs/audits/...` path) light + `--dark`: hover AND pressed on every variant; one docs build; `pnpm gates:push` (wait; commit code + `.gates/receipt.json`).
4. `git push -u origin audit/f2-button-variant-tone`; `gh pr create --base main --title "F2 · Button variant × tone, size vocabulary, IconButton everywhere (#33)" --body-file <file>` — write the body from the commits/ledgers: plan; the final matrix; the migration table applied with counts (cva `default` keys 31→N, `size="default"` 10→N, hand-rolled `<button` 18→N, `rounded-full` overrides 48→N); doctrine/docs/skill/changelog edits; docs URLs + versions (Base UI installed version, CVA 0.7, Tailwind 4.3); acceptance evidence; the three defects; left-out items; "Needs MK" (the F1 fill-vs-border hover question is already parked; note if F2 kept the fill). `Closes #33`. Do NOT merge. Final message = PR URL + body verbatim.
