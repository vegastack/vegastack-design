# Di1 · Display leaves (#42) — `audit/di1-display-leaves`

Worktree: `/Users/mk/projects/vegastack-design/.claude/worktrees/agent-a26dcb3beed941b36`. Based on F2's UNMERGED head `2ac84d39`. Before the PR: wait for F2 to merge, then `git rebase --onto origin/main 2ac84d39` (or `--onto origin/main <your first commit>^`). Never open a PR containing F2's commits.

Decisions FINAL: D8 (Badge `sm` = 16px tier; `minimal` = no bg/border, coloured text + leading dot icon by default, icon configurable), D9 (TruncatedText `focusable`, off inside grids/lists), TD-3 (`text-xs` mono-only). Sources: `02-batch-02-display-leaves.md` (B2-01…B2-11), `04-cross-cutting.md` §2 (`motion-reduce:` 17/11 files; `text-xs` 7/4 files) and §5 (12 `"use client"` files: avatar, button, collapsible, field, progress, resizable, scroll-area, separator, slider, switch, tabs, toggle — a client module poisons `buttonVariants` for RSC importers), issue `issues/11-di1-display-leaves.md`, `component` skill §3 (server-safety is a RUNTIME claim). Files: `packages/ui/registry/ui/{progress,badge,truncated-text,relative-time,kbd,status-icon,skeleton,command,tooltip,avatar,collapsible,resizable,scroll-area,separator,switch,tabs,toggle,field,slider,button}.tsx` (+ every file with `motion-reduce:`/`text-xs`), `tooling/verify-rsc-safety.mjs`, `verify-ui-use-client.mjs`, `design-lint.mjs` (`presentational-client-boundary`), `use-platform.ts`. Docs: React 19 RSC (`react-server` hooks), Base UI wrappers (which pass callbacks), `Intl.RelativeTimeFormat`.

## Scope (all of it)

1. `Progress`: indeterminate = sweeping segment (`animate-pulse` allowed), `aria-valuenow` omitted; test asserts fill ≠ 100%.
2. `Badge`: `size: sm (16px) | md | lg`, `variant: solid | soft | outline | minimal` (ink + leading dot, `icon` prop replaces the dot); remove non-badge looks; migrate every consumer (grep `<Badge` across `packages/ apps/docs`: Board counts, block trend badges, PricingSection/PlanCard, previews, MDX).
3. Remove `"use client"` from the 12 files after confirming no hook/handler (Base UI wrappers passing callbacks keep it with a one-line comment); `verify-rsc-safety` + `presentational-client-boundary` green. Sibling-owned files (`button.tsx` F2, `field.tsx`/`switch.tsx` Fo1, `slider.tsx` M1, `tabs/toggle/scroll-area` N1): ONE-LINE directive deletions only, listed in the PR body.
4. `TruncatedText focusable` (default `true` standalone, `false` inside DataList/DataGrid via a context you export; T1 wires consumption or you do the 2-line wiring if T1 has merged by your rebase).
5. `RelativeTime`: server-render the absolute formatted date, hydrate to relative; SSR test.
6. Delete all 17 `motion-reduce:` utilities. One kbd chip: `Kbd` used by `CommandShortcut` (`command.tsx` — edit only that render; O1 owns the rest) and `TooltipKbd` (`tooltip.tsx` — same); `Kbd os` defaults from `usePlatform` at the caller, `"other"` as SSR default.
7. `StatusIcon` sizes → `--icon-*`; Skeleton line radius `rounded-sm`.
8. `text-xs` on non-mono (7 sites) → `text-sm`.
9. Docs per B2-11 (content only); doctrine `design.md` §Badge tiers, §Motion "reduced motion is global, never restated", §Typography `text-xs` mono-only + `pnpm design:sync`; skills; changelog (`🔧`, `⚠️` Badge API); `@vegastack/ui` minor changeset; ledgers.

Do NOT touch beyond the one-line exceptions: F2 button files; M1 media/slider internals; Fo1 form leaves; O1 overlays (except the two Kbd spots); T1 tables; N1 tabs/toggle/scroll-area/sidebar internals; C1 `item/alert/empty/timeline/notification-bell/onboarding-checklist/stepper`, `collapsible/accordion` hover (you may only delete `collapsible.tsx`'s directive); D1 deps; `tooling/**` (except `verify-ui-use-client.mjs`/`design-lint.mjs` allowlists if a removal forces it); docs chrome.

Acceptance: `grep -rn "motion-reduce:" packages/ui/registry/ui` → 0; `grep -l '^"use client"' packages/ui/registry/ui/*.tsx | wc -l` before/after (≤ 72 target; report real numbers); `text-xs` only in mono contexts; Progress capture partial fill (read PNG); RelativeTime SSR HTML contains a date; Badge `sm` 16px + `minimal` dot in both themes (captures); `node tooling/verify-rsc-safety.mjs`; `node tooling/verify-ui-use-client.mjs`; `pnpm gates:component progress badge truncated-text relative-time kbd status-icon skeleton` (+ each directive-deleted file). PR title: `Di1 · Display leaves: Progress indeterminate, Badge tiers, server-safe leaves (#42)`, `Closes #42`.
