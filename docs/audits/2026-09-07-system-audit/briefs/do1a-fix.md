# Do1-a fix task — PR #54 `audit/do1a-docs-chrome-export`

Worktree: `/Users/mk/projects/vegastack-design/.claude/worktrees/agent-a2f59dc40790d7bce`. This is a FIX task on an open PR, not a new batch. State: 9 commits (8 original + `3bfaf0f3 fix(docs): fail closed on unknown MDX components in the agent export` from the previous fix agent), 0 uncommitted, NOT pushed since the fixes; the receipt in HEAD is stale (`verify-gate-receipt` says the tree moved). The previous agent reported: behaviour contracts pass on all 110 routes; only `video-player` unit tests failed under load (this branch changes no registry source) — rerun that file in isolation.

Reviewer findings that were assigned (verify each is done in the diff; finish any that is not):

1. `apps/docs/lib/mdx-markdown.ts` / `markdown-export.ts`: unknown MDX components must FAIL the build (one exhaustive manifest shared by the MDX map, the stringifier and the placeholder renderer; children-only fallback only for listed structural wrappers); `verify-docs-export.mjs` requires the expected API section/table per component page and catches namespaced/lowercase MDX (`<story.WithControl />`, `<api-table />`); negative self-tests.
2. `tooling/design-lint-emitted-css.mjs`: `neutralisedProseDefault()` must not accept a narrower override (`.prose aside h2 {…}`) as neutralising `.prose :where(h2){font-weight:600}`; selector-coverage comparison; self-test fixture proves it fails.
3. `apps/docs/vrt/docs-shell.spec.ts`: add computed font-size/line-height on an OPENED Base UI portal and an explicit inert/background-isolation assertion for the fullscreen preview; wire the suite into the docs package `test` script (G1-b will add it to the attested lane — do not edit `tooling/gates.mjs`).
4. Remove the `<RegistryInstallCallout />` line from every `apps/docs/content/docs/components/*.mdx` (mechanical, one line each; nothing else in the pages) and delete the component if nothing renders it (MDX map + markdown branch).
5. `docs/ledger/operator-review.md`: "no page left with neither" → measured truth (45 curated · 6 Explorer · 59 neither · 0 both); gate stays "never both, Explorer always wrapped" with negative self-tests.
6. `CHANGELOG.md` 0.7.0 📚 + `design.md` §Docs canon wording: this PR ships infrastructure + three reference pages; the canon is the target shape reached in Do1-b. `sync-changelog` + `changelog-lint` + `pnpm design:sync` + `design:sync:check`.
7. `verify-component-contracts.mjs --self-test` gets a real negative path (in-memory drifted `dataAttributes` must fail) wired where the other self-tests run.
8. `component-preview.tsx` comment + `bugs.md`: fixture selection is `[data-vrt-preview].first()` in DOM order, not a contract decision; keep the G1-b recommendation.
9. `global.css` comment `14/20` → `14/21`.
10. Out-of-lane edits justified in the PR comment: `tooling/design-lint.mjs` hook (allowed), `verify-component-contracts.mjs` (required), the plan doc (fine), `docs/research/design-md-audit/*.json` (generated by `design:sync`? say so; else revert + regenerate), `AGENTS.md:465` (revert if outside §Docs authoring and not required).
11. Record the two Do1-b transitional aliases (`AutoTypeTable: ApiTable` map entry; slug-inferred `registry` frontmatter) in `operator-review.md` + a "Do1-b must remove" note in the PR comment.
    Also: the `.changeset/do1a-data-attributes.md` (`@vegastack/ui` patch) must be committed — CI failed once on `changeset status`.

Finish: `pnpm --filter @vegastack/docs build` (one build); `node tooling/verify-docs-export.mjs` + `--self-test`; `node tooling/design-lint.mjs --docs-shell --emitted-css` + self-test; `pnpm --filter @vegastack/docs test`; `node tooling/verify-component-contracts.mjs --self-test`; `grep -rl RegistryInstallCallout apps/docs/content` → empty; `pnpm lint`; `pnpm typecheck`; `pnpm design:derived && git status --porcelain` clean; `pnpm exec changeset status --since=origin/main`; `git rebase origin/main` (F1 merged — expect conflicts in `mdx.tsx`/`global.css`/`foundations.tsx`/`CHANGELOG.md`; F1's SurfaceLadder registration and forced-colours block must survive); `pnpm gates:push`; commit with receipt; `git push`; confirm CI `receipt-guard` + `verify` execute and pass (`gh run list --branch audit/do1a-docs-chrome-export`, `gh run view <id> --json jobs`; poll to completion); `gh pr comment 54` with finding → fix → evidence. Final message = the comment + CI run id + job results. Do NOT merge.
