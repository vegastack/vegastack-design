# Phase Z — design.md / AGENTS.md finalization summary

Scope: sync `design.md` and `AGENTS.md` to everything the 2026-07 overhaul program (Phases P → B) shipped.
Every correction below was verified against the live repo (greps/reads cited), not against memory or the
decisions log alone — the decisions log told me *where to look*, the source files are what I wrote from.

## design.md corrections (stale → current, with source)

### Typography — full rewrite (frontmatter `typography:` block + prose §Typography)
- **Weight model.** Stale: "300 light (hero display ONLY) · 400 default · 500 labels/h4 · never 600+."
  Current: 400 is the discipline default, 500 labels/h4, **600 is a rare deliberate emphasis (D3 cap)** —
  not a blanket ban. Source: `docs/audits/2026-07-14-system-audit/17-brand-direction.md:20` ("400 default
  · 500 labels · 600 rare emphasis (D3 cap)"). The old 300-weight hero no longer exists anywhere —
  confirmed `--text-display-*--font-weight: 400` for all four display tiers in
  `packages/tokens/dist/theme.css:332,336,340,344`.
- **Scale values.** Stale ladder (`text-display` 36/40/300, `text-h1` 28/34, `text-h2` 22/28, core ladder
  undocumented) replaced with the shipped two-layer scale, verified against
  `packages/tokens/dist/theme.css:197-238` (product ladder: xs 11/16 · sm 12/16 · base 14/21 · lg 16/24 ·
  xl 18/26 · 2xl 20/28 · 3xl 24/32) and `:330-372` (display sm/md/lg/xl = 32/40/56/72, tracking
  −0.04→−0.06em, all weight 400; h1/h2/h3 = 24/20/18 weight 400; h4 = 16/500; label 14/500; label-sm
  12/500; code 13/400; code-sm 12/400). Cross-checked against `skills/add-component/SKILL.md:81-91`
  (already-synced canon) — now consistent.
- **Two-layer product/doc ladder + mono voice token** (`text-mono-label`) added — wasn't documented in
  design.md at all before; source: `theme.css:197-238` (`--type-product-*`/`--type-doc-*`) and
  `skills/add-component/SKILL.md:81-91`.
- Also fixed the Overview bullet ("A 300-weight hero `display`…") and the Do/Don't "never 600+" line to
  match the D3-cap model.

### §States — disabled opacity
- Stale: "disabled (45% + `not-allowed`)" (button States bullet, line ~350 pre-edit).
- Current: `opacity-(--opacity-dim)`, 50%. Source: `packages/tokens/dist/theme.css:83`
  (`--opacity-dim: 50%; /** Element opacity for disabled controls… */`) and decisions log Phase T2 note
  ("design.md §States says disabled = 45%; shipped system uses 50%… text to be corrected in Z").

### Destructive buttons — SOFT-only (D4)
- Stale: `button-destructive` recipe = `{ background: destructive.fill, hover: destructive.hover, … }`
  (a solid-fill recipe) plus a separate, functionally-duplicate `button-soft` entry.
- Current: verified `packages/ui/registry/ui/button.tsx` — the `destructive` variant is
  `'bg-destructive-subtle text-destructive-text hover:bg-destructive-subtle-hover'`; there is no solid
  destructive fill anywhere in the CVA. Rewrote the `button-destructive` recipe to the soft values, deleted
  the now-redundant `button-soft` line, and fixed the Button prose bullet + component-recipe comment.
  Confirms decisions log Phase C: "Solid destructive Button NOT added (P1-07): D4 locks destructive =
  soft-only; design.md's solid-fill recipe is the stale side — corrected in Phase Z."

### Card padding
- Stale prose (Layout section): "Cards use 20px padding (16px compact, 32px hero)" — three wrong numbers
  at once (frontmatter already said 16px, contradicting this prose line).
- Verified `packages/ui/registry/ui/card.tsx:44,46`: default = `py-4`/`px-4` (16px), `size="sm"` = `py-3`/
  `px-3` (12px). **No "hero" size exists on Card at all.** Fixed prose to "16px padding (12px compact via
  `size="sm"`; there is no separate 'hero' size)."

### Button `xs` size + `cta` variant
- `xs` was undocumented despite shipping (`--size-xs: 1.5rem` in `theme.css:69`, `button.tsx` `xs`/`icon-xs`
  CVA entries). Added to the control-height frontmatter comment, the `components.button-sm/xs/lg` recipe,
  and the Button prose bullet.
- `cta` was documented in full in §Brand & Marketing already (correct, Phase B's work) but not
  cross-referenced from §Components — added a one-line cross-link in the Button bullet and a
  `button-cta` frontmatter stub that points to §Brand & Marketing instead of duplicating the recipe, per
  the task's "cross-link, don't duplicate" instruction.

### Radius — xs/sharp added, xl removed
- Stale: 4-step scale (`sm 6 · md 8 · lg 12 · full`), no mention of `xl` at all (silently implying it might
  still exist).
- Verified `theme.css:63-68`: `--radius-xs: 0.125rem` (2px), `--radius-sharp: 2px`, no `--radius-xl` token;
  `tooling/design-lint.mjs:72` has a dedicated `removed-radius-xl` rule. Rewrote the frontmatter `rounded:`
  block (6 steps, `xl` called out as removed/lint-banned, containers capped at `lg`) and the prose §Shapes
  section to match.

### Color
- **chart-7 olive (hue 104)** — already correct in design.md before this pass (`#8d8300 / #b5a80c`); left
  unchanged. Verified against `theme.css:52,176` (`oklch(0.6 0.127 104.2)` light / `oklch(0.72 0.15 103.9)`
  dark).
- **`--brand` phosphor** — was narrated in §Brand & Marketing prose but had no frontmatter entry and no
  numeric values. Added a `colors.brand` frontmatter block and inlined the exact values into the §Brand &
  Marketing accent paragraph: light `oklch(0.6 0.17 148)` (3.5:1), dark `oklch(0.86 0.21 148)` (13.3:1).
  Source: `theme.css:54,186`.
- **`--alpha-*`/`--opacity-*` token vocabulary** — didn't exist anywhere in design.md as named tokens (only
  hardcoded raw percentages like "ring/70", "45%"). Added a new frontmatter `opacityAlpha:` block
  (representative role tokens, not exhaustive — cross-referenced to `theme.css`) plus token names inline in
  the Accessibility/States/Components prose. Source: `theme.css:83-107,187-188`.
- **Focus model — "no rings"**: found and fixed a real internal contradiction. The old Accessibility bullet
  claimed the 2px `:focus-visible` outline is "applied to every interactive element," while the adjacent
  Input/Select bullet correctly described text-entry fields using a border-tint with NO outline (the actual
  Phase P/T5 mechanism). Rewrote the Accessibility bullet, the Overview/Do/Don't lines, and the Button
  States bullet to consistently state: border-tint only on text-entry fields (`focus`, not `focus-visible`),
  native 2px `:focus-visible` outline everywhere else, never a box-shadow ring/glow anywhere. Verified the
  mechanism against `packages/ui/registry/ui/button.tsx`'s `focus-visible:border-ring/(--alpha-tint-border)`
  pattern and the Phase P decisions-log entry (popup `outline-none` → centralized `base.css` outline).

### Motion
- Stale: only `ease-standard` documented as a token; `ease-emphasized`/`exit` were mentioned in a comment
  as prose, not real entries; `ease-spring` and `--motion-blur` didn't exist in the doc at all; no mention
  of the `motion-*` utilities or the mechanism matrix.
- Verified `theme.css:113-119`: 4 real ease tokens (`standard`/`emphasized`/`exit`/`spring`) + `--motion-blur:
  2px`. Verified the 3 utilities (`motion-pop-in`/`motion-enter-up`/`motion-shake`) and their mechanics via
  decisions log Phase M + `skills/add-component/SKILL.md`'s mechanism matrix (§2). Rewrote the frontmatter
  `motion:` block and prose §Motion section to include all 4 eases, the 3 utilities, a link to
  `foundations/motion` + the skill's mechanism matrix (rather than duplicating it), and the reduced-motion
  contract's `::view-transition-group/old/new(*)` kill switch. Source for the view-transition fix:
  `packages/tokens/src/base.css:69-77` (Phase S).

### Brand & Marketing section
- Left structurally as-is per instruction (Phase B's work, already consistent with
  `docs/audits/2026-07-14-system-audit/17-brand-direction.md` and the D17/D18/D20 decisions). Only addition:
  the numeric brand contrast values (see Color, above) — no rewrite.

### Sizes
- **`--size-xs`** added throughout (control-height comment, Components intro prose, Button recipe/bullet).
- **`--icon-*` scale** — was missing `--icon-compact` (12px, exists only for the `xs` control tier).
  Verified `theme.css:73-77`. Added to the `icons:` frontmatter block and the §Iconography prose.
- **Sidebar width tokens** — found a real numeric error: design.md said `width: 256px`; the shipped token is
  `--sidebar-width: 15rem` = **240px** (`theme.css:78`, `--sidebar-width-icon: 3rem` = 48px collapsed,
  `:79`). Fixed the `components.sidebar` recipe to the correct px values and added the collapsed-width
  entry, which wasn't documented at all before.
- **Touch-target ≥24px hit-area rule** — already correctly documented in §Accessibility ("icon buttons get
  a ≥24px hit area"); verified against Phase R's decisions log and left unchanged (no delta found).

## AGENTS.md corrections (stale → current, with source)

- **Engine-vs-primitive distinction added** to the locked-decisions primitive-exception bullet: named
  `react-resizable-panels`/`recharts`/`motion`(lucide-animated)/`tiptap` as sanctioned renderer/engine
  dependencies (same class as the pre-existing `sonner`), distinct from the ONE headless-primitive
  exception (`@vegastack/message-scroller`). Verified via `packages/ui/registry.json` dependency scan
  (`chart`/`dashboard-01` → `recharts`; `resizable` → `react-resizable-panels`; `text-edit` → `tiptap`;
  440 `icon-*` items → `motion`; `sonner` → `sonner`) and decisions log Phase X2. Also folded in the
  Attachment-primitive-doesn't-exist correction (decisions log Phase X2: "no @shadcn/react Attachment
  primitive exists in any published version… AGENTS.md exception language stays true, no broadening
  needed") — the old file never mentioned this, so no contradiction existed, but the task asked for it
  documented; now it is.
- **Component build rules** — added: raw-token bans (z-index/alpha/opacity/radius-xl/size), ref-as-prop
  (React 19, replacing "forwarded refs"), the `render` contract note, a motion rules paragraph
  (tokens + `motion-*` utilities + `raw-motion` lint), `uppercase-mono` (D20), the `flex-truncate-conflict`
  responsive rule, the touch-target hit-area technique (with the real Chromium `<button>` Preflight-clipping
  gotcha from Phase R), icon items installing as `@vegastack/icon-<name>`, the two non-`registry:ui` item
  types (`registry:hook`, `registry:block`) with their real names, and AppShell as the shared-layout source
  of truth. All verified against `tooling/design-lint.mjs` rule IDs, `skills/add-component/SKILL.md`, and
  `skills/design-audit/SKILL.md` (both v0.2.0, the post-overhaul canon) so AGENTS.md doesn't contradict
  them.
- **Single-source-of-truth section** — added two notes that weren't documented anywhere in AGENTS.md before:
  JSON block payloads (`dashboard-01/data.json`) skip the provenance-header stamp (would break as JSON) but
  stay integrity-covered at the item level, and `public/r` is pruned to current item names on every build
  (shadcn build is additive-only; a stale JSON can re-stamp a source with a dead identity — this was a real
  Phase B/S incident). Source: `tooling/registry-header.mjs:53-86`.
- **Layout section** — added `packages/ui/registry/blocks/`, the smoke lane, and a precise skills-version
  note. Also added a **Numbers** section: 525 registry items (522 `registry:ui` incl. 440 icon items + 2
  `registry:hook` + 1 `registry:block`, verified via a `registry.json` item-count/type-breakdown script),
  75-component matrix (71 tracked rows + 4 documented chat-family gap items — verified against
  `docs/ledger/component-matrix.md`'s own "Total: 71 rows… never added to this matrix" language, not just
  the task's stated number), and 91 showcase pages × 2 VRT lanes (counted the literal `PAGES` array in
  `apps/docs/vrt/components.spec.ts` — 91 entries, matches Phase B's final gate evidence "182/182 (91 pages
  × 2 lanes)").

## A precision correction I made vs. the task's literal wording

The task said "skills v0.2.0" as a number to land in AGENTS.md. I checked all 6 skill files' frontmatter:
only `add-component` and `design-audit` are `"0.2.0"` — `brand` is `"0.0.0"`, `consume`/`design-system`/
`release`/the top-level `skills/SKILL.md` are all `"0.1.0"`. A blanket "skills v0.2.0" claim would have been
false for 4 of 6 files, so I scoped the AGENTS.md line to the two that are actually v0.2.0 (the two the
task itself named as "already synced in Phase A").

## Stale items found beyond the task's known-delta list (not fixed — out of edit scope)

These are real contradictions I found while reading skills to verify AGENTS.md/design.md wouldn't
contradict them, but `skills/*/SKILL.md` isn't in this task's edit allowlist, so I flagged them instead of
touching them:

1. **`skills/design-system/SKILL.md` (v0.1.0, never touched by the overhaul) is broadly stale**: says "the
   64-component library" (should be ~75), lists `empty-state` (renamed `empty` in Phase C) and
   `state-select` (renamed `region-select` in Phase C), documents `rounded-{sm,md,lg,xl}` (`xl` was REMOVED
   in Phase T5 and is now lint-banned), and `ease-{standard,emphasized,exit}` (missing `spring`) plus a raw
   `duration-[var(--duration-fast)]` example (the system now bans raw `duration-[…]` via `raw-motion`).
2. **`skills/design-audit/SKILL.md`'s own 20-rule mirror is missing rule 21, `uppercase-mono`** (D20,
   `design-lint.mjs:548-576`) — that lint rule was added in Phase B, one phase after the design-audit skill
   was synced in Phase A, and never backfilled. The skill's own text says "if the script and this list ever
   disagree, the script is the ground truth and this file is stale (re-sync it, don't trust memory)" — so
   this is a self-flagged, self-diagnosable gap, just never re-run.

Both are spawned as separate follow-up tasks (see chat) rather than fixed here, since neither file is in
this task's edit scope (`design.md`, `AGENTS.md`, `CLAUDE.md`, this summary — nothing else).

## CLAUDE.md

Read in full; the SSOT editing-workflow description matches the verified AGENTS.md mechanics exactly
(canonical-only edits, `registry:build` regenerates copy-in + JSON). No factual claim in `CLAUDE.md` was
found to be wrong — left unchanged.

## Verification evidence

- `node tooling/content-lint.mjs` — clean, both before and after all edits.
- All numeric/token claims re-verified against live source at write time (not copied from the decisions
  log): `packages/tokens/dist/theme.css`, `packages/ui/registry/ui/{button,card}.tsx`,
  `packages/ui/registry.json` (item-count + dependency scan via inline `node -e`), `tooling/design-lint.mjs`
  (rule IDs), `apps/docs/vrt/components.spec.ts` (`PAGES` array length), `docs/ledger/component-matrix.md`,
  `packages/tokens/src/base.css` (view-transition reduced-motion block), and both v0.2.0 skill files.
- No changes made to any file outside the allowlist (`design.md`, `AGENTS.md`, this summary); `CLAUDE.md`
  reviewed and left unchanged (no factual error found).
