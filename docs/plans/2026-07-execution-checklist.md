# Execution Checklist — Overhaul v5 (greenfield, nothing deferred)

Companion runbook to `2026-07-system-audit-remediation.md` (v5). Every step is explicit; granular per-file inventories live in the cited audit registers — the executor reads the register, not their memory. Rules that apply to EVERY step: greenfield (no aliases/deprecations/compat shims) · zero hardcoded style values (opacity included) · no duplicate code (shared logic extracted on 2nd use in-system) · flat-only exports · uniform lint rules (no grandfathered exceptions) · every phase ends with the gate.

**THE GATE (run after every phase, all must pass):**

```
pnpm lint && pnpm typecheck && pnpm test
pnpm --filter @vegastack/docs test:vrt        # 74/74 — zero skips, zero missing baselines
pnpm registry:build && git diff --exit-code apps/docs/public/r apps/docs/components/ui packages/ui/registry.json
pnpm registry:verify-consume                  # when any component changed
```

VRT diffs reviewed one by one: intended → new baseline committed with the phase; unintended → fix before proceeding.

## Phase −1 — Rails `[in progress]`

- [x] Local darwin VRT baselines: **74/74 passing** (14 tall pages were a 5s screenshot-timeout issue — config now 30s; baselines currently untracked, land with the freeze commit; CI Linux baselines still require the vrt.yml update_baselines run after MK pushes)
- [x] Findings re-register → `00-REGISTER-v2.md`: 94 verified (80 OPEN · 13 AMENDED · 1 FIXED · 0 stale); marker/bubble 'use client' P0 confirmed still real
- [x] version-sync built + round-trip proven (507 items) + release.yml wired
- [ ] Freeze commit + tag `audit-baseline` — draft message, WAIT for MK's "commit"
- [ ] Archive transitions.dev tweet link from MK (goes in docs/plans/, cited by Phase M)

## Phase P — P0 fixes (register IDs from 00-REGISTER-v2) `[done 2026-07-15]`

- [x] popover.tsx + sheet.tsx `outline-none` → compensated focus style; same guard applied to dialog/alert-dialog (mechanism: popup keeps the centralized base.css `:focus-visible` outline — see decisions log; hover-card included; lint FOCUS_AFFORDANCE extended with the sanctioned `focus:border-` text-entry pattern, fixing the pre-existing textarea/otp-input baseline lint failure)
- [x] sidebar.tsx collapsed buttons: visually-hidden label (`sr-only`, not display:none) + regression test
- [x] truncated-text.tsx trigger keyboard access (relative-time pattern; TruncatedText + IconText, TableCellText inherits) + real-overflow browser tests
- [x] registry.json: remove notification-bell→separator phantom dep (also `@vegastack/button` — icon-button already declares it; NEW `tooling/verify-registry-deps.mjs` wired into registry:build, green on 507 items)
- [x] field.tsx horizontal orientation renders description (`basis-full`, mirrors FieldError) + test
- [x] marker/bubble `'use client'` verified still absent → added (P0-01); select.tsx transition list gains `scale` (P0-06)
- [x] Gate: lint ✓ typecheck ✓ test 698/698 ✓ VRT 74/74 zero diffs ✓ registry idempotent ✓ verify-consume ✓ (7 real CLI graphs + 507×2 simulated)

## Phase T — Token slices (each slice: tokens → bridge → migrate ALL sites → lint rule ON → VRT review → gate)

- [x] **T1 typography** `[done 2026-07-15]`: token ladders (product xs11→3xl24 base14 + doc compat, unitless-ratio leadings) via `--type-*` bindings + `@theme inline` remap; display tier text-display-sm/md/lg/xl (32/40/56/72, tracking −0.04→−0.06); h1–h3 re-based (24/20/18); 122 registry + 128 preview sites migrated (size-preserving shift + role upgrades: markdown-view/text-edit→h1–h4/label, overlay titles→h4, popover title→label, card→h4/label, FilterChip 12→14 P2-19); Fumadocs boundary (`:root` doc ladder + `.vs-type-product`/`[data-base-ui-portal]` product) in global.css; twMerge font-size classGroup extended (h*, code*, display-_); typography.mdx/design-principles.mdx/foundations.tsx rewritten (+TypeCoreLadder); lint off-scale text-_ ban ON. Gate: lint/tc/test 698 ✓ · VRT 21 intended diffs reviewed individually → baselines updated, confirm 74/74 ✓ · registry idempotent ✓ · verify-consume ✓
- [x] **T2 opacity** `[done 2026-07-15]`: 24 role tokens (--alpha-* modifiers via Tailwind `/(--var)` color-mix, --opacity-* element steps, 0/100 exempt endpoints) + theme-split soft washes + NEW precomposed `{family}-subtle-hover` colors (A11Y FIX: soft-button hover was 3.6–3.9:1 in light — light tuned to fill@7% composite, dark = exact current composite); every registry+preview site migrated (37+3 files, zero leftovers); base.css outline-ring/50 tokenized; contrast-check parses alphas + composites in gamma sRGB (76 checks green); lint raw-alpha + raw-opacity bans ON. Gate: lint/tc/test 698 ✓ · VRT 74/74 ZERO diffs (lossless) ✓ · idempotent ✓ · verify-consume ✓
- [x] **T3 z-index** `[done 2026-07-15]`: two band tokens (--z-raised 10, --z-overlay 50; DOM-order nesting within band) + contract in foundations/elevation §Stacking; 21×z-50 + 7×z-10 migrated to `z-(--z-*)`; nested-overlay hit-tests in `test/stacking.browser.test.tsx` (compiled-CSS suite: Select/Popover-in-Dialog, Tooltip-in-Sheet, nested Dialogs, toast-over-Dialog — 5 elementFromPoint assertions); sonner DOCUMENTED-EXEMPT top band (mounts before dialog portals; library z load-bearing, asserted by test); raw-z lint ban ON; 5 positioner test assertions + portal contract test updated to the band token. Gate: lint/tc ✓ test 703/703 ✓ VRT 74/74 zero diffs ✓ idempotent ✓ consume ✓
- [x] **T4 motion tokens** `[done 2026-07-15]`: --motion-ease-spring (audit §f linear(), custom `easing` type) + --motion-blur 2px (bridged as blur-motion); duration-400→duration-slow + duration-200→duration-base (3-step scale kept); 37 bare transitions × 28 files get co-located `duration-fast ease-standard` (variant prefixes carried); overlay ease unified (dropdown/context/tooltip/select + chevron); sidebar ease-linear→standard ×2; select scale bug was fixed in P; lint `transition-pairing` ON (string-literal-scoped; caught image.tsx's missing ease beyond the inventory). Gate: lint/tc ✓ test 703 ✓ VRT 74/74 zero diffs ✓ idempotent ✓ consume ✓
- [x] **T5 sizes/radius/icons** `[done 2026-07-15]`: --size-xs/sm/md/lg wired into every audit-06 control CVA (+avatar/skeleton/message adopt the shared rhythm); radius-xs (real 2px token) + radius-sharp added, radius-xl REMOVED + lint-banned (bubble→rounded-lg, intended); icon role tokens (--icon-compact/inline/default/action/feature) across 31 elements + all svg selectors + slot containers; sidebar widths tokenized (JS constants removed); TIMINGS + FLOATING in @vegastack/utils, 10 components rewired (tooltip 6→8, hover-card collision 16→8 unification deltas logged); 3 lint rules ON (removed-radius-xl, raw-control-size, raw-icon-size). Mid-phase regex bug (size-3 vs 3.5) caught in VRT review + fixed. Gate: lint/tc ✓ test 703 ✓ VRT 74/74 (baselines refreshed to capture the sub-threshold radius change) ✓ idempotent ✓ consume ✓
- [x] **T6 brand** `[done 2026-07-15]`: --brand theme-split (light 0.6/0.17/148 = 3.5:1 on card; dark phosphor 0.86/0.21/148) + CVD evidence recorded (Viénot sim, ΔL 0.10/0.28 + never-hue-alone rule in colors.mdx §Brand accent); chart-7 collision confirmed → olive primitives (hue 104); font tokens display/pixel(GeistPixelSquare)/serif=Newsreader + text-mono-label voice token; Lora OUT (token+next/font+binding); font-delivery contract in typography.mdx; 17 literals resolved (16 derived-or-primitive-ref via new SD preprocessor `derive-interaction-states`, overlay = documented exception); $descriptions incl. dark-omissions group note; compactOklch output canonicalization. Gate: lint/tc ✓ test 703 ✓ VRT 2 intended diffs reviewed → baselines updated, confirm 74/74 ✓ idempotent ✓ consume ✓

## Phase U — Upgrades (isolated; no behavior edits) `[done 2026-07-15]`

- [x] shadcn 4.13 + `registry validate` first step of registry:build (507 valid); ESLint 10.7 (ts-eslint ^8.64); react-day-picker 10.0.1 (zero adaptation needed — calendar pixels identical; API table shrank = only VRT diff, reviewed; ^9→^10 in mdx + registry item); @shadcn/react 0.2.1; lucide ^1.21.0 everywhere; style-dictionary 5.5.0 (byte-stable output); contrast-check: purple ghosts out + fail-closed missing tokens + muted-on-bg/card AA + 2.5:1 faint floor + 1.4.11 ring/primary/brand (90 checks green; border/track documented exemptions). Gate: lint/tc ✓ test 703 ✓ VRT reviewed→74/74 ✓ idempotent ✓ consume ✓

## Phase C — Consistency & structure (register-driven; no aliases) `[done 2026-07-15 — every sub-item below landed; per-item detail in the decisions log + .c2/.c10/.c11 agent summaries. Gate: lint/tc ✓ tests 793 ✓ VRT 34 intended diffs reviewed (API-table prop rows, TableHead 32px, Empty anatomy, rename pages, demo simplifications) → baselines updated, confirm 74/74 ✓ idempotent ✓ consume ✓]`

- [x] bubble ring-glow→border-tint; color-picker shadow-sm removed; chevron policy (ChevronsUpDown=combobox-style / ChevronDown=select-style) applied everywhere; dark:bg-input/30 scoping documented; TableHead→32px token; Card padding spec=16 in design.md
- [x] ALL 24 forwardRef comps → ref-as-prop (inventory: 02 §c4); missing leaf refs added (bubble/marker/message)
- [x] Flat-only exports: dotted namespaces removed from Card/Alert/Breadcrumb/EmptyState/Pagination + all internal usages updated
- [x] True-synonym props unified; two-axis comps keep variant+intent orthogonal (Badge)
- [x] Size props added: Input/Textarea/RadioGroupItem/OTPInput on the shared scale
- [x] DRY: button/badge/status-icon compose <Spinner>; notification-bell renders <Badge>
- [x] Renames DIRECT (no aliases): empty-state→empty (shadcn subcomponent shape), state-select→region-select + data split to sibling module; all previews/MDX/nav/registry/tests updated in the same change
- [x] field.tsx → shadcn Field shape (FieldGroup/FieldSet/FieldLegend/FieldContent)
- [x] IconButton RETAINED — re-documented as the aria-label-enforcing wrapper; docs previews use it for icon-only cases
- [x] A11y mediums (05 §3): copy-button live region · pagination aria-disabled keyboard block · image error alt · message-scroller reduced-motion scrollTo · text-edit aria-labelledby/id · emoji-picker roving tabindex · alert-dialog loading confirm · field-inline disabled/readonly/error API
- [x] Axe state-variant coverage: every implemented state (error/open/loading/disabled/checked/collapsed) axe-tested across the ~30 comps (inventory: 05 §c)
- [x] Ledger + component-matrix refreshed to post-sweep reality

## Phase X1 — Combobox & Command

- [x] Combobox primitive on Base UI (input+list+empty+loading+multi patterns), full tests/preview/MDX/registry — 18 flat exports, 17 tests, new VRT page baselined (75 pages now)
- [x] Command characterization tests FIRST (ranking, arrows, Home/End, loop, disabled-skip, Escape/focus-return, IME, async, controlled) as the spec — 11 pinned vs cmdk before the rebuild
- [x] Command rebuilt data-driven on Base UI (greenfield API; cmdk REMOVED from packages/ui + apps/docs); char. tests adapted deliberately where API changed — 9/11 semantically unchanged, 2 documented DEVIATIONs (disabled-not-skipped = verified Base UI limitation; controlled value = selected not highlighted). `useCommandFilteredItems` replaces `useCommandState`. Fixed real `aria-required-children` violation (Empty/Loading now siblings of the listbox). cmdk: zero source hits, out of lockfile.
- [x] CountrySelect/RegionSelect refactored onto Combobox (thin data-fed compositions) — public APIs unchanged; region-select tests passed 0-edit (18/18); country-select 3 DEVIATIONs (role button→combobox, consistency fix)
- Gate: lint ✓ typecheck ✓ 820/820 tests ✓ VRT 75/75 (4 intended diffs reviewed individually) ✓ registry:build idempotent (build-twice byte-identical) ✓ verify-consume real CLI 7/7 (incl. refactored country-select) + 508/508×2 simulated ✓. Details: `docs/plans/.x1-combobox-summary.md`, `.x1-command-summary.md`.

## Phase X2 — Attachment · Item · Resizable · Chart

- [x] Each: canonical comp · CVA+tokens only · axe/keyboard/VRT tests · preview · MDX · registry entry · gate. **Attachment premise corrected**: NO @shadcn/react Attachment primitive exists in any published version (verified via npm; 0.2.1 exports only ./message-scroller) → built self-owned presentational like the rest of the chat family; AGENTS.md exception language stays true, no broadening needed. Item (10 exports, useRender render-prop, listitem-role-drop fix for interactive rows, 20 tests) · Attachment (9 exports, composes IconButton/Spinner/Progress, live-region states, 22 tests) · Resizable (react-resizable-panels@4.12.2 verified against installed .d.ts — percent-string/pixel-number size API documented, keyboard resize free, 18 tests)
- [x] Chart: recharts@3.9 wrapper themed ONLY via chart-1..8 (ChartConfig.color is a token union; shadcn THEMES/ChartStyle injection + hex attribute-selector hacks DELETED), accessibilityLayer in every example, 11 tests. Ready for Phase S dashboard block.
- Gate: lint ✓ (0 warnings after TableHead scope fix) · typecheck ✓ · 891/891 tests ✓ · VRT 79/79 (4 new baselines individually reviewed) ✓ · registry:build idempotent (512 items) ✓ · verify-consume real CLI 7/7 + 512/512×2 ✓. **VRT infra fix shipped**: reduced-motion emulation in the spec (recharts' JS draw animation raced capture; recharts honors prefers-reduced-motion natively) + discovered maxDiffPixelRatio 0.01 masks sub-1% diffs on tall pages — broken chart baselines "passed" the threshold and survived --update-snapshots; deleted + regenerated fresh. Summaries: docs/plans/.x2-{item,attachment,resizable,chart}-summary.md

## Phase R — Responsive (inventory: 12 §a–e)

- [x] Viewport clamps: DropdownMenu/ContextMenu/SelectContent get `max-w-[var(--available-width)]`
- [x] min-w-0: filter-bar chip value span fixed; country-select's original site dissolved by the X1 Combobox rebuild (ComboboxValue got min-w-0+overflow-hidden); flex+truncate sweep zero offenders + `flex-truncate-conflict` lint rule ON
- [x] select.tsx line-clamp+flex conflict VERIFIED in compiled cascade (flex wins, clamp inert) then fixed (SelectValue → min-w-0 truncate; ComboboxValue keeps flex, gains shrink); tabs horizontal overflow-x-auto; pagination flex-wrap + max-sm:sr-only labels (icon-only below sm). Bonus: badge text-ellipsis, emoji-picker own viewport clamp, date-range presets stack below sm (rail → horizontal chip row)
- [x] Touch targets ≥24px via invisible hit-area — checkbox 24/26, radio 24/26, slider thumb 24, switch sm/default vertical expansion, FilterChip remove 20→24 REAL box (Chromium clips button ::before overflow when nested — verified; margins compensate, zero visual delta), data-list bare checkboxes fixed transitively. elementFromPoint boundary-probe tests on every target (121 tests in the 6 files)
- [x] truncated-text tap-to-toggle on (hover:none) devices (role=button/aria-expanded, measurement paused while expanded); tooltip hover-only contract documented; dvh sweep clean (sidebar svh intentional); safe-area env() insets for sonner offsets + sheet flush edges; settings-row → @container/settings-row queries
- [x] Mobile VRT project (375×812, isMobile+hasTouch, dsf 1) — 79 mobile baselines generated + spot-reviewed (pagination icon-only ✓ settings-row stacks ✓ date-picker fits ✓ tabs fit ✓); local baselines are gate evidence, Linux CI set stays MK's update_baselines action
- [x] **VRT infra fix (found by this phase's confirm run): AutoTypeTable row order was nondeterministic across builds** — ts-morph symbol enumeration isn't stable between program instances and the generator cache lives in .next (cleared per build); baselines from build N failed against build N+1 (4 pages). Fixed in mdx.tsx: generateTypeTable wrapper stamps own-vs-inherited via the transform hook (declaration path ∉ node_modules) and sorts own-first → required-first → alphabetical. Side benefit: every API table now leads with the component's OWN documented props instead of wherever TS happened to put them. Proven: full 158-shot suite passes against a fresh rebuild with wiped generator cache.
- Gate: lint ✓ typecheck ✓ 924/924 ✓ VRT 158/158 both lanes vs fresh rebuild ✓ idempotent ✓ verify-consume 7/7 + 512/512×2 ✓. Summaries: docs/plans/.r-touch-summary.md, .r-mobile-summary.md

## Phase M — Motion (mechanism matrix per CX-13; tweet link = MK TODO, see report)

- [x] Mechanisms shipped + documented in motion.mdx matrix: Base UI lifecycle (overlays/disclosures — already the reference pattern, verified) · keyed presence (motion-pop-in/enter-up utilities in tokens/utilities.css, remount-on-key) · replay APIs (useAnimationReplay/useShakeOnInvalid hook item = shake; lucide-animated CheckIcon startAnimation() = check draw; AnimatedNumber rAF tween = number)
- [x] v4 mapping: menus/modal/sheet/tooltip/hover-card/accordion/tabs-indicator verified already-compliant from T4 (no change needed) · icon swaps copy-button/auto-save/password-eye done (split-button verified n/a — static chevron, regression-tested) · badge pop notification-bell (auto, keyed on displayed count) + Badge/Marker/Message/Bubble opt-in `animateIn` · **success check stroke-draw DEVIATION**: lucide-react can't receive pathLength on its inner path (verified in its source) and inline SVG is icon-rule-banned — built-in success states use motion-pop-in; the draw-in lives in the sanctioned lucide-animated CheckIcon (pathLength variants + imperative handle); unused motion-check-draw CSS utility deleted (no dead code) · error shake input/otp/checkbox/radio auto-on-invalid-transition via MutationObserver (Base UI writes aria-invalid straight to DOM, never through props — agent-verified) + `shakeSignal` replay; Field = FieldError motion-enter-up + leaf self-shake (no double-shake) · SkeletonReveal export + documented pattern · AnimatedNumber shipped (rAF tween, token-var durations via getComputedStyle, Intl-formatted frames, live-region announces settled value only) · TextShimmer = existing `shimmer` utility (documented, no duplicate component) · input-clear n/a (no clear affordances exist)
- [x] EXCLUDED documented in motion.mdx: avatar hover lift, card 3D tilt, FAB morph; toast (sonner-owned) + animate-spin/pulse + animated-icon mirrors = documented exceptions
- [x] Reduced-motion safe (global reset + keyframe end = rest state; AnimatedNumber instant via matchMedia); replay/interruption/computed-style/focus-preservation tests (~90 new); **WebKit+Firefox smoke lane** (`pnpm --filter @vegastack/ui test:smoke`, 10 motion files × 3 engines = 450 tests green; day-one catch: WebKit Tab-skips-buttons broke a simulated-Tab test whose component was fine — rewritten to assert activation not tab-simulation); **raw-motion lint ON** (bans animate-[…], cubic-bezier(), linear(), duration-[…], ease-[…] in class strings)
- Gate: lint ✓ (0 warnings after exhaustive-deps destructure fix in 4 shake files) · typecheck ✓ · 1000/1000 tests · VRT 160/160 (80 pages × 2 lanes; 15 intended-diff pages re-baselined after review incl. new animated-number page + rewritten foundations/motion) · idempotent (514 items) ✓ · verify-consume real CLI 7/7 + 514/514×2 ✓ (verifier taught that registry:hook files are .ts). Summaries: docs/plans/.m-{swap,feedback,shake,number}-summary.md

## Phase S — App shell

- [x] Sidebar: mobile Sheet mode (openMobile state, --sidebar-width-mobile) + use-mobile registry:hook + SidebarRail (focusable button, beats shadcn's mouse-only handle) + SidebarInset + SidebarMenuSkeleton (deterministic width cycle) + sidebar_state cookie persistence (write-only, SSR pattern documented) + variant sidebar|floating|inset + collapsible icon|offcanvas|none + trigger ≥44px hit-area. All prior exports preserved; 34 tests. Harness finding: the vitest browser iframe is 414×896 (mobile!) — sidebar tests default matchMedia to desktop with a mobile opt-in.
- [x] AppShell/AppShellSidebar/AppShellHeader/AppShellContent/AppShellSkeleton: landmark trio + skip-link + @container/app-shell-content + router-focus split documented; PageHeader → real `<header>` + TruncatedText title (min-w-0 latent bug fixed); Breadcrumb → BreadcrumbSegment/BreadcrumbCollapsed/BreadcrumbTrail (static maxItems collapse; real links in a DropdownMenu; ResizeObserver variant evaluated + deferred with rationale). Audit deviation: AppShellContent takes `variant` directly instead of pairing SidebarInset (second-main-landmark + peer-selector conflicts — documented). AppShellSkeleton server-safe for loading.tsx. 19+37 tests.
- [x] dashboard-01 registry:block (7 files: page/loading/data.json + app-sidebar/stat-cards/dashboard-chart/recent-activity): AnimatedNumber stat cards + deltas, token-only AreaChart (Y-axis width fixed after VRT review), DataList activity with RelativeTime pinned to data.json's generatedAt (deterministic), Empty zero-state + per-region loading/error, loading.tsx = AppShellSkeleton. New /docs/blocks section + dashboard-01 page. 8 smoke tests.
- [x] View Transitions: verified next@16.2.9 mechanism (experimental.viewTransition, static-export-compatible); view-transition-name on shell header/sidebar; consumer config documented in mdx; reduced-motion gap CLOSED in tokens/base.css (::view-transition-* pseudo-elements need their own kill switch — the universal reset can't reach the snapshot layer).
- **Tooling hardened by this phase's failures**: registry-header/verify-headers skip .json payloads (a `//` header corrupted the block's data.json) and only treat `@placeholder/` targets as docs copy-ins (a consumer-app `app/dashboard/page.tsx` target briefly shipped a broken route INTO the docs app); Playwright test timeout 30→120s (sidebar page ~150k px post-demos); **webServer reuseExistingServer → false** (a stale reused server produced a baseline of the pre-rewrite sidebar page — second stale-server incident, now structurally impossible).
- Gate: lint ✓ typecheck ✓ 1065/1065 tests ✓ VRT 164/164 (82 pages × 2 lanes, fresh-build-guaranteed) ✓ idempotent (517 items incl. the block) ✓ verify-consume 517/517×2 + real CLI ✓. Summaries: docs/plans/.s-{sidebar,header,appshell,dashboard}-summary.md

## Phase D — Docs infra

- [x] Bloat SPIKE + fix: profiled out/ = **785MB**, 755MB of it API tables expanding every inherited DOM/aria prop (sidebar.html 16.4MB raw/512KB gz + 3× 8MB RSC copies). Chosen fix: own-props-only API tables (reuses Phase R's declaration-path classifier; "(no own props)" note row when a part adds nothing) — the shadcn/Radix docs convention. **After: 220MB (−72%), worst page 4.2MB raw/189KB gz; VRT suite wall-clock 4.4m→1.6m.** Remaining weight = search index + legitimate content.
- [x] SEO: metadataBase=design.vegastack.com (verified from detail/04) + `%s · VegaStack Design` template; sitemap.ts (91 pages via source.getPages()); robots.ts (disallows /r/*); per-page OG images (fumadocs-ui/og + ImageResponse, verified working under output:export — 91 branded PNGs at build; dark bg + phosphor accent)
- [x] public/_headers: /r/* CORS + short revalidating cache (it's the update channel), _next/static immutable. Historical note: this phase wired `TURBO_TOKEN`/`TURBO_TEAM` across all workflows; that wiring was removed 2026-07-23 because PR code inherited the token and signed caching also requires `TURBO_REMOTE_CACHE_SIGNATURE_KEY`. Future enablement is trusted-job-only with all three values.
- [x] copy-as-prompt (fetches the page's static .md sibling — fumadocs' own MarkdownCopyButton mechanism — composes name+install+markdown, toasts; gated on real registry components); responsive-frame toggle (375/768/full container widths, honest framing: container-query demos respond, viewport-media-query ones need a device); **per-preview theme DEFERRED per CX-15's sanctioned path** (Base UI Portal `container` isn't plumbed through any primitive; scoped .dark breaks portaled popups; iframe-isolation route = documented follow-up); PropsPlayground (generic config-driven API) + ButtonPlayground shipped in button.mdx
- [x] llms.txt/raw routes re-verified: generator derives from source.getPages() (nothing hardcoded); all renamed/new pages present in sitemap/llms.txt/llms.mdx, zero stale route refs
- Gate: lint ✓ typecheck ✓ 1065/1065 ✓ idempotent ✓ VRT full re-baseline (both lanes; API tables + global preview toolbar changed every page — button/playground/toolbar spot-reviewed) then 164/164 confirm vs fresh build ✓. Summaries: docs/plans/.d-{seo,preview}-summary.md

## Phase A — Authoring infra

- [x] skills/add-component v0.2.0: complete authoring contract — SSOT workflow, full token vocabulary (verified vs theme.css), motion mechanism matrix, naming/API canon (flat/ref-as-prop/intent/data-slot/§7.6), responsive + a11y checklists, file template + deliverables checklist (incl. the previously-missing VRT PAGES registration step), test conventions (style-mirror, elementFromPoint probes, smoke lane)
- [x] skills/design-audit v0.2.0: 1:1 mirror of design-lint's 20 enforced rule IDs + !important exceptions + VRT review discipline (delete-then-regen, fresh-build, individual diff review). **AST accessible-name rule shipped** in design-lint (TS compiler API JSX walk: `<Button size="icon*">` without aria-label/labelledby fails; IconButton stays the type-level path; probe-verified firing + exit-code ordering bug found/fixed — the summary block sat above the appended rule). **Uniformity closed**: docs previews are now design-linted permanently (27 violations fixed: 13 arbitrary→scale steps, 7 raw sizes→tokens, 5 transition pairs, 2 hex-shaped false positives reworded); motion-lint (raw-motion) already ON since M.
- [x] Ledger refresh: component-matrix rows 65–75 (X1/X2/S additions + the chat family gap closed), Command rebuild facts, Hooks/Blocks tables, §7.6 classification corrections (Item/AttachmentTrigger are useRender primitives), authoring-guide target-placeholder fix (@ui/), 2 pre-existing factual bugs corrected (content-lint description, stale VRT counts)
- Gate: lint ✓ (previews now included) typecheck ✓ 1065/1065 ✓ VRT 164/164 + marker baselines regenerated for its sub-threshold text change (delete-then-regen rule) ✓. Summaries: docs/plans/.a-{uniformity,skills}-summary.md

## Phase B — Brand/marketing layer

- [x] Marketing scope mechanism: `.vs-marketing` (tokens/utilities.css) re-binds the semantic vars .dark sets, class-scoped and independent of the page theme; `MarketingSurface` useRender wrapper applies it. Prototyped on the home hero first (agent self-review vs spec) then orchestrator-approved the full page look against audit 17 before baselining.
- [x] Primitives shipped (canonical+tests+preview+mdx each): SectionHeader (eyebrow/display/serif-em/description) · FigureFrame (sharp frame + 10px FIG mono annotation) · Terminal (phosphor prompt glyph, CopyButton, deterministic) · LogoRow (alpha-dimmed, hover restores; fictional wordmarks only) · Testimonial (serif-italic quote + mono attribution) · StaggeredTextReveal (CSS-only motion-enter-up + delay steps, reduced-motion static) · ParticleField (canvas, seeded mulberry32 PRNG — no Math.random, lazy, reduced-motion single frame, `data-drawn` paint signal, count cap) · MarketingSurface · Button `cta` variant (brand outline, radius-sharp, mono-uppercase)
- [x] Display prose recipes live on the home page: Geist display hero + Newsreader italic emphasis span + THE one Geist Pixel flourish (next/font module-scope in the page — --font-pixel isn't theme-bridged; documented)
- [x] docs home rebuilt as full dogfood (hero/logo-row/platform/terminal-install/FigureFrame live preview/testimonial/sharp CTA), reviewed against the spec (accent budget ~10 held) + VRT'd both lanes; design.md gained the Brand & Marketing usage rules (marker roles + budget, sharp scoping, mono voice + uppercase-mono-exclusive + 12px floor/14px cap, serif scope, Pixel single-use, alpha-ramp, scope mechanism). **D20 lint mechanics ON**: `uppercase-mono` rule (uppercase type ⇒ mono, ≤14px; content-transforms exempt; 3 command-heading offenders fixed by dropping uppercase).
- **Collision class eliminated**: the marketing Terminal collided with the `terminal` animated-icon item → ALL 439 icon items renamed `icon-<name>` (mirror script updated incl. its own collision guard; icons.mdx install refs fixed). **Registry-prune shipped**: shadcn build is additive-only — 965 stale JSONs had accumulated (old renames + the icon renames) and a stale old-name JSON re-stamped sources with dead identities; registry-header now prunes public/r to current items first.
- **2 cross-cutting bugs found by the agent's verification**: tailwind-merge dropped `text-mono-label` as a "color" (utils classGroup fix — same class as the T1 incident); Server Components can't CALL buttonVariants() from the client module (home CTAs recomposed via `<Button render={<Link/>}>`).
- Gate: lint ✓ typecheck ✓ 1120/1120 (91 files) ✓ VRT 182/182 (91 pages × 2 lanes; home + button + 8 new pages reviewed then baselined) ✓ idempotent (525 items) ✓ verify-consume 525/525×2 + real CLI ✓. Summary: docs/plans/.b-brand-summary.md

## Phase Z — Finalization

- [x] design.md + AGENTS.md: every delta from this program reflected (D1–D20 as amended, CX dispositions, greenfield amendments) — dedicated sync agent, corrections listed in the decisions log §Z (disabled 50%, soft-only destructive, card padding, 240px sidebar, radius steps, focus-model unification, Brand rules, Numbers). Skills drift closed too (design-audit rule 21, design-system Tokens modernization).
- [x] Full gate + registry idempotency + verify-consume + fresh VRT reviewed — lint 10/10 · typecheck 10/10 · unit 1120/1120 · smoke 450/450×3 engines · build-twice byte-identical (525) · consume real-CLI 7/7 + 525/525×2 · **VRT 182/182** (91 pages × 2 lanes — the plan's "148" predates the mobile lane + marketing/home/blocks pages; superseded, logged in decisions log §Z).
- [x] Changeset authored (`.changeset/2026-07-system-overhaul.md`, tokens/ui/utils minor, `changeset status` valid); **STOPPED — MK triggers publish/deploy** (full MK-only list in `2026-07-execution-report.md`).
