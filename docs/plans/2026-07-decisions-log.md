# Decisions Log — Overhaul v5 Autonomous Execution (Fable 5, started 2026-07-15)

One line per micro-decision or proactive improvement, per the autonomy contract in
`HANDOFF-FABLE5.md`. Format: `[Phase] decision — rationale — files touched`.

<!-- entries appended chronologically during the run -->

## Release prep (2026-07-18)

- **[REL] First release = 0.1.0 via the publish-direct path** — the 7 accumulated changesets were squashed; the initial "zero versions + one minor changeset" plan was EMPIRICALLY rejected: changesets' `linked` group bumps the tokens/preset/icons trio from 0.0.0 to **1.0.0** (tested, then reverted). Shipped state instead: all five packages at `0.1.0`, hand-seeded `CHANGELOG.md` 0.1.0 entries per package, **zero pending changesets** — on first push to main the changesets action skips versioning and runs `changeset publish` directly, publishing 0.1.0 as-is. Registry re-stamped: 525 items + 532 headers all at 0.1.0. Future releases use the normal changeset→Version-PR flow (from 0.1.0 the linked group bumps sanely).

## Post-run: MK exhaustive component sweep (go-dark, 2026-07-18)

- **[SW+] color-picker check ink RE-FIX (MK follow-up)** — the sweep's `mix-blend-difference` check was itself wrong: difference-against-white renders the swatch's INVERSE, i.e. a different murky hue per swatch (dark-ish on blue — MK's report), not a legible neutral. Now LUMINANCE-AWARE via CSS relative color syntax: `oklch(from var(--swatch-color) clamp(0,(0.65 - l)*999,1) 0 h)` — a step function yielding pure white ink on dark swatches and pure black on light ones, derived from each swatch's own resolved token in both themes. Verified: white-on-blue (4× zoom), black on the brightened dark-theme yellow (computed oklch(0 0 …)). Miss cause recorded: verification had asserted presence + reasoned about the blend instead of judging rendered ink on both branches. CLASS-SWEPT: no other `mix-blend`/invert-derived ink in the registry; tooltip's inverted surface is the deliberate bg-foreground/text-background token pair; the trigger chip draws no ink on its dynamic fill.

- **[SW] Mandate executed**: all 83 components swept SEQUENTIALLY with screenshot evidence — a capture harness (`.sweep-capture.mjs`, since removed) rendered EVERY demo block per page as labeled light+dark contact sheets (166 sheets + targeted zooms); each component reviewed personally, fixed at component level, ledger'd in `docs/plans/.sweep-ledger.md` (per-component evidence notes).
- **[SW] MK-reported bugs, root causes**: (1) loading spinner INVISIBLE on primary buttons in light — Spinner's base bakes `text-muted-foreground` (its JSDoc even denied it) and `size="inherit"` kept it → inherit now carries `text-current` (fixes button/badge/attachment/command/combobox in one move; verified: spinner ink === host ink). (2) icon-vs-label proportion — text-bearing Button sizes (default/lg) used the standalone 16px `--icon-default` next to 14px labels; now `--icon-inline` (14px), completing the proportional ladder xs/sm already followed; icon-only squares keep 16. Font weight KEPT at the documented 500 (design.md: labels=500) — flag if still heavy post-rebalance. (3) menu shortcut ink on destructive rows — `ContextMenu/DropdownMenuShortcut` flipped to neutral accent on highlight; now destructive-tinted at rest AND highlight via a compound group-data selector (probe-verified; deterministic by specificity).
- **[SW] New bugs found by the sweep**: icon-button docs page HUNG the dev server 25-60s→500 (build unaffected) — Fumadocs Story's dev-time control analyzer looping on `IconButtonProps`' Omit-over-Base-UI type surface; fixed with a narrow hand-typed client story host; all 28 story pages probed fast after. radio-group's horizontal-orientation demo stacked vertically (Field w-full defeats the row — vA finding MISSED in FX3 triage; now w-auto wrappers, zoom-verified). Label's `required` demo demonstrated an invisible attribute — demo now styles the `data-required` hook visibly ("(required)" suffix), no-asterisk doctrine intact. alert-dialog API placeholder noise collapsed (missed page); systemic: **89 part headings inserted across 22 pages** that had unheaded AutoTypeTables. Remote-image determinism: avatar/item previews were on pravatar.cc, markdown-view on unsplash → local `/preview/avatar-{1,2,3}.svg` fixtures. Stale "ring" wording in text-edit anatomy fixed.
- **[SW] Dev-ops finds**: dependency upgrades under a RUNNING dev server wedge the loader ("callback already called" / "ReadableStream already closed") — always clean-restart after installs; a `.next/dev/types` routes.d.ts can be corrupted by dev-server/typegen races (delete + rerun).
- **[SW] Dependency audit**: patch/minors upgraded across the workspace (tailwind 4.3.2, next 16.2.10, playwright 1.61.1, vitest 4.1.10, tiptap 3.27.4, lucide 1.24, motion 12.42.2, RHF 7.81, prettier 3.9.5, thesvg 3.2.6, turbo 2.10.5, wrangler 4.111, globals 17, changesets, postcss) + react-markdown 10 (major, verified drop-in). Held: typescript@7 (native-compiler major vs locked TS6), @types/node (tracks locked Node 24).
- **[SW] Gate**: lint 10/10 · typecheck 10/10 · 1144/1144 unit · 453/453 smoke ×3 engines · registry rebuilt + consume PASSED · VRT full delete-then-regen 182/182 + confirm 182/182.

## Post-run: fumadocs latest + feature adoption (MK-directed, 2026-07-17)

- **[FD] Upgraded** fumadocs-ui/core 16.10.5→16.11.5, mdx 15.0.12→15.2.0, typescript 5.2.6→5.3.0, twoslash 3.2.0→3.3.0. One post-upgrade trap: the RUNNING dev server kept the old loader in memory → every mdx errored "callback already called"; clean restart + `.next` purge resolved (not a code issue).
- **[FD] remark-npm adopted** — 87 install blocks across 85 pages converted from single `bash pnpm-dlx snippets to `npm blocks with the canonical `npx` form; the (default-enabled) plugin generates npm/pnpm/yarn/bun tabs at build time. Verified live: pnpm tab renders `pnpm dlx shadcn@latest add …`. (Gotcha for tests: the tabs don't respond to synthetic JS `.click()` — real pointer events only.)
- **[FD] Validate Links adopted** (`next-validate-link`) — `scripts/lint-links.ts` (bootstrap registering `fumadocs-mdx/node`'s loader BEFORE importing the source — plain tsx can't parse the `.source` entry's direct `.mdx` imports) + `lint-links-impl.ts`; validates all 99 pages incl. `#fragment` links against real TOCs; wired into the docs `lint` script; fail-closed PROVEN by negative probe (planted broken link + bad fragment → exit 1, both reported).
- **[FD] Fumadocs Story adopted** (`@fumadocs/story` 1.2.0) — `createNextStory()` wrapper in next.config, CSS preset, `lib/story.ts` factory; stories in `components/stories/*.story.tsx`, surfaced as an "## Explorer" section (the exhaustive auto-generated prop surface) COMPLEMENTING the curated PropsPlayground (copyable JSX), not replacing it. Pilot (badge) verified: enum controls correctly derived from CVA union types, live rendering, variants picker, and — critically — `output:'export'` static build passes (306 pages). Rollout scoped to SINGLE-ROOT components; compound-anatomy components (dialog/select/…) can't render from bare props and a wrapper would only duplicate the playground — documented skip.
- **[FD] Last-modified adopted** — `fumadocs-mdx/plugins/last-modified` (git-derived) in source.config + `PageLastUpdate` in the page header row (the `layouts/docs/page` entry exposes it as a component, not the `lastUpdate` prop the top-level wrapper has).
- **[FD] GithubInfo trialled and REVERTED** — it fetches the repo from the GitHub API at render time; while the repo is private/unpushed the 404 CRASHED every docs page (caught by a story agent mid-flight). Bare `githubUrl` icon restored; re-add once public (comment in layout.shared.tsx). **Deferred with reasons**: Banner (nothing to announce yet), ImageZoom (no real screenshots in docs yet), Feedback + Ask AI (need a POST backend — incompatible with static export; revisit with a small CF Worker), search/OG (already on the correct static-export setups).
- **[FD] Story rollout: 27 components** (13+14 via agents + badge pilot), each an `## Explorer` section with 1–3 preset variants. **Server-safe trap found+solved**: 7 components (spinner/kbd/status-icon/skeleton/textarea/label/progress-indicator) are deliberately server-safe (no `'use client'`, locked rule) and crashed Story's RSC boundary ("Functions cannot be passed to Client Components") — fixed with per-story `*.client.ts` re-export shims (`'use client'` re-export makes the import a client reference), canonical untouched. Compound-anatomy components skipped with reasons (can't render from bare props; a wrapper would duplicate the curated playground).
- **[FD] Gate**: lint 10/10 (incl. the new lint:links) · typecheck 10/10 · unit 1144/1144 · static export 306/306 pages · VRT full delete-then-regen 182/182 + confirm 182/182 (every page legitimately changed: PM tabs, Explorer sections, last-updated stamp).

## Post-run: MK bug reports + full visual audit remediation (2026-07-17)

- **[FX] DatePicker scroll-to-top on open (MK report)** — our `CalendarDayButton` mirror of rdp's DayButton called `.focus()` (no `preventScroll`) in its `modifiers.focused` effect; with the DatePicker's `autoFocus` default the effect fires on first paint while the PORTALED popup is still unpositioned at the document top-left → the browser scrolls the page to it. Fix: `focus({ preventScroll: true })` (popup is fixed-positioned — suppressing the scroll loses nothing; keyboard focus still lands on the day, asserted). Regression test: below-the-fold trigger opens with scrollY unchanged AND activeElement = calendar-day. **Class sweep**: grepped every programmatic `.focus()`/`scrollIntoView` in the registry — color/emoji-picker roving focus fires only from keydown inside a positioned popup (and the emoji grid NEEDS scroll-on-focus for its internal scroll container — preventScroll there would break it), field-inline/text-edit are user-initiated on visible elements; then EMPIRICALLY drove all 16 overlay components' below-fold triggers via script: 0px scroll delta on every one. — `date-picker.tsx`, `date-picker.test.tsx`.

- **[FX] MK-reported bugs, root causes**: (1) double toast = a second `<Toaster/>` mounted in the toast page's options demo — Sonner BROADCASTS to every mounted toaster; demo rewritten to per-toast `toast(…, { position })`, mdx now warns mount-once. (2) Toasts made dismissable-by-default (`closeButton = true`), X on top-RIGHT — two CSS traps en route: Sonner declares the close-button side vars at `[data-sonner-toaster][dir]` attribute-specificity (a toaster-root utility class loses) → vars moved onto each toast's own class where direct declaration beats inheritance; and `unset` on a custom property means INHERIT (custom props are inherited) → `auto`. (3) Combobox popup: new `ComboboxPopupInput` part (sticky full-bleed header + Search icon + hairline, CommandInput anatomy) replaces the bordered standalone input in country/region-select; all four always-mounted Empty/Status rows got `empty:hidden` (Base UI keeps them mounted with cleared children = phantom py-6 gap). (3b) MK follow-up: the header's `focus-within:border-ring/…` tint painted the hairline near-white in dark — the popup input is AUTO-focused so the tint was always-on and meaningless; removed (CommandInput keeps its tint: user-initiated focus). (4) markdown-view task-list checkboxes now render the real `Checkbox` (inert, full-contrast, GitHub parity; `contains-task-list` drops bullets; `@vegastack/checkbox` registryDependency added). (5) `RelativeTime` gained `unitStyle: long|short|narrow` → "2h ago"; playground + regression test.
- **[FX] Audit fan-out**: 5 vision agents read all 182 fresh baselines band-by-band + 16 scripted popup-open captures → ~65 findings; triaged (2 stale mid-fix, a few documented-as-deliberate, rest fixed by 4 parallel fix agents with disjoint file ownership; single `registry:build` at integration).
- **[FX] Canonical bugs fixed**: SidebarMenuBadge had NO vertical anchor (floated below its row; upstream shadcn's per-size top-* was dropped — replaced with size-agnostic `top-1/2 -translate-y-1/2`); Button loading with `icon*` sizes rendered spinner+icon overflowing the square (spinner now REPLACES the icon child; found+fixed a latent spread-order bug where Button clobbered consumer `data-loading`); SplitButton loading no longer half-dims the chevron (focusableWhenDisabled while loading; native disabled still dims both); `disabled:` variants never matched Base UI's `data-disabled` on accordion/collapsible/tabs-trigger/slider-thumb (systemic sweep: those 4 fixed, 15 components verified-correct native paths); date-picker dropdown caption chevrons wrapped under labels (rdp v10 nests the chevron INSIDE caption_label — `[&>span]:inline-flex` fix); color-picker Yellow mapped to the neutral `--color-accent` (→ `--color-chart-7`, duplicate Lime removed, palette = 12, uniqueness test) and the selected-swatch disc covered 80% of the color (→ check drawn in `mix-blend-difference` ink); Empty `surface="card"` was invisible on card canvases (+`border-border`); AttachmentMedia's fixed `size-(--size-lg)` height defeated vertical `aspect-square` (+`h-auto`); notification-bell "99+" grew inward over the icon (re-anchored `left-full -translate-x-3`, 1-digit pixel-identical); PageHeader actions crushed the title at 375 (flex-wrap + basis-48 minimum); AppShellSkeleton kept the desktop rail at mobile (hidden md:flex — skeleton-matches-content); Sheet unified to the Dialog rhythm (p-5, close top-3/right-3); chart axis ticks + AnimatedNumber now `font-mono` (numerals canon); tabs list + terminal body got the ships-in-repo `scroll-fade-x` scroll affordance; pagination links `tabular-nums`; filter-bar active chips keep the muted-label/emphasized-value hierarchy; emoji grid `justify-items-center`; figure-frame number `whitespace-nowrap`; data-list end-aligned sort header dropped `flex-row-reverse` (baseline lift); `ShakeSignal` named type ends the "unknown" API rows.
- **[FX] Provider mirror drift caught**: `packages/ui/src/provider/toaster.tsx` still carried pre-overhaul raw `/30`/`/10` alphas and lacked the closeButton work — `packages/ui/src` is NOT in the package's design-lint root (`registry` only), so gates never saw it. Re-mirrored wholesale from canonical. FOLLOW-UP for MK: consider adding `src` to the ui package's design-lint roots.
- **[FX] Docs/VRT infra**: message-scroller's `content-visibility:auto` made every below-fold transcript capture EMPTY (void VRT coverage) → capture-time `content-visibility: visible` override in the spec; image demos switched from live unsplash + DNS-failure error to a local fixture (`public/preview/landscape.svg`) + same-origin 404 (deterministic); sidebar previews pass `mobileBreakpoint={1}` so the 375 lane renders real rails (was: 10 empty boxes); resizable's height-on-group teaching pattern corrected in all four surfaces (v4 sets inline height:100% — height belongs on a wrapper); API-reference sections across 13 pages gained per-part headings with no-own-prop parts collapsed to prose (the "(no own props)?" stray marker fixed via `required: true` on the synthetic row); stale no-rings-era claims corrected (textarea/otp/bubble/popover/input focus text, tooltip default 6→8, opacity-50 → token citations, button "16"→15 variants).

## Post-run: preview fullscreen + playground rollout (MK-directed, 2026-07-16)

- **[PG] Fullscreen preview = CSS overlay, NOT native `requestFullscreen()`** — Base UI portals popups to `document.body`; a natively-fullscreened element would hide every Dialog/Select/Tooltip demo. Overlay: `fixed inset-0 z-(--z-overlay) bg-background`, Esc (skipped when a demo popup already consumed it via `defaultPrevented`), close-button focus on entry, scroll-lock, composes with the width toggle (fullscreen+mobile = device frame). Verified live: overlay covers viewport, scroll locks/restores, Esc exits. — `preview-controls.tsx`, `component-preview.tsx`.
- **[PG] 45 PropsPlaygrounds (44 new + Button expanded), 5 parallel agents, disjoint files** — classification of all 83 ui components in `2026-07-preview-playgrounds.md` (38 skips reasoned: menus per-item props, marketing scope, single-trivial-axis, composition-scale). Orchestrator wired all 45 registrations in `mdx.tsx`; placement normalized site-wide to button.mdx's real precedent (after `## Usage`) after two agents followed the instruction text instead of the file.
- **[PG] Agent premise corrections (source beat my extraction)**: TruncatedText has no `mono` prop (it's TableCellText's); SplitButton's `destructive` is a per-action prop, not root; Resizable's axis prop is `orientation` (not `direction`) with unitless string sizes; `pagination` link default size is `icon`; `empty`'s variant/intent live on EmptyMedia; attachment's primary axis is `state` (added); OTPInput `length` is numeric; Field has no `required` prop → `disabled`; extra real axes covered beyond hints: input/textarea/otp/radio-item `size`, progress-indicator `shape`, skeleton `count`, separator `decorative`.
- **[PG] Button variant truth = 15** (4 `*-outline` compound intents + `cta`) — my earlier SKILL.md "11" correction was itself wrong (regex missed quoted keys); fixed to 15 and the playground now covers all 15×8 incl. icon-size child/aria-label switching in render + toCode.
- **[PG] `motion.mdx` gained "Motion at a glance"** — the plain-language inventory table (what each animation looks like / where / how to trigger), positioned before the Mechanism matrix.
- **[PG] Gate**: lint 10/10 · typecheck 10/10 · unit 1120/1120 · playgrounds live-verified in dev (badge JSX regeneration on toggle, fullscreen behavior probe) · VRT full delete-then-regen + confirm run (all pages legitimately changed: toolbar button on every preview + 45 new sections).

## Phase Z

- **[Z] design.md + AGENTS.md synced by dedicated audit agent** — every program delta verified in-text: disabled 45%→50% (`--opacity-dim`), destructive = soft-only (solid destructive removed from prose), card padding corrected (16 default / 12 compact — prose had three conflicting values), sidebar 256→240px (`--sidebar-width`), radius steps list matches shipped `xs/sm/md/lg/sharp` (xl removed), focus model unified to no-rings (native outline + text-entry border tint — prose contradicted itself pre-sync), Brand section (phosphor `--brand`, mono voice, marker-role budget), engine-vs-primitive distinction for message-scroller, Numbers section refreshed (525 items / 75 matrix rows / 91×2 VRT). — `design.md`, `AGENTS.md`, docs/plans/.z-docs-summary.md.
- **[Z] Skills drift closed (zdocx flags)** — design-audit SKILL.md gained rule 21 `uppercase-mono` (D20 mirror, keeps the 1:1 lint-rule promise); design-system SKILL.md Tokens section modernized (raw `/NN` alpha → `bg-{family}-subtle`/`--alpha-*`, ring-ring/rounded-xl/arbitrary-duration removed, sharp radius + spring ease + motion-* utilities added, no-rings focus model in the Don't bullet). — `skills/design-audit/SKILL.md`, `skills/design-system/SKILL.md`.
- **[Z] Changeset authored, NOT versioned/published** — `.changeset/2026-07-system-overhaul.md`: minor bumps for tokens/ui/utils with the full overhaul changelog; validated via `npx changeset status`. Versioning/publish is release.yml on merge to main = MK's trigger.
- **[Z] Checklist's "148 baselines" figure superseded** — the plan was written before the mobile VRT lane (Phase R) and the marketing/home/blocks pages (Phases S/B) landed; the shipped suite is **91 pages × 2 lanes = 182 baselines**, all green on the final run. Logged rather than silently edited.
- **[Z] FINAL GATE (all green, 2026-07-16)** — lint 10/10 · typecheck 10/10 · unit 1120/1120 (91 files) · smoke 450/450 (×3 engines) · registry build-twice byte-identical (525 items) · verify-consume real-CLI 7/7 + simulated 525/525×2 · VRT 182/182 fresh run. Evidence + MK-only actions in `2026-07-execution-report.md`.

## Phase P

- **[P] P0-02 mechanism: popup surfaces keep the native outline instead of a bespoke ring** — removed `outline-none` from the focusable Popup elements of popover/hover-card/dialog/alert-dialog/sheet so the centralized `base.css :focus-visible` 2px outline is the compensating indicator (single-source focus per design.md §Accessibility; keyboard-only, mouse shows nothing). Viewports (non-focusable containers) keep `outline-none`, exemption list re-scoped with rationale. — `popover.tsx`, `hover-card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `sheet.tsx`, `tooling/design-lint.mjs`.
- **[P] Lint FOCUS_AFFORDANCE extended with `focus:border-`** — the design.md-sanctioned text-entry border-tint pattern (Input/Textarea/OTP darken `border` to `ring/70` on `focus`, deliberately not `focus-visible`) was not recognized, producing false-positive violations on textarea/otp-input (pre-existing baseline lint failure). — `tooling/design-lint.mjs`.
- **[P] P0-05 layout: horizontal Field description renders `basis-full`** — wraps to its own full-width line under the control+label row, mirroring FieldError's existing horizontal treatment. — `field.tsx`.
- **[P] IMPROVEMENT: `verify-registry-deps.mjs` added + wired into `registry:build`** — fail-closed cross-check of declared `registryDependencies` vs actual `@/components/ui/*` imports (both phantom and missing deps); would have caught P0-07 at authoring time. Verified green across all 507 items. — `tooling/verify-registry-deps.mjs`, root `package.json`.
- **[P] P0-07 scope: removed BOTH `@vegastack/separator` and `@vegastack/button`** from notification-bell's registryDependencies (register names separator only) — the component imports only IconButton, and icon-button's own item already declares `@vegastack/button`, so shadcn's transitive resolution keeps the install complete. — `packages/ui/registry.json`.

## Phase X1

- **[X1] Characterization spec captured FIRST** (CX-4): `command.characterization.test.tsx` — 11 behavioral pins (filtering/empty/arrows/Home/End/loop/disabled-skip/Enter-onSelect/controlled/async/IME) verified green against cmdk before any rebuild; it is the acceptance spec for the Base UI rebuild, with per-line `// DEVIATION:` discipline for deliberate API changes.
- **[X1] Combobox = Base UI 1.6 Combobox wrapped in the house idiom** (18 flat exports, tests/preview/MDX/registry/nav/VRT page). Key agent-verified API decisions (full writeup docs/plans/.x1-combobox-summary.md): non-modal by default (Base UI's modal clip-path hole breaks growing chip rows), Content does NOT auto-wrap List (listbox can't contain the Empty status role), filtering requires the function-child Collection pattern, `useComboboxFilteredItems` re-exported for grouped filtering, multiple-mode Value goes aria-hidden while open. vitest optimizeDeps gained the combobox subpath (duplicate-React guard).
- **[X1] Command = `<Combobox.Root inline open modal={false}>`** — Base UI's documented inline mode IS the palette shape (list in normal flow, never floating). Same 10 export names; `useCommandFilteredItems` replaces cmdk's `useCommandState` (dropped — no equivalent store). API is now DATA-DRIVEN (`items` mandatory): verified in Base UI source that filtering/Empty only work off a root `items` array — no static-children fallback exists. — `command.tsx`, docs/plans/.x1-command-summary.md.
- **[X1] Command a11y fix landed for real**: `CommandEmpty`/`CommandLoading` (role=status) moved from inside the listbox to siblings of `CommandList` — the old cmdk structure tripped `aria-required-children` and the test suppressed it; suppression now deleted. `CommandInput` hardcodes `aria-expanded="true"` (verified Base UI gap: inline mode never sets it; Command is architecturally always-expanded).
- **[X1] Characterization outcome: 9/11 pins unchanged, 2 documented DEVIATIONs** — (1) disabled items are reachable by arrow keys (Base UI hardcodes `disabledIndices: []`, verified in source; items stay inert on activation — an upstream limitation documented in JSDoc + mdx States + keyboard table, NOT a design choice); (2) `value`/`onValueChange` now maps to Base UI's native SELECTED axis, not the highlight — Base UI separates highlighted (`data-highlighted`) from selected (`aria-selected`) where cmdk conflated them; greenfield API takes the idiomatic mapping. `autoHighlight="always"` (cast past the too-narrow public type — runtime honors it, verified) reproduces cmdk's first-item-pre-highlighted default.
- **[X1] cmdk prop replacements documented + tested**: `keywords` → item data + custom `filter`; `forceMount` → static sibling outside the filtered result; `shouldFilter={false}` → Base UI's `filter={null}`. Each has a test in command.test.tsx. IME safety is free (Base UI's Enter handler checks keyCode 229).
- **[X1] CountrySelect/RegionSelect = thin Combobox compositions, public APIs unchanged** — Base UI "Select-style" pattern (search input inside the popup; trigger role flips to `combobox`/`aria-haspopup="dialog"`). Trigger label deliberately does NOT use our `ComboboxTrigger` (it wraps children in always-`aria-hidden` `Combobox.Icon`, which would strip the flag+name from the a11y tree — verified in source); both compose Base UI's `Combobox.Trigger` directly with `render={<Button/>}`. Region's re-select-to-clear toggle kept via per-item `onClick` with root `onValueChange` deliberately unwired (single change signal). region-select tests passed 0-edit (18/18); country-select had 3 role-rename DEVIATIONs (button→combobox — consistency fix, region already used it).
- **[X1] cmdk REMOVED everywhere** — package.jsons (ui + docs), vitest optimizeDeps, registry.json deps (command/country-select/region-select now declare `@base-ui/react` + `@vegastack/combobox` graph), lockfile clean, zero source hits. Agent caught one real docs bug during spot-typecheck: `defaultInputValue` misplaced on CommandInput instead of Command.
- **[X1] Gate evidence**: lint ✓ typecheck ✓ 820/820 ✓ VRT 75/75 after 4 individually-reviewed intended diffs (command page fully rewritten; country/region +26px MDX growth; combobox = new baseline) ✓ registry:build build-twice byte-identical ✓ verify-consume real CLI 7/7 incl. refactored country-select + 508/508×2 simulated ✓.

## Phase X2

- **[X2] Attachment premise correction (externally stale plan input)**: the audit (04, P2-44) anticipated a headless `@shadcn/react` Attachment primitive from shadcn's June-2026 chat-components family; VERIFIED via npm today that no published version ships it — 0.2.1 (latest) exports only `./message-scroller`. Attachment is therefore built SELF-OWNED presentational, consistent with the already-self-owned chat family (message/bubble/marker). Consequence: the AGENTS.md "ONLY non-Base-UI primitive" exception language stays TRUE and needs NO broadening — the P2-44/X2 wording action dissolves; noted for the Phase Z AGENTS.md pass instead.
- **[X2] New component engines sanctioned by plan v5 itself**: `react-resizable-panels@^4` (Resizable) + `recharts@^3.8.0` (Chart) — same dependency class as the existing tiptap/sonner engines (renderers/behavior engines, not Base-UI-category headless primitives), explicitly named by the plan. Installed centrally in packages/ui + apps/docs; vitest optimizeDeps extended (single-React-copy guard). Phase Z documents the engine-vs-primitive distinction in AGENTS.md.
- **[X2] Chart theming = tokens only**: shadcn's THEMES/ChartStyle light-dark-literal injection mechanism is DELETED in our adaptation — ChartConfig colors resolve exclusively to `var(--chart-1..8)` (already theme-split in the token layer); per-config hex is banned (design-lint would reject it anyway).
- **[X2] Parallel-build discipline**: 4 agents own disjoint file sets (canonical+test+preview+mdx each); ALL shared files (registry.json, meta.json, preview/index.tsx, vrt spec, package.jsons, vitest.config) are orchestrator-wired afterward from agent-reported snippets — the concurrent-edit failure class is structurally excluded.
- **[X2] Item: interactive rows drop `role="listitem"`** — forcing listitem onto a `render`-composed `<a>`/`<button>` overrides the native link/button role (no ARIA dual-role), silently hiding it from AT; agent caught it via getByRole failures. Documented (JSDoc + mdx): role=list groups are for non-interactive/mixed rows; purely-interactive rows compose standalone. — `item.tsx`.
- **[X2] Attachment: no `AttachmentAction` wrapper** — the shadcn reference's action wrapper is redundant with `IconButton` (which already type-enforces the accessible name); demos compose IconButton directly. `AttachmentProgress` wraps Progress; uploading state auto-overlays `Spinner` in the media slot; live announcements opt-in via `AttachmentDescription live` (auto-save-input's pattern).
- **[X2] Resizable: react-resizable-panels v4 API verified against installed .d.ts, not folklore** — percent sizes are unitless STRINGS ("30"), numbers are PIXELS (30); ref forwards to `elementRef` (lib isn't forwardRef-wrapped); group `disabled` ≠ handle `aria-disabled` (two tests pin the split); handle hit-target box-model math rebuilt (reference's doesn't center horizontally). Keyboard resize ships free.
- **[X2] VRT infra: reduced-motion emulation added to the spec** — recharts' JS-driven draw animation (first JS animation in the system) races `toHaveScreenshot` (which only disables CSS animation). recharts 3.9 honors prefers-reduced-motion natively, so `page.emulateMedia({reducedMotion:'reduce'})` renders settled end states AND pins the a11y-correct reduced-motion rendering — the right posture before Phase M ships more JS animation. Verified: 75 pre-existing baselines byte-stable under the emulation. — `apps/docs/vrt/components.spec.ts`.
- **[X2] VRT threshold-masking incident (sharpens the earlier T-phase note for MK)**: the chart page's FIRST auto-written baseline captured mid-animation charts (dots, no lines); `--update-snapshots` then REFUSED to fix it because maxDiffPixelRatio 0.01 on a 54k-px-tall page ≈ 700k pixels of slack — the missing lines (~10k px) "matched". Diagnosed via a live-DOM probe (paths present, strokes resolved) + bisect (plain screenshot vs toHaveScreenshot both correct on fresh baselines). Rule adopted: NEW pages get baselines via delete-then-regenerate, never trust update-snapshots to fix a suspect baseline. Recommendation for MK (Phase Z report): consider per-element or lower-ratio thresholds for tall pages.
- **[X2] TableHead `scope` bug found via lint warning**: `scope = 'col'` was destructured but never applied to the `<th>` — the intended a11y default silently dropped. Fixed (`scope={scope}`), 6/6 table tests green, zero visual delta. — `table.tsx`.
- **[X2] Gate evidence**: lint ✓ (0 warnings) · typecheck ✓ · 891/891 (78 files; +71 = the four new suites) · VRT 79/79 with 4 individually-reviewed new baselines · registry:build build-twice byte-identical (512 items) · verify-consume real CLI 7/7 + 512/512×2 simulated.

## Phase R

- **[R] SelectValue/ComboboxValue `line-clamp-1 flex` conflict VERIFIED then fixed** (audit's verify-before-fix): compiled-cascade check showed `.flex` wins (line-clamp inert → clip-no-ellipsis, no shrink). SelectValue (plain-text dominant, checked demos) → `min-w-0 truncate text-left` with a JSDoc recipe for composite children; ComboboxValue (genuinely composite: flag+name) keeps flex, gains `min-w-0 overflow-hidden` (text spans carry their own truncate). select/combobox/country/region VRT pages byte-identical after — zero visual cost.
- **[R] `flex-truncate-conflict` lint rule ON** (design-lint): flex/inline-flex + truncate/line-clamp-* in one class literal is banned; correct pattern documented in the rule message. Zero offenders at adoption.
- **[R] Menus/select get `max-w-[var(--available-width)]`** (dropdown/context/select) — Base UI exposes the var; popover/hover-card already proved the pattern.
- **[R] Tabs horizontal overflow**: TabsList gains `max-w-full overflow-x-auto` (horizontal orientation only). Pagination: `flex-wrap` on content + Previous/Next text `max-sm:sr-only` (icon-only below sm; aria-labels already name the controls). Badge: `text-ellipsis` added (consumer `max-w-*` caps now elide; costless at default w-fit). Emoji-picker popup: own `max-w-[calc(100vw-var(--spacing)*8)]` clamp (was relying on the inherited clamp exactly matching 320px). DateRange presets: `max-sm:flex-col` + PresetRail becomes a horizontal scrollable chip row below sm (side-by-side rail+calendar exceeded the popup clamp on phones).
- **[R] Touch targets ≥24px floor via invisible hit-areas, zero visual change** (agent-verified with elementFromPoint boundary probes + style-mirror technique since the unit harness is CSS-less): checkbox 16/14→24/26 · radio same (+data-size parity) · slider thumb 16→24 · switch sm/default get vertical-only expansion (lg already compliant). **Real Chromium discovery**: native `<button>` (Preflight `appearance:button`) clips overflowing `::before` hit content when nested — getComputedStyle lies, elementFromPoint tells the truth; FilterChip's remove × therefore grew its REAL box 20→24 with compensating margins (glyph position + chip footprint unchanged — filter-bar VRT byte-identical). data-list's bare sm checkboxes verified fixed transitively.
- **[R] TruncatedText tap-to-toggle on no-hover devices** (audit #6): `(hover: none)` matchMedia + role=button/aria-expanded tap disclosure (Enter/Space/Escape/blur handled); overflow measurement pauses while expanded (else unclamping instantly self-collapses — agent-caught). Tooltip stays the hover/keyboard path; tooltip.mdx + JSDoc now state the hover-only contract plainly.
- **[R] Safe-area**: sonner offsets = `calc(var(--spacing)*N + env(safe-area-inset-*))` via its verified offset/mobileOffset API; sheet pads each side's flush edge the same way. Zero change where env()=0. Design note logged: left/right sheets could also pad bottom for footer actions — deferred, revisit if real usage shows it.
- **[R] settings-row → container queries** (`@container/settings-row` + `@sm/settings-row:*`): stacking now follows CONTAINER width (a row in a narrow card on desktop stacks correctly). Desktop VRT: hero unchanged (preview ≈414px > the 384px @sm threshold, rows stay horizontal).
- **[R] data-list default cell truncation CONSIDERED AND REJECTED**: wrapping is a legitimate table default; the table already scrolls horizontally (its documented strategy), and forcing nowrap+ellipsis hides data behind a hover-only affordance. Consumers opt in per column; audit #8's dogfooding idea noted for a future DataList `truncate` column flag.
- **[R] Mobile VRT lane shipped**: second Playwright project `mobile-chromium` 375×812, isMobile+hasTouch, dsf 1 (deterministic baseline sizes); expect timeout 30→60s (mobile pages are taller). Reduced-motion emulation from X2 applies to both lanes.
- **[R] dvh sweep**: no plain-vh regressions; sidebar's svh intentional (mobile-safe).
- **[R] VRT-infra: AutoTypeTable determinism fix** — the phase's own confirm run caught API-table rows SHUFFLING between builds (ts-morph symbol order is per-program-instance; the fs generator cache lives in `.next` and dies with every build). Fix: `generateTypeTable` wrapper in mdx.tsx stamps a marker tag via the transform hook when a prop's declaration lives in-repo (vs node_modules), then sorts own-first → required-first → alphabetical. First attempt (documented-props-first) failed review: @types/react's aria-* attrs carry JSDoc and buried our props under ~50 aria rows — caught by visual inspection, replaced with the declaration-path classifier. Proven deterministic: 158/158 vs a fresh rebuild + wiped cache. Bonus UX: own API always tops the table. — `apps/docs/components/mdx.tsx`.
- **[R] Gate evidence**: lint ✓ typecheck ✓ 924/924 (+33 R tests) ✓ VRT 158/158 (79 pages × desktop+mobile lanes, all baselines regenerated under the deterministic sort, spot-reviewed) ✓ registry:build build-twice byte-identical ✓ verify-consume real CLI 7/7 + 512/512×2 ✓.

## Phase M

- **[M] Motion vocabulary = 3 utilities in tokens/utilities.css** (`motion-pop-in` scale .9→1 + fade on --ease-spring; `motion-enter-up` fade + 4px rise; `motion-shake` decaying ±4px on --duration-slow) — mount-triggered CSS animations for the keyed-presence mechanism, keyframe end states = resting styles so the global reduced-motion reset degrades to instant-correct. Compile-verified before agents consumed them.
- **[M] `raw-motion` lint ON**: animate-[…], cubic-bezier(), linear(), duration-[…], ease-[…] banned in class strings; animate-spin/pulse stay (documented loader exception).
- **[M] Success check stroke-draw — premise-corrected deviation**: lucide-react spreads props onto the svg root only (verified in its compiled source), so `pathLength` can never reach the check's path; hand-authored inline SVG is icon-rule-banned. Built-in success states (copy-button, auto-save) use motion-pop-in; the draw-in mechanism IS already shipped as the sanctioned lucide-animated `CheckIcon` (pathLength 0→1 variants + imperative `startAnimation()` handle) — documented in motion.mdx's replay-API row. The speculative `motion-check-draw` CSS utility was deleted the same day it was added (greenfield no-dead-code; nothing in-tree could use it).
- **[M] Shake replay = class-toggle + animationend, NOT key-remount** — remounting a form control steals focus/caret (proven by test); `useAnimationReplay` (+ `useShakeOnInvalid`) ships as a `registry:hook` item. **Upstream finding**: Base UI writes aria-invalid/data-invalid straight to the DOM (Input IS Field.Control; Checkbox/Radio/OTP self-register with Field context) — invalid state never round-trips through our wrappers as props, so the hook watches the DOM via MutationObserver, working uniformly for Field-driven and manual invalid. Auto-shake fires once per false→true edge; `shakeSignal` replays on repeated failures. Field itself doesn't shake (its leaves already do) — FieldError enters with motion-enter-up instead; the classic combo composes from two non-duplicating mechanisms.
- **[M] AnimatedNumber = rAF tween, not CSS @property/counter()** — counters can't render Intl.NumberFormat output (currency/compact/locale grouping) which every animated frame needs. Durations read live from the token vars (getComputedStyle) with a documented mirror fallback; Newton-Raphson bezier solver matches the CSS curve; ticking frames aria-hidden + a polite live region announces only the settled value; tabular-nums for layout stability. New registry item + docs page + VRT page.
- **[M] Badge/Marker/Message/Bubble get opt-in `animateIn` (default OFF)** — static lists must not pop; notification-bell's badge animates unconditionally and replays keyed on the DISPLAYED count (150→200 both render "99+" → no false cue). SkeletonReveal ships as a tiny export because the key-both-branches detail is easy to break inline; the inline pattern stays documented as the escape hatch. TextShimmer = the existing `shimmer` utility (documented; a wrapper component would duplicate it).
- **[M] Cross-browser smoke lane** (`test:smoke`): 10 motion-exercising files × chromium+webkit+firefox = 450 tests. Day-one catch: WebKit's Tab-skips-buttons macOS convention failed a simulated-Tab keyboard test whose component was proven fine in all three engines (probe: programmatic focus + Enter/Space works everywhere) — test rewritten to assert activation (what WCAG 2.1.1 requires), not tab-simulation mechanics.
- **[M] Overlays/menus/accordion/tabs verified already-compliant** (T4 did the tokenization); duration-slow modal promotion NOT taken — flat fast-everywhere matches the dense dev-tool aesthetic (audit 09 §f flags it as a legitimate option, not a defect). progress-indicator got the value-sweep transition (stroke-dasharray, mirroring linear Progress).
- **[M] Consume-verifier taught registry:hook = .ts** — it hardcoded `${name}.tsx` for every graph node; hooks are plain .ts (caught by the gate: data-list/field graphs pull use-animation-replay transitively... via checkbox/radio → real CLI proved hooks install correctly once the assertion matched reality).
- **[M] exhaustive-deps cleanup**: the 4 shake-wired controls destructure the hook's stable fields instead of carrying the per-render container object into dependency arrays.
- **[M] MK TODO (unchanged)**: archive the transitions.dev author's tweet link in-repo (per §Open 1 resolution).
- **[M] Gate evidence**: lint ✓ (0 warnings) · typecheck ✓ · 1000/1000 tests (80 files) · smoke 450/450 ×3 engines · VRT 160/160 (80 pages × 2 lanes; 15 intended-diff pages individually spot-reviewed then delete-regenerated; new animated-number baselines) · registry:build build-twice byte-identical (514 items) · verify-consume real CLI 7/7 + 514/514×2 ✓.

## Phase S

- **[S] Sidebar extended, not rewritten** — every pre-existing export/behavior kept. Mobile = Sheet mode below `mobileBreakpoint` via new `use-mobile` registry:hook; `openMobile` state split from desktop `open` (shadcn's shape, our idiom); cookie persistence is WRITE-only (SSR pattern = server layout reads cookie → defaultOpen, documented); SidebarRail is a real focusable button (upgrade over shadcn's mouse-only drag strip); SidebarMenuSkeleton widths cycle deterministically (no Math.random — VRT); trigger hit-area ≥44px (Phase R pattern). Deviations logged in-summary: --sidebar-width-mobile ships as a var-fallback arbitrary class (tokens package was out of the agent's scope — Phase Z may promote it), trigger must live OUTSIDE Sidebar on mobile (a nested trigger only mounts once the closed sheet opens — documented in mdx).
- **[S] Harness finding: the vitest browser iframe is 414×896 — MOBILE-sized.** Real matchMedia would put every sidebar test in Sheet mode; sidebar.test.tsx defaults matchMedia to desktop via scoped beforeEach with an explicit mobile opt-in. Recorded so future responsive tests don't re-trip it.
- **[S] PageHeader = real `<header>` (banner) + TruncatedText title** — and the fix EXPOSED a latent bug: the h1 flex item lacked min-w-0, so overflow detection could never engage. BreadcrumbTrail/BreadcrumbCollapsed ship the STATIC maxItems collapse (first + last N; hidden segments = real links in a DropdownMenu); the ResizeObserver width-driven variant was evaluated and DEFERRED with rationale (multi-node measurement at top-of-page risks thrash/hydration mismatch — unlike TruncatedText's single-node case). Decorative BreadcrumbEllipsis untouched.
- **[S] AppShell composition contract**: reuses SidebarProvider's own flex row (no extra div; data-slot overridden to app-shell); consumer renders the header+content column themselves — keeping `<header>` a SIBLING of `<main>` preserves the banner role (nesting strips it). AUDIT DEVIATION (justified): AppShellContent takes `variant` directly instead of composing SidebarInset — SidebarInset renders a second `<main>` (landmark violation) and its peer selector requires direct-sibling placement incompatible with a banner between nav and main. Scroll = plain overflow-y-auto, not ScrollArea (second focusable scroll landmark would fight the skip-link target; native scroll restoration wins). AppShellSkeleton stays server-safe (RSC may render client components as JSX — only the declaring module crosses the boundary).
- **[S] dashboard-01 = registry:block per audit 11 §c "both"**: hash-tracked AppShell primitive + copy-once starter block, mirroring shadcn's sidebar↔dashboard-01 split. RelativeTime pinned to data.json's generatedAt + refresh={false} (deterministic docs/VRT). View Transitions: next@16.2.9's experimental.viewTransition verified static-export-compatible; view-transition-name wired on shell regions; consumer next.config flag documented rather than hacked.
- **[S] Reduced-motion gap closed in base.css**: `::view-transition-group/old/new(*)` get `animation: none` under prefers-reduced-motion — the universal `*` reset never reaches the snapshot layer's pseudo-element tree (dbx-agent finding, fixed at the token layer).
- **[S] Tooling failures found by the gate, fixed fail-closed**: (1) registry-header stamped a `//` provenance header INTO the block's data.json → invalid JSON → import crash; both stamper and verifier now skip .json payloads (item-level meta.integrity still covers them). (2) resolveDocsCopyPath treated the block's `app/dashboard/page.tsx` CONSUMER target as a docs copy-in and installed a broken /dashboard route into the showcase — copy-ins are now `@placeholder/` targets only. (3) Playwright test timeout 30→120s (sidebar page ~150k px). (4) **reuseExistingServer → false**: a stale reused server produced a sidebar baseline of the PRE-rewrite page (second stale-server incident after X2's chart) — the race between port-kill and managed-server restart is now structurally impossible; every VRT run rebuilds.
- **[S] Chart polish caught in VRT review**: dashboard chart's Y-axis width 32→44px (4-digit tick labels clipped).
- **[S] Gate evidence**: lint ✓ typecheck ✓ 1065/1065 (83 files) ✓ VRT 164/164 (82 pages × 2 lanes, fresh-build-guaranteed) ✓ registry:build build-twice byte-identical (517 items incl. the block) ✓ verify-consume real CLI 7/7 + 517/517×2 ✓.

## Phase D

- **[D] Bloat spike verdict (CX-15's hypothesis confirmed with different root cause than assumed)**: 755MB of the 785MB export was the API tables' inherited-prop expansion (every aria-*/DOM/Base-UI prop × up to 18 exports per page × HTML + 3 RSC payload copies). Fix = own-props-only tables via the Phase R declaration-path classifier (the two features share one mechanism); parts adding no props get one explanatory row. Docs get BETTER (own API first, no 300-row noise) and 72% smaller; worst page 189KB gzipped; VRT wall-clock 4.4m→1.6m. Budgets recorded: raw 220MB total / worst page ≤5MB raw, ≤200KB gz.
- **[D] SEO/OG**: per-page OG images PROVEN to work under output:'export' (fumadocs' own next-static example pattern) — no static-fallback compromise needed; OG colors are literal copies of the dark-theme tokens (Satori can't resolve CSS vars — inline-justified lint suppressions, the sanctioned mechanism). robots disallows /r/* (service-token-gated, not for crawlers).
- _*[D] /r/* cache = short + revalidating, NOT immutable_* — the registry is the pull-based update channel; check-updates must see fresh hashes. _next/static immutable.
- **[D] Turbo remote caching (superseded 2026-07-23)**: the historical two-value workflow-wide
  wiring was removed after review showed same-repository PR code could inherit the token and
  `remoteCache.signature=true` additionally requires `TURBO_REMOTE_CACHE_SIGNATURE_KEY`. Any future
  enablement must be limited to trusted non-PR jobs and supply all three values.
- **[D] Per-preview theme DEFERRED (CX-15's sanctioned path)** — verified no registry primitive plumbs Base UI Portal's `container`, so a scoped .dark wrapper strands portaled popups in the page theme; honest deferral with the iframe-isolation follow-up written up, rather than a half-working toggle.
- **[D] copy-as-prompt reuses fumadocs' static-.md-sibling fetch** (no build-time duplication); responsive-frame toggle ships with honest limits documented (container-query demos respond; viewport-media-query demos need a device — and the system's shell moved to container queries in R/S precisely so the toggle is useful). PropsPlayground designed generic-first (config-driven), Button as the exemplar.
- **[D] Gate evidence**: lint ✓ typecheck ✓ 1065/1065 ✓ registry idempotent ✓ full VRT re-baseline (every page changed by design: API tables + global preview toolbar) with button/playground/toolbar spot-review, then 164/164 confirm against a fresh build ✓.

## Phase A

- **[A] AST accessible-name rule** (`icon-button-name`): TypeScript compiler API JSX walk inside design-lint — `<Button size="icon*">` needs aria-label/aria-labelledby on the element (spreads exempt: wrapper-internal Buttons receive names via props). Probe-proven firing; found+fixed an ordering bug where the lint summary/exit sat ABOVE the appended rule (violations would've printed after "✓ clean" with exit 0).
- **[A] Uniform lint closed for previews**: docs preview files are exemplar code and are now design-linted permanently (apps/docs lint script); the 27 pre-existing violations were FIXED, not suppressed — incl. two hex-shaped false positives (`&#123;` entity, `#482` PR ref) resolved by rewording rather than weakening the rule.
- **[A] Skills = derived from enforcement, not prose**: design-audit v0.2.0 mirrors design-lint's rule IDs 1:1 so the audit checklist can't drift from the enforced reality; add-component v0.2.0 documents the full post-overhaul canon incl. the test techniques this run invented (style-mirror, boundary probes) and the VRT-page registration step that agents previously had to be told out-of-band.
- **[A] Ledger corrections found by verification**: Item/AttachmentTrigger reclassified as useRender primitives (source-verified), authoring-guide's registry target examples were stale (pre-@ui/ placeholders), content-lint description was backwards, chat family added to the matrix (rows 72–75 — pre-existing coverage gap).
- **[A] Gate evidence**: lint ✓ (previews included) · typecheck ✓ · 1065/1065 ✓ · VRT 164/164 with marker's sub-threshold text change re-baselined per the delete-then-regen rule ✓.

## Phase B

- **[B] `.vs-marketing` scope**: re-binds the same semantic vars `.dark` sets, on a CLASS, so a light product page can host a dark marketing surface (the audit's cross-temperature bridge). Portaled popups inside marketing surfaces inherit the PAGE theme — documented limitation (marketing pages rarely portal; home doesn't). MarketingSurface = the useRender wrapper.
- **[B] Prototype-then-approve honored (CX-10)**: home hero built first, agent self-reviewed against audit 17's checklist, orchestrator visually approved the full page (dark warm ground ✓ display+serif-em hero ✓ mono eyebrows + phosphor marker dots ✓ ONE Pixel flourish ✓ sharp mono CTA vs neutral secondary ✓ FIG annotation ✓ accent budget ~10 ✓) before baselines were accepted.
- **[B] D20 mechanics**: `uppercase-mono` lint ON — uppercase TYPE (a text-* utility present) requires font-mono/text-mono-label and caps ≤14px; uppercase CONTENT transforms (avatar initials, no type utility) are principled-exempt. Command group headings dropped `uppercase` (they're Sans labels, not brand voice). Big mono stays sanctioned for non-uppercase DATA NUMERALS (AnimatedNumber stat cards).
- **[B] Icon items renamed `icon-<name>` (439)** — the marketing Terminal was the first real collision with the bare-lucide-name icon items; the mirror script's own collision guard proves the class was anticipated. Prefix eliminates it permanently; file paths/@ui/icons targets unchanged (install name only); mirror script + icons.mdx updated.
- **[B] registry-header now PRUNES public/r** — shadcn build is additive-only; 965 JSONs had accumulated for 525 items, and stale old-name JSONs actively re-stamped sources with dead identities (clobbering fresh stamps during the icon rename). Anything in public/r not named for a current item is deleted first.
- **[B] Cross-cutting fixes from agent verification**: tailwind-merge classGroup gained `text-mono-label` (was classified as a color and silently dropped by cn() — second instance of the T1 bug class); home CTAs compose `<Button render={<Link/>}>` because calling buttonVariants() from a Server Component crosses the RSC boundary illegally (Button's own JSDoc now the reference).
- **[B] ParticleField determinism contract**: seeded mulberry32 (a `seed` prop, never Math.random), count cap constant, reduced-motion = one static frame, `data-drawn` attribute as the paint signal tests/VRT wait on. StaggeredTextReveal is CSS-only (motion-enter-up + per-word delay), no JS animation lib.
- **[B] Gate evidence**: lint ✓ typecheck ✓ 1120/1120 (91 files) ✓ VRT 182/182 (91 pages × 2 lanes) ✓ registry:build build-twice byte-identical (525 items) ✓ verify-consume real CLI 7/7 + 525/525×2 ✓.

## Phase C (done)

- **[C] P0-02-class sweep**: bubble's ring-glow → `focus-visible:border-ring/(--alpha-tint-border)` + global outline restored (its `outline-none` dropped); `--alpha-ring-glow` token deleted (zero uses — greenfield, no dead tokens); color-picker's `shadow-sm` removed (flat system); TableHead 36→32px (`h-(--size-md)`, intended VRT delta on table pages); dark-tint scoping policy documented at the pattern's home (input.tsx) per P1-24.
- **[C] Chevron policy verified already-compliant** (P1-23): ChevronsUpDown = combobox-style (country/region-select), ChevronDown = select-style (select, date-picker, split-button); formal policy text lands in the ledger refresh.
- **[C] Flat-only exports (CX-12)**: Object.assign namespaces removed from alert/breadcrumb/empty/pagination (card follows once the ref-migration agent releases the file); all 587 dotted usages across previews/MDX/tests/JSDoc rewritten to flat names with import fixing.
- **[C] Renames DIRECT (no aliases)**: `empty-state`→`empty` with the full shadcn Empty anatomy (Empty/EmptyHeader/EmptyMedia[variant=default|icon × intent]/EmptyTitle/EmptyDescription/EmptyContent) — our size/bordered/surface/intent features preserved; header nesting = small intended spacing change. `state-select`→`region-select` + the 1,373-line dataset split to `region-select-data.ts` (same registry item, two files; verify-registry-deps taught multi-file self-imports).
- **[C] True-synonym unification → `intent`** (P1-03): alert `variant`→`intent`, badge `color`→`intent` (badge keeps its orthogonal `variant` fill axis — CX-12); alert-dialog + empty already used `intent`. Button's single 14-value enum is NOT a synonym case (its variant deliberately bakes style×family into one documented axis) — left as the documented exception, logged.
- **[C] Return-type annotation split resolved by REMOVAL** (P3-03): the five explicit `: React.ReactElement` / `JSX.Element` return annotations dropped — inference is the file-dominant norm; `React.ReactElement` remains only as PROP types (correct usage).
- **[C] DataList `align` gains `center`** (P3-04) to match the overlay family's three-value alignment.
- **[C] IconButton re-documented as the compile-time aria-label guarantee** (P1-21 reversal).

- **[C] forwardRef→ref-as-prop delegated + verified**: all 24 files (~60 exports) + 11 previously ref-less leaf exports (bubble/marker/message) migrated by a subagent; verified by zero-forwardRef grep, typecheck, 239 scoped tests (summary: docs/plans/.c2-refprop-summary.md). API tables now document extra rows (e.g. new size props) — the +38px-per-row VRT growth class, reviewed.
- **[C] A11y mediums delegated + verified** (7 items — copy-button live region, pagination self-enforcing aria-disabled, message-scroller reduced-motion behavior override, text-edit id/labelledby, emoji/color-picker roving tabindex, alert-dialog action loading, field-inline disabled/readOnly/error) with tests; summary: docs/plans/.c10-a11y-summary.md. pagination.mdx guidance updated to the built-in enforcement.
- **[C] Size props landed (P1-04)**: Input sm/default/lg on --size-* (native numeric `size` attr replaced by the variant — Omit'd), Textarea density tiers (min-height+padding — multiline fields don't use fixed control heights), RadioGroupItem sm/default mirroring Checkbox's micro-scale, OTPInput slot sm/default/lg with matching type tiers.
- **[C] DRY (P2-06/07)**: button/badge/auto-save-input compose `<Spinner>` (new `size="inherit"` variant defers to host `[&_svg]` selector sizing; spinner's own scale re-tokenized to --icon-*); notification-bell count badge composes `<Badge variant="solid" intent="destructive">` (dot mode stays a bare status dot — Badge has no 8px form); status-icon KEEPS its Loader (icon-map component; Loader is data there, not a loading widget) — documented exception.
- **[C] P2-04**: auto-save-input `data-status`→`data-state` (image's convention wins); status-icon's `data-status` stays (prop reflection, not an async lifecycle).
- **[C] Field (P3-02)**: FieldGroup/FieldSet/FieldLegend/FieldContent added + `orientation="responsive"` (container-query driven via FieldGroup's @container); ergonomic Field renders responsive as a FieldContent label-column + control.
- **[C] Solid destructive Button NOT added** (P1-07): D4 locks destructive = soft-only; design.md's solid-fill recipe is the stale side — corrected in Phase Z.
- **[C] Axe state coverage + slider keyboard + ledger delegated + verified**: ~75 new state axe tests across 42 files, 8 slider keyboard tests, ledger/authoring-guide refreshed (incl. an agent-caught correction: Marker and BubbleContent DO use useRender; only Bubble root is exempt) — summary: docs/plans/.c11-axe-summary.md. Known deferred: cmdk's CommandLoading role-nesting (suppressed with rationale; cmdk is REMOVED in X1 anyway).
- **[C] Registry tooling hardened**: registry-header now CREATES missing copy-ins (renames/new items); alert.test's pinned focus-trio assertion inverted to the P2-35 contract; preview keys renamed (emptyState→emptyDemo, stateSelect→regionSelect).

## Phase U

- **[U] Bumps landed**: shadcn 4.7→4.13 (+ `registry validate` now the FIRST step of registry:build — 507 items valid), eslint 9.39→10.7 (typescript-eslint ^8.64 peer-supports it; config pkg peer → ^10), react-day-picker 9.14→10.0.1, @shadcn/react 0.1→0.2.1, lucide-react unified ^1.21.0 (resolves 1.21.0 under the release-age policy), style-dictionary 5.4.4→5.5.0 (output proven byte-stable vs 5.4.4 via full var diff).
- **[U] react-day-picker 10**: zero code adaptation needed — typecheck/tests/rendered-calendar pixels all clean; the ONLY visual delta is the AutoTypeTable API reference shrinking (v10 removed deprecated props like initialFocus). Peer-dep note + registry item dep updated ^9→^10.
- **[U] contrast-check**: purple ghosts removed AND missing-token pairs now FAIL (the old `continue` failed open, P2-24); added muted-foreground on background/card (AA), a 2.5:1 placeholder legibility floor for muted-foreground-faint (sub-AA by design, never invisible), and 1.4.11 non-text checks for ring/primary/brand (3:1). `border` hairlines + switch `track` documented as the deliberate non-gated exemptions (decorative separation / redundant affordance). 90 checks green.

## Phase T6

- **[T6] Brand = theme-split `--brand`**: light `oklch(0.6 0.17 148)` (computed: 3.52:1 on card, 3.61:1 on background — WCAG 1.4.11 with margin; max AA-passing L at C0.17 is 0.64), dark `oklch(0.86 0.21 148)` (MK's phosphor, 13.3:1). Marker roles only, documented in colors.mdx §Brand accent + token $description.
- **[T6] chart-7 collision CONFIRMED and reassigned** — shipped chart-7 was green.600 (hue 145 ≈ brand 148); reassigned to design.md's olive (new `color.olive.500/400` primitives, hue 104), light `0.6 0.127 104.2` / dark `0.72 0.15 103.9`.
- **[T6] CVD evidence (side-by-side check per CX-9)**: brand vs success under Viénot deuteranopia/protanopia keeps linear-RGB distance 0.081–0.302 and luminance ΔL 0.10 (light) / 0.28 (dark); the binding rule stays "never hue alone" — markers pair pulse/shape/mono-label, success always carries an icon. Recorded in colors.mdx.
- **[T6] 17 literal-OKLCH semantics resolved**: 8 hover/active + 8 subtle-hover now DERIVED by the new SD preprocessor (`derive-interaction-states`: fill L−0.05/−0.09; fill@7%/30% gamma-sRGB composite over subtle) — value-identical to the previous literals (asserted by full var diff); 8 subtle/text literals → new primitives (red/green/amber/blue .100/.650/.925); `overlay` stays the ONE documented alpha-composite literal.
- **[T6] SD DTCG side-effect fixed at the output layer** — registering a preprocessor activates SD's padded color stringification ("0.5050 0.0030 75.00"); `compactOklch()` in build-tokens.mjs re-canonicalizes all emitted oklch() (theme.css + tokens.json + tokens.ts). Residual: SD rounds hue to 2 decimals (27.518→27.52, ≤0.005° — imperceptible, accepted); info-subtle-hover dark is now the exact derived composite (0.354 vs my hand-nudged 0.352).
- **[T6] Fonts**: serif accent = **Newsreader** (Lora OUT everywhere: token, next/font, global.css binding); `--font-family-display` (Geist, split for marketing) + `--font-family-pixel` = **GeistPixelSquare** (my pick of the 5 cuts — the classic pixel look; single-flourish rule in the token description); `text-mono-label` role token (12/16, mono, +0.05em — the voice layer; uppercase applied at call sites, mono-exclusive lint comes with Phase A/B). Font-delivery contract documented in typography.mdx (geist npm + next/font self-host for static export).
- **[T6] $descriptions**: added to all new tokens + the dark file's group-level description documenting intentional omissions (theme-invariant fills/steps).

## Phase T5

- **[T5] Control scale gains `--size-xs` (24px)** — Button xs is KEPT per CX-12, so the scale it sits on must exist; the password-eye and input file-button (24px hit areas) join it. h-6/size-6 literals stay ALLOWED outside controls (badge, switch track, select scroll strips have their own scales — lint boundary documented in the rule).
- **[T5] Avatar/skeleton/message-gutter adopt `--size-*`** — avatars aren't controls but share the same 28/32/40 physical rhythm; migrating them keeps the raw-control-size lint uniform (zero exceptions) and re-skins them coherently.
- **[T5] Icon tokens use design.md role names** (`--icon-inline/default/action/feature` = 14/16/20/24) + `--icon-compact` (12px, exists only for the kept xs control tier). 31 icon elements + all svg-selector sites + icon-slot containers migrated; `[&_svg]:size-1` dot glyphs and `size-1/2` fractions are geometry, not icon scale — exempt.
- **[T5] radius: `--radius-xs` (2px, real token — was a silent Tailwind fallback, P1-10) + `--radius-sharp` (2px marketing gesture, D18; same value TODAY but a separate role Phase B may retune); `--radius-xl` REMOVED from the bridge and `rounded-xl` lint-banned (silent-fallback trap); bubble 16→12px (`rounded-lg`) — intended VRT diff.
- **[T5] Sidebar widths are tokens** (`--sidebar-width: 15rem`, `--sidebar-width-icon: 3rem` in :root); the component's JS constants and inline-style defaults removed — per-instance override stays via the style prop.
- **[T5] JS timings module** — `TIMINGS` (1500 feedback-revert / 800 autosave / 700+300 hover) + `FLOATING` (sideOffsetAttached 4, sideOffsetDetached 8, collisionPadding 8) exported from @vegastack/utils; 10 components rewired. UNIFICATION deltas: tooltip sideOffset 6→8 (detached role), hover-card collisionPadding 16→8 — logged as intended; submenu offset 0 stays (deliberate flush).
- **[T5] BUG found+fixed during VRT review: selector regex mis-ordered 3 vs 3.5** — `]:size-3\b` matched inside `size-3.5` (word boundary at the dot), producing broken `size-(--icon-compact).5` classes in 6 files (breadcrumb chevrons visibly wrong). Fixed; VRT re-run.
- **[T5] OBSERVATION for the report: VRT tolerance is `maxDiffPixelRatio: 0.01`** (Phase −1 config) — sub-1%-of-page changes pass silently (bubble's intended 16→12px radius change didn't trip it). Left unchanged (tightening would invalidate all baselines mid-program), but flagged for MK: the later visual phases (T6/M/B) should treat "VRT green" as "<1% differs", and the per-phase review relies on the intended-change inventory, not only failures.
- **[T5] command dialog h-12 (48px) left as-is** — off-scale palette-input height is a Phase C decision (register P2-20); not silently retuned here.

## Phase T4

- **[T4] Duration scale stays 3-step** — message-scroller's `duration-400` resolves DOWN to `duration-slow` (300ms; the audit's "changes the feel" caveat accepted — one inactive-fade transition, imperceptible class of change) rather than adding a 4th step; `duration-200` → `duration-base` (pure rename).
- **[T4] 37 bare-transition sites × 28 files** get co-located `duration-fast ease-standard` (variant prefixes carried: `[button,a]:`, `before:`); includes checkbox.tsx which the amended register dropped on a pattern technicality but which was still untokenized (uniform-rule call). Curve change 0.4,0,0.2,1 → 0.2,0,0,1 is the a4/b1 fix — hover/focus only, invisible to static VRT.
- **[T4] Overlay ease unification (b2)**: dropdown-menu/context-menu/tooltip/select popups + select chevron gain `ease-standard`; sidebar's 2 `ease-linear` → `ease-standard` (b3 — collapse is a discrete toggle, not drag-following).
- **[T4] New tokens**: `--motion-ease-spring` = audit §f's linear() spring verbatim (`linear(0, 0.5 60%, 1.05 80%, 0.98 90%, 1)`, custom `easing` $type passes through SD) — Phase M may retune by eye; `--motion-blur: 2px` bridged as `blur-motion` utility.
- **[T4] Lint `transition-pairing`** — string-literal-scoped: any `transition*` token requires `duration-*` + `ease-*` in the SAME literal; `transition-none/-discrete` exempt. Immediately caught one site beyond the audit inventory (image.tsx had duration but no ease) — fixed.

## Phase T3

- **[T3] Two bands, not a ladder**: `--z-raised: 10` (local raises inside a component's own stacking context) + `--z-overlay: 50` (every portaled surface; DOM order resolves nesting — Base UI appends portals to `<body>`, body is `isolate`). Contract documented in foundations/elevation §Stacking. 21×z-50 + 7×z-10 migrated to `z-(--z-*)`; raw-z lint ban ON.
- **[T3] Sonner = documented-exempt top band (CX-8 disposition's second arm)** — the toaster mounts at app boot BEFORE any dialog portal, so DOM order would put later modals above it; its library z is load-bearing (a toast fired mid-dialog must stay visible — asserted by test). Attempting to band it would need `!important` (its stylesheet hardcodes the container z) — banned.
- **[T3] Nested-overlay tests live in `test/stacking.browser.test.tsx` WITH compiled CSS** — first attempt in the CSS-less unit suite failed rightly: the stacking contract IS CSS (z bands + fixed positioning + DOM order); without compiled utilities Base UI's `data-base-ui-inert` guard covers everything. Follows the existing `test/contrast.css` compiled-entry pattern. 5 hit-tests (elementFromPoint): Select-in-Dialog, Popover-in-Dialog, Tooltip-in-Sheet, nested Dialogs, toast-over-Dialog.
- **[T3] `test/overlay-portal.browser.test.tsx` assertion updated** — the portal contract now asserts `z-(--z-overlay)` (was `z-50`).

## Phase T2

- **[T2] Token model: `--alpha-*` (color-mix modifiers, %) + `--opacity-*` (element opacity) + precomposed colors** — per CX-7's three classes. 24 role tokens (values NOT collapsed across roles: e.g. alert 20% border vs sonner 30% border stay distinct — lossless first). Tailwind v4 `/(--alpha-x)` modifier verified to emit `color-mix(in oklab, C var(--alpha-x), transparent)` with solid fallback. `opacity-0/100` exempt as structural endpoints.
- **[T2] Theme-split alpha tokens** — `--alpha-soft-hover` (20%→30% dark) and `--alpha-soft-surface` (10%→20% dark) move the `dark:` duplication out of class strings into the token layer (bubble). — `semantic.dark.tokens.json`.
- **[T2] A11Y FIX (found by the new composited contrast gate): soft-button hover failed AA in light** — `{family}-text` on `{family}@20% over subtle` = 3.6–3.9:1, a pre-existing failure the gate never measured. Fix per CX-7 class 3: precomposed `{family}-subtle-hover` tokens — light tuned to the fill@7%-over-subtle composite (all pass ≥4.55), dark authored as the EXACT current fill@30% composite (zero visual change). Button soft variants now `hover:bg-{family}-subtle-hover`. Bubble keeps the alpha pair until Phase C normalizes its `text-destructive` fill-as-text choice.
- **[T2] contrast-check extended** — parses `--alpha-*`/`--opacity-*` percentages per theme, composites in gamma sRGB (browser-faithful), checks outline rest/hover composites + the new subtle-hover pairs: 76 checks green.
- **[T2] breadcrumb separator `text-muted-foreground/70` → `text-muted-foreground-faint`** — the faint token IS this role; a bespoke alpha would duplicate it (small intended VRT delta).
- **[T2] design.md discrepancy noted for Phase Z**: design.md §States says disabled = 45%; shipped system uses 50% everywhere → tokenized as `--opacity-dim: 50%` (lossless); design.md text to be corrected in Z.
- **[T2] Lint**: `raw-alpha` + `raw-opacity` bans ON (design-lint), registry-wide, zero exceptions.

## Phase T1

- **[T1] Scoping mechanism: `--type-*` binding vars + `@theme inline` var-reference remap** — `text-xs…3xl` utilities compile to `var(--type-<step>)`; theme.css emits both ladders (`--type-product-*`, `--type-doc-*`) plus an active binding defaulting to product. Chosen over a global value remap because Fumadocs injects its chrome classes via `@source inline(...)` — they compile through OUR theme, so an unscoped remap would shrink the docs shell (CX-6's exact concern). — `sd-hooks.mjs`, `semantic.tokens.json`.
- **[T1] Docs boundary: shell = doc ladder, previews + `[data-base-ui-portal]` = product ladder** — Base UI portals mount popups on `<body>` OUTSIDE the preview subtree; the portal containers carry `data-base-ui-portal`, giving a stable selector so portaled popups keep product type. Fumadocs' own portals lack the attribute and correctly stay on the doc ladder. — `apps/docs/app/global.css`, `component-preview.tsx`.
- **[T1] Core ladder values (leading pairs my call)**: xs 11/16 · sm 12/16 · base 14/21 (design.md body) · lg 16/24 · xl 18/26 · 2xl 20/28 · 3xl 24/32. Doc ladder = Tailwind stock values, tokenized as `type-doc-*` (the deliberate 16px-prose spec).
- **[T1] Display tier**: `text-display-sm/md/lg/xl` = 32/36 · 40/44 · 56/60 · 72/76, weight 400 (audit 17: display 400–500; alpha/size carry hierarchy), tracking −0.04/−0.045/−0.05/−0.06em (checklist endpoints, smooth ramp). Old `text-display` (36/300) REMOVED — zero call sites existed; weight-300 hero superseded by audit 17's 400-discipline.
- **[T1] Heading aliases re-based**: h1 24/32 · h2 20/28 · h3 18/24 (tracking −0.02/−0.015/−0.01) — h1 drops from 28 to the 3xl step; the display tier now owns >24px. h4/label/label-sm/code/code-sm unchanged.
- **[T1] Migration = size-preserving shift + role upgrades**: mechanical one-pass map (xs→sm, sm→base, base→lg, lg→xl, xl→2xl, 2xl→3xl) across 122 registry + 128 preview sites, so no component changed pixel size except deliberately; then role upgrades: markdown-view/text-edit heading ladders → `text-h1…h4`/`text-label` (weight 500→400 on h1–h3 — intended, design.md rule), dialog/alert-dialog/sheet titles → `text-h4`, popover title → `text-label`, card title → `text-h4` (+`text-label` at sm density), FilterChip 12→14px (register P2-19).
- **[T1] Known intended VRT deltas**: body copy line-height 20→21px; heading weights 500→400 in markdown/text-edit previews; overlay titles gain 6px line-height (leading-none → h4's 22px); chip text 12→14. Each reviewed in the VRT pass.
- **[T1] BUG found+fixed during VRT review: ladder line-heights must be unitless ratios** — first VRT run failed 74/74; root cause: Tailwind's stock `--text-*--line-height` values are unitless `calc()` ratios, so when another rule overrides only font-size (fumadocs 13px code blocks), line-height scales along. Absolute-rem line-heights broke that contract and shifted every code block ~5px. Both ladders re-authored as unitless ratios (`calc(21 / 14)` style). — `semantic.tokens.json`.
- **[T1] BUG found+fixed: tailwind-merge stripped custom role utilities** — `cn()` classified `text-h1…h4`, `text-code*`, `text-display-*` as colors, so `cn('text-h1', …, 'text-foreground')` silently dropped the size (MarkdownView headings rendered at body size). The twMerge `font-size` classGroup extension now lists every custom type utility. — `packages/utils/src/index.ts`.
- **[T1] Scope coverage: `vs-type-product` also on the shared preview `Wrapper`** — hero demos are used directly in MDX outside `ComponentPreview`, so scoping only the Tabs container left them on the doc ladder (caught live: one Marker demo at 16px). — `apps/docs/components/preview/wrapper.tsx`.
- **[T1] Stale docs corrected while updating foundations**: typography.mdx + design-principles.mdx claimed "Lora serif headings, max font-semibold" — neither was true of the shipped system (headings are Geist; nothing exceeds 500). Rewritten to the current spec; Lora font-loading section stays until T6 swaps Newsreader in. — `typography.mdx`, `design-principles.mdx`, `foundations.tsx` (+ new `TypeCoreLadder` specimen).
- **[P] Targeted regression tests added** — sidebar collapsed accessible-name, truncated-text/IconText tabIndex upgrade (real-overflow browser tests), field horizontal description, popup outline-none class guards (dialog/popover/sheet/alert-dialog). — 7 test files.
