# Plan: Facelift doctrine — conventions, numerals, page rhythm, link-button recipe, decision register

**Status:** approved by MK 24-09-2026 ("Approve all as recommended", Regent #136), with D1 amended: commit the register **after stripping the decision-sheet link and anything internal** (Round 16). Shipping runs under MK's standing "Standing ship-it for the facelift" (24-09-2026).
**Origin:** Regent issue `vegastack/engg-clients-regent-ai-platform-web-app-v2#136` (brief 00d of the facelift epic #135, approved by MK 24-09-2026). It owns DS-03 (doc half), DS-25, DS-26, DS-29, DS-72, DS-73, DS-74, DS-75 and REG (commit the decision register, operator Round 14). The canonical DS-ID table lives in 00a's brief.
**Branch / worktree:** `docs/facelift-doctrine` off `origin/main` (`dc228eb2c`, registry 0.16.1, `@vegastack/design` 0.7.2), at `~/projects/vegastack-design-facelift-doctrine`.
**Shape:** one doctrine PR (AGENTS.md § Verification, "per wave … in a PR that touches nothing else"). It edits `design.md`, `skills/**`, `apps/docs/content/**`, two docs-app pages, `registry.json` meta strings, `component-contracts.json` summary strings, the register and its two tooling comments. It edits **no file under `packages/ui/registry/ui/*.tsx`** — every source half (AnimatedNumber face, TableCellText `text-xs`, `data-table-parts`/`data-list`/`timeline`/`tool-call-chip` JSDoc, PageHeader `font-heading`) rides 00a's component PRs.

## Re-grounding against `main` (dc228eb2c) — corrections to the brief

1. **The register's whole directory is excluded, not the file.** `.git/info/exclude:9` is `docs/plans/2026-09-18-shadcn-reset/` ("Untracked by MK's instruction: the shadcn-reset mandate, plan and decision sheets"). `.git/info/exclude` is per-clone and unversioned, and a tracked file is never subject to it, so REG is `git add -f` of `decisions.md` alone; the other five sheets (`README.md`, `extras.md`, `implementation.md`, `migration-notes-7a.md`, `progress.md`) stay untracked and the exclude line stays. The new worktree has no copy of the file: it is copied from the main checkout.
2. **The register body already matches `decisions.json`**: 180 rows, 72 ours, zero ID or verdict differences (parsed 24-09-2026). Only the header prose is stale ("171 micro-decisions", "110 shadcn, 61 ours"), plus `_source` ("179 rows"), `tooling/upstream/lib.mjs:117` ("170 rows", "untracked") and the `--sync-decisions` comment in `tooling/upstream/verify-parity.mjs` ("untracked by MK's instruction").
3. **There is no decision-ID family list** in `design.md` or either skill, so "add VOI to the family lists" has nothing to edit. VOI enters the register as a new section when MK approves 00a's VOI-1 row; nothing here.
4. **`skills/public/vegastack-design-system/references/components.md` is GENERATED** from `component-contracts.json` (`pnpm design:derived`). The "Names hide abilities" table, "Which component for X" and the page-shape reference go in the hand-written `SKILL.md`; the roster changes only through contract `summary` strings.
5. **`skills/internal/component/SKILL.md` is 491 lines** and `skill-lint` caps SKILL.md at 500. The "Before you add a prop" checklist, the decision tree and the block rules go in a new `skills/internal/component/references/conventions.md`, with a two-line pointer in § 3.
6. **The repo is PUBLIC** (`gh repo view` → `PUBLIC`). Committing the register publishes it, including its header's claude.ai decision-sheet link and review narrative. Round 14 decided to commit it; this plan asks MK to confirm that with the visibility in view (decision D1 below).
7. **Proposed decision rows are not citable yet.** The brief's conventions table cites API-19 and API-25, which are 00a's _proposed_ rows (the register tops out at API-17). The table cites only existing IDs (API-17, FRM-4); a proposed ID is added to the table in the PR that lands the approved row.
8. **Four doctrine lines describe behaviour 00a has not shipped** (brief § Risks): `pagination.mdx:174-177` (DS-71), field meta "auto-wired ARIA" and `SKILL.md:160-165` (DS-47), toast "one toast per action" (DS-64), sidebar docs (DS-04); plus 00a's doctrine halves DS-10(a) (`lint-rules.md:230-233`) and DS-13 (`foundations/elevation.mdx` overlay width table). Task 9 lands each only if its 00a PR is on `main` first; otherwise it waits for the next doctrine PR.
9. **Line drift:** the Polymorphism bullet is `SKILL.md:180-182`, the retired-vocabulary `text-h1` row is `:224`, the modal-shadow row is `:231`, the view-switch bullet is `:65-66`, the Chip bullet `:70-73`. `button.mdx:105-110` already carries the correct recipe. `registry.json:442` is Button's `whenNotToUse` ("variant=link"). Dialog has no shadow at all (`dialog.tsx`); Sheet is `shadow-lg`.

## Decisions MK makes on this plan

- **D1 — Commit the register to a public repo.** Recommended: yes, as decided in Round 14. **MK's ruling (24-09-2026): commit, and strip the private decision-sheet link and anything internal.**
- **D2 — Gated lines (item 8 above).** Recommended: land each with or after its 00a PR, never before; if 00a has not merged when MK says `ship it`, those lines move to the next doctrine PR.
- **D3 — A `button.test.tsx` assertion for the link recipe.** The brief asks for it; it is a test file, not source, but it puts `button`'s affected Chromium lane into this PR. Recommended: include it.
- No decision rows are proposed or added here. Rows from 00a/00b land only on MK's approval of each, in their own PRs.

## Tasks

- [ ] **Task 1: REG — commit the decision register with true counts**
  - Files — Create (tracked): `docs/plans/2026-09-18-shadcn-reset/decisions.md` (copied from `~/projects/vegastack-design`) · Modify: `packages/ui/upstream/decisions.json` (`_source`), `tooling/upstream/lib.mjs` (JSDoc at :115-121), `tooling/upstream/verify-parity.mjs` (`--sync-decisions` comment)
  - Interfaces — Produces: a tracked register whose header reads "180 micro-decisions … 108 shadcn, 72 ours"; `_source` names 180 rows; both tooling comments say the register is tracked. Consumed by 00a/00b row numbering (next free: INT-11, A11Y-17, LAY-13, FRM-15, OVL-17, API-18).
  - Steps: `cp ~/projects/vegastack-design/docs/plans/2026-09-18-shadcn-reset/decisions.md docs/plans/2026-09-18-shadcn-reset/` → edit the header counts and fold in the rows added since (TYP-14…18, ICO-8, OVL-15, COL-23, OVL-10 flip) → edit `_source`, lib.mjs and verify-parity comments → verify: `node tooling/upstream/verify-parity.mjs --sync-decisions && git diff packages/ui/upstream/decisions.json` shows only the `_source` edit; `pnpm exec prettier --check docs/plans/2026-09-18-shadcn-reset/decisions.md`; `pnpm upstream:check && pnpm upstream:selftest` green → `git add -f docs/plans/2026-09-18-shadcn-reset/decisions.md` → `git check-ignore docs/plans/2026-09-18-shadcn-reset/decisions.md` exits 1 → commit `docs(register): commit the shadcn-reset decision register with true counts`

- [ ] **Task 2: DS-74 numerals and DS-03 page-heading face**
  - Files — Modify: `apps/docs/content/docs/foundations/typography.mdx` (mono paragraph :80-82, roles table :97-106, font tokens :111), `design.md` (:875 "tabular figures on code and data"), `skills/public/vegastack-design-system/SKILL.md` (retired-vocabulary `text-h1`/`text-h2` row :224; a numerals line in Do), `apps/docs/content/docs/guides/migrating-shadcn-reset.mdx` (role-utility table :308-310), `apps/docs/content/docs/components/data-list.mdx` (:384-400 mono section), `apps/docs/content/docs/components/data-grid.mdx` (column API `mono`)
  - Interfaces — Produces: one numerals rule — counts, dates, amounts, quantities are `font-sans tabular-nums`; `font-mono` only for codes and identifiers. One heading table — page `font-heading text-2xl font-semibold` rendered by `PageHeader` (never a hand-sized h1) · section h2 `font-heading text-base font-medium` · group label `text-xs font-medium text-muted-foreground`. Task 7 cites both.
  - Steps: edit (the "Compact numerals" row becomes `tabular-nums`, no mono; Geist Mono = "code and identifiers"; `mono` column docs say "for codes and IDs; numbers use `align: "end"` + `className: "tabular-nums"`") → verify: `grep -rn "text-3xl font-semibold" apps/docs/content skills/public design.md` returns no page-heading claim; `node tooling/content-lint.mjs`; `pnpm design:sync:check` → commit `docs: numerals in the regular font, one page-heading face (DS-74, DS-03)`

- [ ] **Task 3: DS-25 one link-button recipe**
  - Files — Modify: `skills/public/vegastack-design-system/SKILL.md` (Polymorphism bullet :180-182), `packages/ui/registry.json` (button `meta.whenNotToUse`, :442), `apps/docs/app/not-found.tsx` (:21, :24-31), `apps/docs/app/(home)/page.tsx` (:216, :226, :346, :526, :535) · Test: `packages/ui/registry/ui/button.test.tsx` (D3)
  - Interfaces — Produces: the recipe `<Link href="…" className={buttonVariants({ variant, size })}>`, stated once in the skill and used by every docs-app call site; `buttonVariants` is imported from `@/components/ui/button` (no `'use client'` in that module, so Server Components use it directly).
  - Steps: add the assertion and run it (it passes on today's source — it pins the recipe, it is not a bug repro):
    ```tsx
    test("a link styled with buttonVariants keeps the link role", async () => {
      const screen = await render(
        <a href="/docs" className={buttonVariants({ size: "lg" })}>
          Docs
        </a>,
      );
      await expect
        .element(screen.getByRole("link", { name: "Docs" }))
        .toBeInTheDocument();
      expect(screen.container.querySelector('[role="button"]')).toBeNull();
    });
    ```
    → rewrite the call sites and the skill bullet ("never `Button render={<Link/>} nativeButton={false}` — Base UI sets `role="button"` on a non-native element") → meta: "Navigation to a URL — style the link with `buttonVariants()`." → verify: `grep -rn "nativeButton={false}" apps/docs/app skills/public` is empty; `pnpm check:component button`; `pnpm -F @vegastack/docs typecheck` → commit `docs: one link-button recipe, buttonVariants on the link (DS-25)`

- [ ] **Task 4: DS-26 theme choice without a local toggle**
  - Files — Modify: `apps/docs/content/docs/guides/provider-setup.mdx` (§ Theme toggle, :55-79), `skills/public/vegastack-design-system/SKILL.md` (known gaps: `theme-toggle` is deferred)
  - Interfaces — Consumes: `useVegaStackTheme()` from `@/components/ui/provider`; `DropdownMenuSub`, `DropdownMenuRadioGroup`, `DropdownMenuRadioItem` from `@/components/ui/dropdown-menu` · Produces: a "Theme in the user menu" snippet — a `DropdownMenuRadioGroup` whose `value` is `theme` and whose `onValueChange` is `setTheme`, items "Light", "Dark", "System".
  - Steps: rewrite the section → verify: paste the snippet into a scratch `apps/docs/app/_scratch-theme.tsx`, run `pnpm -F @vegastack/docs typecheck`, delete the scratch file; `node tooling/content-lint.mjs`; `pnpm -F @vegastack/docs lint:links` → commit `docs(provider): theme choice as a user-menu radio group (DS-26)`

- [ ] **Task 5: DS-29 empty-state tiers and the view-switch rule**
  - Files — Modify: `apps/docs/content/docs/foundations/empty-states.mdx` (frontmatter description, tier table :12-23, Rules), `apps/docs/content/docs/components/empty.mdx` (Usage: link to the tiers), `skills/public/vegastack-design-system/SKILL.md` (:65-66 view switch; link to the tiers), `apps/docs/content/docs/components/toggle-group.mdx` (Do/Don't :166), `packages/ui/registry.json` (toggle-group and radio-group `meta`)
  - Interfaces — Produces: ten tiers — tier 5 is the favourites well only (drop targets are `Dropzone`); tier 6 `Empty` carries `role="alert"` when it replaces content after a failed load; new "No matches" (`SearchX`, "Clear filters") and "Couldn't load" (`role="alert"`, `-text` icon ink, "Try again"). Copy defaults: retry "Try again", loading "Loading {noun}…", list end "End of list", default media `EmptyMedia variant="icon"`. One view-switch rule: a form value is a RadioGroup; an immediate view or scope switch over the same content is a ToggleGroup with `deselectable={false}`; swapping in-page regions is Tabs; switching URLs is route tabs.
  - Steps: edit → verify: `node tooling/content-lint.mjs`; `grep -n "toggle-group\|ToggleGroup" skills/public/vegastack-design-system/SKILL.md apps/docs/content/docs/components/toggle-group.mdx` shows the same rule in both; `pnpm registry:build` then `git status` shows only the toggle-group and radio-group items changed → commit `docs: empty-state tiers for no-matches and load errors, one view-switch rule (DS-29)`

- [ ] **Task 6: DS-72 docs and meta say only what is true**
  - Files — Modify: `packages/ui/registry.json` (meta/description: label "Use required", chip "Combobox chips", select "multi-select" and "long/grouped lists", command "fuzzy-search picker", emoji-picker "via onSelect", chip-input "Combobox field chrome", message-scroller "virtualised/virtualise", badge "Anything clickable"), `packages/ui/component-contracts.json` (the matching `summary` strings: chip :4231, chip-input :4392, emoji-picker :8783, message-scroller :12117), `apps/docs/content/docs/components/chip.mdx` (§ Composing another engine's chip), `apps/docs/content/docs/components/emoji-picker.mdx` (:28-36, SearchableSelect does not install panel-search), `skills/public/vegastack-design-system/SKILL.md` (Chip bullet :70-73)
  - Interfaces — Produces: true strings — ComboboxChip is Base UI's own chip (not composed on Chip; recomposing it is a known deviation in Task 7); EmojiPicker returns via `onValueChange`; ChipInput is `InputGroup` chrome + `Tag`; MessageScroller is not virtualised; Badge renders a link through `render` (upstream's Link section); Select supports `multiple` and long lists go to SearchableSelect. Regenerated `skills/public/vegastack-design-system/references/components.md`.
  - Steps: edit each string against its source file (`grep` the prop or import it claims) → `pnpm design:derived` → `pnpm registry:build` → verify: `pnpm design:derived:check`; `node tooling/verify-component-contracts.mjs`; `node tooling/verify-public-api-docs.mjs`; `grep -rn "onSelect\|Combobox field chrome\|virtualis" packages/ui/registry.json packages/ui/component-contracts.json` returns nothing for these items → commit `docs: registry meta and docs say only what is true (DS-72)`

- [ ] **Task 7: DS-73 conventions doctrine**
  - Files — Modify: `design.md` (new § "Conventions for components we own" after § 6 Engineering conventions; decision tree in § Components and blocks), `skills/public/vegastack-design-system/SKILL.md` ("Names hide abilities", "Which component for X", page shape AppShellPage → PageHeader → FilterBar → DataList → Empty tiers; recipe fixes :177-179 "parts are flat exports, there is no `Dialog.Trigger`" and :231 "`shadow-md` popover · `shadow-lg` sheet · none on a dialog"), `skills/internal/component/SKILL.md` (§ 3 pointer, two lines) · Create: `skills/internal/component/references/conventions.md`
  - Interfaces — Consumes: Task 2's heading and numerals rules, Task 5's view-switch rule · Produces: the conventions table (axis words, identity accessors, second-line option text, why-unavailable, empty-state copy, in flight, paging, element choice, default copy, settled value, counts in controls, headings, announcements, slots, exports, tones) citing only existing IDs (API-17, FRM-4), with its known deviations listed (`md` on Stat/StatusIcon/Chip/SidebarMenuSubButton, SettingsSection `titleAs`, TruncatedText `as`, `dismissable`, physical `side`, popup `container`, option-row recipe, PanelSearch look, async-write status, ComboboxChip on Chip); the five-step component/variant/part/block/docs-example tree with the block rules; the seven-item "Before you add a prop" checklist; a "Names hide abilities" line per component listed in the brief, each naming only abilities the current source has.
  - Steps: write → verify: `node tooling/skill-lint.mjs` (both SKILL.md under 500 lines, no repo path in the public skill, links resolve); `node tooling/sync-package-skills.mjs && node tooling/sync-package-skills.mjs --check`; `pnpm design:sync && pnpm design:sync:check`; for each "Names hide abilities" line, `grep` the named prop in `packages/ui/registry/ui/<name>.tsx` → commit `docs: conventions for components we own, decision tree, prop checklist (DS-73)`

- [ ] **Task 8: DS-75 page rhythm**
  - Files — Modify: `apps/docs/content/docs/foundations/spacing.mdx` (new § "Page rhythm" before § Related), `skills/public/vegastack-design-system/SKILL.md` (page-shape line links it)
  - Interfaces — Produces: the recipe on the stock scale — gutters `px-4 py-6` / `md:px-8 md:py-8`; PageHeader → first content `gap-6`; between sections `gap-8`; section heading → content `gap-3`; card padding from `--card-spacing` (Card `size`); `FieldGroup` `gap-6`, inside a `Field` `gap-3`; list toolbar → table `gap-3`; one control-height tier per row (`h-6/7/8/9`). Documented as defaults, not tokens.
  - Steps: write → verify: `node tooling/content-lint.mjs`; `pnpm -F @vegastack/docs lint:links` → commit `docs(spacing): the page-rhythm recipe (DS-75)`

- [ ] **Task 9: 00a-dependent doctrine lines (D2)**
  - Files — Modify, each only when its 00a PR is on `main`: `apps/docs/content/docs/components/pagination.mdx` (:174-177, after DS-71), `packages/ui/registry.json` field meta + `skills/public/vegastack-design-system/SKILL.md` (:160-165) + `design.md` FRM guidance (after DS-47: "`Field` wires the control through Base UI Field; pass ids only to override"), `apps/docs/content/docs/components/toast.mdx` (one toast per action, `toast.update` for repeats, after DS-64), sidebar docs (after DS-04), `skills/internal/review/references/lint-rules.md` (:230-233, after DS-10(a) if the measured ink changed), `apps/docs/content/docs/foundations/elevation.mdx` (overlay width table, after DS-13)
  - Interfaces — Consumes: the merged 00a source for each line · Produces: docs that match shipped behaviour
  - Steps: `git fetch origin && git rebase origin/main` → for each line, confirm its source is on `main` (`git log origin/main --oneline -- packages/ui/registry/ui/<name>.tsx`) → edit or defer (list deferrals in the PR body) → verify: `node tooling/content-lint.mjs`; `pnpm registry:build` leaves only the intended items changed → commit `docs: doctrine for the shipped 00a changes`

- [ ] **Task 10: changeset and full local proof**
  - Files — Create: `.changeset/facelift-doctrine.md` (`"@vegastack/design": patch` for the skills, `"@vegastack/ui": patch` for registry meta; body opens with `📚`)
  - Interfaces — Consumes: every earlier task · Produces: a branch that passes `PR quality` locally
  - Steps: write the changeset → verify: the ship skill's package-vs-changeset diff (`git diff --name-only origin/main...HEAD -- packages/design packages/design-tokens` vs `grep -h '^"@vegastack' .changeset/*.md`); `node tooling/changeset-lint.mjs`; `node tooling/changelog-assemble.mjs --dry-run && node tooling/changelog-assemble.mjs --check`; `node tooling/changelog-lint.mjs`; `pnpm registry:build && git status --short` clean; `pnpm lint`; `pnpm design:verify`; `pnpm -F @vegastack/docs lint`; `pnpm -F @vegastack/docs typecheck`; `pnpm verify`; visual read of the changed docs pages (typography, spacing, empty-states, provider-setup, button, toggle-group, chip, emoji-picker, not-found, home) in light and dark at 320px and wide per `skills/internal/ship/references/visual-review.md`, captures not committed → commit `chore: changeset for the facelift doctrine`

- [ ] **Task 11: hand back, then ship on MK's `ship it`**
  - Files — none
  - Interfaces — Produces: a published `@vegastack/design` patch and registry patch, deployed to design.vegastack.com; the version number Regent #136 consumes
  - Steps: report to MK with the branch, verification output and deferred Task 9 lines, and stop → on MK's own `ship it` in this repo (Regent's standing approval does not cover it): push, PR, green `PR quality`, exact-SHA squash merge, approve the Version Packages PR's `action_required` run, exact-SHA merge, OIDC publish, `deploy.yml`, production probe (`docs/RELEASING.md`) → verify: `npm view @vegastack/design version` and the registry version on design.vegastack.com

## Out of scope

Everything the brief lists as out of scope: every component source change (00a), 00b and 00c items, proposing or approving decision rows, the deferred optional items, the convention renames (listed as known deviations only), Regent pages and wrappers, any sanctioned-dependency, CI, workflow, runner, Access or version-reversal change.

## Gates and release

Task 10's commands locally, then `PR quality` in CI. One changeset, `📚`, patch on `@vegastack/design` and `@vegastack/ui`. Shipping is MK's `ship it` (AGENTS.md non-negotiable 5); it covers the whole release sequence and at most three corrective iterations. A step needing a trust-boundary change stops.
