# MK exhaustive component sweep — ledger (go-dark run, 2026-07-18)

Mandate: every component, every variant/prop, both themes, screenshot evidence, behavioral +
a11y + consistency checks (no rings; icon/text proportion; ink consistency on tinted rows;
weights ≤500; token-only), fixed at component level, no deferrals. Sequential A→Z, one at a time.
Contact sheets: scratchpad/sweep/<slug>-{light,dark}.png (regenerate with apps/docs/.sweep-capture.mjs).

Pre-sweep fixes (MK-reported): spinner size=inherit now text-current (invisible-on-primary bug);
Button default/lg icons -> --icon-inline 14px (proportion); menu shortcuts tint destructive on
destructive rows (context+dropdown). Weight kept at documented 500 — flag to MK if still heavy.

## Dependency audit (SW3)
Upgraded: tailwind 4.3.2 family · next 16.2.10 · playwright 1.61.1 · vitest 4.1.10 · tiptap 3.27.4 ·
lucide 1.24 · motion 12.42.2 · RHF 7.81 · prettier 3.9.5 · thesvg 3.2.6 · turbo 2.10.5 · wrangler 4.111 ·
globals 17 · changesets 2.31.1 · postcss 8.5.19 · react-markdown 10 (major, verified drop-in).
Held: typescript@7 (native-compiler major vs locked TS6), @types/node (tracks locked Node 24).

## Components (83)
- [x] accordion — CLEAN both themes (disabled dim ✓ post data-disabled fix, chevron align ✓, no rings, rhythm ✓); behavior covered by unit tests + controlled demo
- [x] alert — CLEAN both... both themes verified w/ zooms (warning amber vs error red distinct; icons baseline-aligned; dismiss/actions/no-icon gutter ✓)
- [x] alert-dialog — FIXED docs: 6 no-prop placeholder tables collapsed to prose (missed in earlier docs sweep); open-state clean per popup audit; triggers ✓
- [x] animated-number — CLEAN — mono numerals ✓ everywhere (cards/playground/explorer), tween covered by tests
- [x] app-shell — CLEAN — rail badge centered ✓, variants/skeleton/md-hide ✓, em-dash placeholders deliberate
- [x] attachment — CLEAN — vertical square thumb w/ fixture ✓ (h-auto fix), uploading/error/compose ✓, playground+API ✓
- [x] auto-save-input — CLEAN — states/controlled demos ✓; shakeSignal "unknown" is the opaque-by-design alias resolving, JSDoc carries meaning
- [x] avatar — FIXED demos — shapes/sizes/groups ✓ (zoom-verified circular) but pravatar.cc remote srcs → local /preview/avatar-{1,2,3}.svg fixtures (determinism); item + markdown-view remote imgs fixed in the same pass
- [x] badge — CLEAN — variant×intent matrix ✓, loading spinner in-tint ✓ (text-current fix), sizes/states/motion ✓
- [x] breadcrumb — CLEAN — separators/collapse/menu/trail ✓, API headings ✓
- [x] bubble — CLEAN — 7 variants/align/grouping/reactions/replies/collapsible/error-popover ✓
- [x] button — FIXED+verified — spinner now visible in host ink (zoom evidence), icons 14px proportional (zoom), 15 variants/8 sizes/CTA/states ✓; weight stays documented 500
- [x] card — CLEAN — pricing/density/borders-only ✓, footer buttons right-aligned ✓
- [x] chart — CLEAN — mono axis ticks verified (zoom), Jan tick unclipped, tooltip variants/legend ✓
- [x] checkbox — CLEAN — states/sizes/invalid/matrix ✓, disabled dim ✓
- [x] collapsible — CLEAN — disabled dim visible ✓ (data-disabled fix), controlled/keep-mounted ✓
- [x] color-picker — RE-FIXED (MK caught blend-ink regression): mix-blend-difference gave a different murky ink per swatch (dark-ish on blue) → check ink now LUMINANCE-AWARE via CSS relative color (white when swatch oklch L<0.65, black above); verified both branches: white-on-blue at 4x zoom, black ink computed on the brightened dark-theme yellow; class-swept registry — no other blend-derived ink exists
- [x] combobox — CLEAN — 17 part headings ✓, chips/async/empty ✓; behavior live-verified earlier (filter/empty/popup-input)
- [x] command — CLEAN — hero zoom: input hairline ✓ no phantom gap ✓ disabled dim ✓ icons proportional ✓; characterization-tested
- [x] context-menu — FIXED docs (14 unheaded API tables → part headings; systemic batch: 89 headings across 22 pages); destructive shortcut tint verified by probe; open anatomy audited clean
- [x] copy-button — CLEAN (sheet + earlier keyed-swap/pop-in verification; icon proportions ride Button fix)
- [x] country-select — CLEAN — popup input fix live-verified earlier (sticky header, no white hairline, list padding); flag column ✓
- [x] data-list — CLEAN — sort/selection/loading/empty/filtered/scale states ✓, mono amounts ✓, header baseline fix in
- [x] date-picker — CLEAN post-fixes — scroll-jump fixed+tested, dropdown caption inline (structural test), disabled range dims ✓
- [x] dialog — CLEAN — open-state rhythm audited (p-5/top-3 family unify), headings ✓; triggers render
- [x] dropdown-menu — FIXED docs headings (14) + destructive shortcut tint (probe-verified); anatomy audited clean
- [x] emoji-picker — CLEAN — grid centering fix in; search input padded (audited); popup shots reviewed earlier
- [x] empty — CLEAN — card surface border fix VISIBLE in sheet, sizes/intents/matrix ✓
- [x] field — CLEAN — invalid border-tint (no ring) ✓, success/labelAction/horizontal/disabled/borderless/RHF ✓
- [x] field-inline — CLEAN — playground/empty/accessible-name/borderless ✓
- [x] figure-frame — CLEAN — dark MarketingSurface wrap ✓, FIG number nowrap ✓, ratios/no-caption ✓
- [x] filter-bar — CLEAN — active-chip label/value hierarchy fix zoom-verified, demos/API ✓
- [x] hover-card — CLEAN — trigger renders; open state audited earlier (consistent w/ popover pair)
- [x] icon-button — FIXED — Story dev analyzer hung 60s+/500 on IconButtonProps type walk (dev-only; build fine); narrow client story host now; all 28 story pages probed fast
- [x] image — CLEAN — local fixture, error fallback icon renders (deterministic 404), rounding scale ✓
- [x] input — CLEAN — invalid border-tint no-ring ✓, addons incl invalid group ✓, types/states ✓
- [x] item — CLEAN — variants/media(avatar-icon-image w/ local fixture)/actions/link-row/group ✓
- [x] kbd — CLEAN — combos/sizes/platform mac↔Ctrl matrix ✓, mono glyphs ✓
- [x] label — FIXED demo: required-hook was invisible (deliberate no-asterisk doctrine) → demo now styles data-required visibly "(required)"; zoom-verified; states dim ✓
- [x] logo-row — CLEAN — dark marketing ground, dimmed wordmarks (audited earlier + dark sheet)
- [x] markdown-view — CLEAN — DS checkboxes in dark ✓, local image fixture ✓, heading ladder/GFM ✓ (react-markdown 10 verified)
- [x] marker — CLEAN — row consistency fix visible (dark sheet), all demos ✓
- [x] marketing-surface — CLEAN — dark ground both themes, plain prose copy (backtick fix in)
- [x] message — CLEAN — alignment/avatar/header-footer rhythm (dark+light sheets; vE audited clean)
- [x] message-scroller — CLEAN — all transcripts render populated in sheets; VRT capture override in place; behavior tested
- [x] notification-bell — CLEAN — outward badge growth verified (fix), sizes/dot ✓
- [x] otp-input — CLEAN — invalid red slots/mask/groups/separator/Field ✓
- [x] page-header — CLEAN — wrap fix contract tested; desktop sheet ✓
- [x] pagination — CLEAN — tabular numerals ✓, edge states dim ✓, sizes ✓
- [x] particle-field — CLEAN — deterministic dots (vE + sheets)
- [x] password-input — CLEAN — states/reveal/requirements ✗→✓ with icons (not color-alone) ✓
- [x] popover — CLEAN — API part-headings ✓ (batch fix), docs a11y text corrected earlier, open anatomy audited
- [x] progress — CLEAN — values/sizes/indeterminate/colored fills/custom track/matrix ✓
- [x] progress-indicator — CLEAN — values/sizes/shape matrix (sheet + earlier fixes)
- [x] radio-group — FIXED — horizontal orientation demo stacked vertically (Field w-full defeated the row; missed in FX3 triage) → w-auto wrappers; zoom-verified inline row
- [x] region-select — CLEAN — popup input verified live earlier; free-text fallback demos ✓
- [x] relative-time — CLEAN — unitStyle select in playground, modes contrast fixed, localization/live/determinism ✓
- [x] resizable — CLEAN — ALL demos render real heights incl vertical (wrapper fix visible), hand-written API ✓
- [x] scroll-area — CLEAN — vertical/horizontal/both + comparison ✓
- [x] section-header — CLEAN — XL size row present (fix), serif emphasis/center ✓
- [x] select — CLEAN — sizes/disabled/invalid/multiple/form ✓ (popup verified earlier)
- [x] separator — CLEAN — orientations/examples ✓
- [x] settings-row — CLEAN — labelProps demo now non-visual (identical labels), control variety ✓
- [x] sheet — CLEAN — sides/customization; p-5 rhythm unified (tested); open state audited
- [x] sidebar — CLEAN — badge centering zoom-verified on its own page, groups/rail/variants ✓
- [x] skeleton — CLEAN — shapes/card/counts/reveal ✓
- [x] slider — CLEAN — range/steps/controlled/labels/disabled ✓
- [x] spinner — CLEAN — in-button spinner visible in host ink (fix), sizes/colors ✓
- [x] split-button — CLEAN — loading halves uniform (fix visible), disabled dims both, 15-variant matrix ✓
- [x] staggered-text-reveal — CLEAN — settled states, reduced-motion doc ✓
- [x] status-icon — CLEAN — shape+color per state, sizes, labels ✓
- [x] switch — CLEAN — sizes/states/controlled/invalid ✓
- [x] table — CLEAN — mono ids+amounts, footer, overflow, selectable/indeterminate ✓
- [x] tabs — CLEAN — line/pill/counts/vertical/disabled ✓ + scroll-fade on overflow
- [x] terminal — CLEAN — mono/phosphor/copy + scroll-fade ✓
- [x] testimonial — CLEAN — serif quote/mono attribution/dark ground ✓
- [x] text-edit — FIXED docs nit — stale "focus-within ring" anatomy comment → border tint; toolbar/read-only/invalid/sized ✓
- [x] textarea — CLEAN — states/invalid tint/auto-grow (placeholder fix) ✓
- [x] toast — CLEAN — page + behavior verified live earlier (single mount, X top-right)
- [x] toggle — CLEAN — sizes/on-off/disabled ✓
- [x] toggle-group — CLEAN — single/multiple/sizes/vertical ✓
- [x] tooltip — CLEAN — sides/align/offset(default-8 docs fixed)/delay/kbd/arrow ✓
- [x] truncated-text — CLEAN — clamps/IconText/tooltipSide/TableCellText ✓
