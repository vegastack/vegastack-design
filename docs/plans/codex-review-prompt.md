MAXIMALLY STRICT, ZERO-BIAS adversarial review of the VegaStack design system BUILT in /Users/kmanojkumar/code/org-design. Assume nothing works; try hard to break it. Review the ACTUAL built code (packages/*, apps/docs, tooling/, skills/, the registry under apps/docs/public/r) AND its consistency with docs/requirements.md + docs/plans/*. Verify against official docs + the cloned /Users/kmanojkumar/code/references/fumadocs — run/inspect real files, do not trust comments or the plan. Be relentless, cite file:line, classify each finding high/medium/low, and demand ROOT-CAUSE fixes (no surface patches). Do not be charitable.

Check EVERY angle:
1. Does it actually build + typecheck + lint + pass Vitest/vitest-axe? Verify, don't assume. Any error is a finding.
2. Does the Fumadocs showcase render EVERY component via the REAL @vegastack packages (workspace links + local-registry copy-in)? Hunt for mocks/stubs, unstyled renders, broken imports, wrong relative paths, missing @source, components that don't actually mount.
3. Token pipeline: DTCG to Style Dictionary custom color/oklch transform; separate light/dark builds (no name collision); @theme inline bridge with NO self-referential vars (font/ease use --font-family-* / --motion-ease-*); base.css built + exported; tsup outputs match the package exports; the --primary (not --color-*) override actually repaints.
4. Registry + integrity: whole-item canonical hash; registry-stamp; local serve + copy-in; components.json; the fail-closed verify preflight; registry.json at packages/ui.
5. Every component meets the requirements section 7.6 contract: all UI states, the knobs contract (className/render/CVA/data-*/ref/slots), a11y (keyboard + ARIA + :focus-visible + axe), JSDoc props feeding AutoTypeTable.
6. Tailwind v4 wiring: globals, @custom-variant dark, @source depth. SEMANTIC TOKENS ONLY — flag ANY component-level hardcoded style: inline style={}, arbitrary values (bg-[#...], h-[13px], text-[0.8rem]), raw palettes (bg-neutral-900), or !important. Every visual value (color/spacing/radius/font/size) must resolve through a semantic token. Sanctioned icons only (lucide/lucide-animated/thesvg via Icon/BrandIcon), no inline svg icons. No outline:none / disabled-focus (must be :focus-visible).
7. Base UI usage (render prop, data-starting-style/ending-style transitions, 'use client' at the leaf); forms (RHF Controller + Zod 4 z.email()).
8. Do the agent skills actually work (add-component scaffolds correctly; consume preflight; design-audit lint)?
9. Gaps + edge-cases + unhandled failure modes not covered (empty/error/loading states, partial builds, hash mismatch, missing tokens).
10. Any contradiction between the built code and the locked decisions (Model A unprefixed exports, public-npm token layer, Base UI, etc.) or between any two docs.
11. PORT QUALITY (refine, not blind-copy): the 64-component inventory is ported from /Users/kmanojkumar/code/engg-vegastack-platform/src/components/{common,ui} (styling also in src/app/globals.css + src/app/tailwind-palette.css + src/lib/utils.ts). Cross-check each built component against its platform source: (a) were ALL its variants/sizes/states/features ported — none dropped or simplified? (b) was it REFINED, not copied verbatim — did any platform anti-pattern leak in (component-level hardcoded styles, hex/arbitrary values, raw palettes, !important, outline:none/disabled-focus, leftover Radix asChild instead of Base UI render prop + data-starting-style, inconsistent variant/prop naming across components)? (c) is every visual value routed through a semantic token, with consistent structure across all 64 components? Flag BOTH dropped variants AND un-refined copies.
12. COVERAGE — nothing missed: verify docs/ledger/component-matrix.md exists, has ONE ROW PER COMPONENT for the COMPLETE inventory (cross-check the row count against requirements §12 + the platform's src/components/common — flag any component MISSING from the matrix), and every cell is ✅ (built · Vitest · vitest-axe · browser-render · §7.6 contract · registry-item · local copy-in · in-showcase). Then SPOT-CHECK at least 8 components spanning different groups to confirm the matrix is NOT lying: each must actually have a passing Vitest test file, a vitest-axe assertion, a showcase page that renders, and a registry item with meta.integrity + a @vegastack/tokens range. Flag any component absent from the matrix, any column not green, and any ✅ cell that isn't truly passing.
