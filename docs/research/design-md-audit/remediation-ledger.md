# Remediation ledger

This ledger records implementation findings from the design.md audit. The machine inventory is
`audit-register.json`, regenerated from `packages/ui/component-contracts.json` and gated by
`tooling/verify-component-contracts.mjs`. (The `baseline.json` worktree freeze and the per-agent
`audits/agents/` transcripts were dropped before commit — machine-local paths and a snapshot this
changeset invalidates; the contract verifier is the durable authority.)

| ID      | Severity | Finding                                                                                                 | Remediation                                                                                                      | Evidence                                                                                                                     | Status                                                                    |
| ------- | -------- | ------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| REM-001 | P0       | `design.md` frontmatter was not valid, deterministic YAML                                               | Generate resolved light/dark frontmatter from DTCG and byte-match the public `/design.md` copy                   | `tooling/sync-design-md.mjs --check`                                                                                         | remediated                                                                |
| REM-002 | P0       | Inventory totals and coverage surfaces disagreed                                                        | Introduce the exact 539-record contract and generate matrix, routes, smoke membership, and audit register        | `tooling/verify-component-contracts.mjs`                                                                                     | remediated                                                                |
| REM-003 | P0       | `.vs-marketing` omitted dark-theme variables                                                            | Generate `.dark` and `.vs-marketing` from the same dark dictionary and fail on set/value drift                   | `tooling/verify-theme-parity.mjs`                                                                                            | remediated                                                                |
| REM-004 | P0       | Portaled surfaces could escape the marketing theme                                                      | Propagate an internal theme-scope context into Base UI portal roots and the Toaster mirror                       | `verify-portal-theme-scope.mjs`: 12/12 canonical owners/hosts                                                                | remediated                                                                |
| REM-005 | P0       | LogoRow secondary copy failed AA contrast                                                               | Use the contrast-safe semantic muted/foreground pair                                                             | contrast gate + LogoRow tests                                                                                                | remediated                                                                |
| REM-006 | P1       | Broad/color transitions delayed direct interaction feedback                                             | Ban `transition-all` and color transitions; narrow layout/opacity/transform transitions                          | `tooling/design-lint.mjs`                                                                                                    | remediated                                                                |
| REM-007 | P1       | Alpha/opacity roles, raw tracking/weight/effects, direct icon sizes, and faint text escaped enforcement | Add fail-closed lexical/AST lint rules and repair confirmed sites                                                | package/docs design lint                                                                                                     | remediated                                                                |
| REM-008 | P1       | Duplicate or displaced provenance headers were accepted                                                 | Require exactly one provenance header at line 1                                                                  | `tooling/verify-headers.mjs`                                                                                                 | remediated                                                                |
| REM-009 | P1       | Component docs and public APIs lacked uniform contracts                                                 | Require six component-page sections plus public props/component JSDoc, defaults, and examples                    | contract verifier + `verify-public-api-docs.mjs`: 100 records                                                                | remediated                                                                |
| REM-010 | P1       | Animated-icon behavior was generator-implicit and reduced motion consumer-owned                         | Normalize the generator/wrapper, pin Motion, and add all-member behavior/static coverage                         | `verify-animated-icons.mjs`: 439/439; behavior artifact                                                                      | locally remediated; four-lane visual evidence remains under REM-011       |
| REM-011 | P1       | VRT had missing and orphaned images and only two theme lanes                                            | Require four lanes, element-scoped fixtures, and all-icon chunks; validate exact Linux PNG membership/dimensions | `verify:vrt-baselines`                                                                                                       | externally blocked: 680 images require pinned Linux workflow              |
| REM-012 | P1       | Real registry installation sampled a stale hand list in one layout                                      | Derive transitive external-engine set cover and run the actual CLI in both layouts                               | final-tree breadth: 539/539 × two layouts, post-write + `tsc`; real 13 × two layouts rerun blocked before install by npm DNS | implementation remediated; current real-CLI acceptance externally blocked |
| REM-013 | P1       | Signed-manifest negatives were incomplete                                                               | Add positive signature verification plus tampered item/manifest and wrong-signer negatives                       | local hash verifier + deploy workflow                                                                                        | local hash complete; OIDC signature runs only in deploy workflow          |

No lower-severity deferral is approved by this document. The independent hit-area closeout's two
verifier P1s, two target-width P1s, and two geometry/tolerance P2s were all corrected before the
final registry build, and the geometry/tolerance contract they cover is now enforced continuously by
the pointer-target probe in `apps/docs/vrt/contracts.spec.ts`. Any remaining entry is an
acceptance-evidence blocker named explicitly in `zero-gap-proof.md`, not an unrecorded code deferral.
