# CI/CD release-efficiency primary-source ledger

Access date: 2026-07-28. This is the implementation research record for
`docs/plans/2026-07-28-cicd-release-efficiency-audit.md`. Only official documentation or primary
upstream source is eligible. “Plan effect” records mechanics only; it cannot override VegaStack's
locked policy or an MK checkpoint.

| Area                                | Primary source                                                                                                                                                                                                                                                 | Installed/current relevance                                           | Conclusion                                                                                                                                                                                                  | Plan effect                                                                                                                                                                                                             |
| ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Git diff records                    | [Git diff options](https://git-scm.com/docs/diff-options)                                                                                                                                                                                                      | Git on the implementation host; `versionBumpOnly` uses `--no-renames` | `--no-renames` prevents a rename-only record from hiding content shape; binary and mode metadata still require explicit handling, while untracked paths have no diff record                                 | Keep the changed-inventory/diff-record reconciliation and reject untracked, binary, mode, missing-record, rename, and deletion mutations. No policy change.                                                             |
| GitHub token permissions            | [Workflow syntax: permissions](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#permissions)                                                                                                                                 | Current Actions workflows use job/workflow permission blocks          | A specified permission block sets unspecified scopes to none; keep read-only workflow defaults and job-local OIDC/write scopes                                                                              | No receipt job needs new token authority. Workflow-security assertions remain fail closed.                                                                                                                              |
| GitHub artifact attestations        | [Artifact attestations](https://docs.github.com/en/actions/concepts/security/artifact-attestations) and [feature prerequisites](https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations#prerequisites) | Repository is private on GitHub Team                                  | Attestations bind workflow/repository/SHA but are not a claim that the artifact is safe. Private/internal repository attestations require Enterprise Cloud; Team supports them only for public repositories | Do not add an unavailable attestation dependency. The local receipt remains explicitly non-cryptographic; npm OIDC/Sigstore boundaries remain unchanged.                                                                |
| Self-hosted cache semantics         | [Dependency caching](https://docs.github.com/en/actions/concepts/workflows-and-actions/dependency-caching)                                                                                                                                                     | Persistent self-hosted macOS runners                                  | Actions caches used by self-hosted runners are still stored in GitHub-owned cloud storage; self-hosted machines are not clean per job                                                                       | Supports the later `cache: pnpm` shadow canary, not immediate removal. Preserve frozen installs and measure persistent-runner state.                                                                                    |
| Node filesystem metadata and writes | [Node.js 24.18 filesystem API](https://nodejs.org/docs/latest-v24.x/api/fs.html)                                                                                                                                                                               | Installed Node is v24.18.0; engines require Node ≥24.14               | `lstat` observes a symlink itself, `Stats.mode` exposes mode, `FileHandle.sync()` flushes, and concurrent promise modifications are not synchronized/thread-safe                                            | Whole-tree git identity remains the conservative content/type/mode/symlink fingerprint. Later evidence-store writes must use immutable temp files, flush, same-filesystem rename, and serialized/conflict-safe writers. |
| Sigstore identity                   | [Sigstore CI quickstart](https://docs.sigstore.dev/quickstart/quickstart-ci/)                                                                                                                                                                                  | Current deploy uses keyless blob signing and exact workflow identity  | Verification supports an exact GitHub workflow/branch certificate identity and bundle verification                                                                                                          | Preserve exact `deploy.yml@refs/heads/main` identity pinning; receipt schema changes do not replace registry signature verification.                                                                                    |
| Vite dependency optimization        | [Dependency pre-bundling](https://vite.dev/guide/dep-pre-bundling) and [`optimizeDeps` options](https://vite.dev/config/dep-optimization-options)                                                                                                              | Vitest 4.1.9 uses Vite 8.0.16 browser server                          | Default crawling ignores test files; late discoveries rebundle and reload. Explicit `entries` covers nonstandard entrypoints, and `include` forces linked packages into the pre-bundle                      | Crawl the browser tests as entries and explicitly include the linked design package entrypoints. A semantic mutation test prevents a return to cold-run reloads; no coverage or worker change.                          |
