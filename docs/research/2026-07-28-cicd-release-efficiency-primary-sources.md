# CI/CD release-efficiency primary-source ledger

Access date: 2026-07-28. This is the implementation research record for
`docs/plans/2026-07-28-cicd-release-efficiency-audit.md`. Only official documentation or primary
upstream source is eligible. “Plan effect” records mechanics only; it cannot override VegaStack's
locked policy or an MK checkpoint.

| Area                                | Primary source                                                                                                                                                                                                                                                             | Installed/current relevance                                           | Conclusion                                                                                                                                                                                                  | Plan effect                                                                                                                                                                                                             |
| ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Git diff records                    | [Git diff options](https://git-scm.com/docs/diff-options)                                                                                                                                                                                                                  | Git on the implementation host; `versionBumpOnly` uses `--no-renames` | `--no-renames` prevents a rename-only record from hiding content shape; binary and mode metadata still require explicit handling, while untracked paths have no diff record                                 | Keep the changed-inventory/diff-record reconciliation and reject untracked, binary, mode, missing-record, rename, and deletion mutations. No policy change.                                                             |
| GitHub token permissions            | [Workflow syntax: permissions](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#permissions)                                                                                                                                             | Current Actions workflows use job/workflow permission blocks          | A specified permission block sets unspecified scopes to none; keep read-only workflow defaults and job-local OIDC/write scopes                                                                              | No receipt job needs new token authority. Workflow-security assertions remain fail closed.                                                                                                                              |
| GitHub artifact attestations        | [Artifact attestations](https://docs.github.com/en/actions/concepts/security/artifact-attestations) and [feature prerequisites](https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations#prerequisites)             | Repository is private on GitHub Team                                  | Attestations bind workflow/repository/SHA but are not a claim that the artifact is safe. Private/internal repository attestations require Enterprise Cloud; Team supports them only for public repositories | Do not add an unavailable attestation dependency. The local receipt remains explicitly non-cryptographic; npm OIDC/Sigstore boundaries remain unchanged.                                                                |
| Self-hosted cache semantics         | [Dependency caching](https://docs.github.com/en/actions/concepts/workflows-and-actions/dependency-caching)                                                                                                                                                                 | Persistent self-hosted macOS runners                                  | Actions caches used by self-hosted runners are still stored in GitHub-owned cloud storage; self-hosted machines are not clean per job                                                                       | Supports the later `cache: pnpm` shadow canary, not immediate removal. Preserve frozen installs and measure persistent-runner state.                                                                                    |
| Node filesystem metadata and writes | [Node.js 24.18 filesystem API](https://nodejs.org/docs/latest-v24.x/api/fs.html)                                                                                                                                                                                           | Installed Node is v24.18.0; engines require Node ≥24.14               | `lstat` observes a symlink itself, `Stats.mode` exposes mode, `FileHandle.sync()` flushes, and concurrent promise modifications are not synchronized/thread-safe                                            | Whole-tree git identity remains the conservative content/type/mode/symlink fingerprint. Later evidence-store writes must use immutable temp files, flush, same-filesystem rename, and serialized/conflict-safe writers. |
| Sigstore identity                   | [Sigstore CI quickstart](https://docs.sigstore.dev/quickstart/quickstart-ci/)                                                                                                                                                                                              | Current deploy uses keyless blob signing and exact workflow identity  | Verification supports an exact GitHub workflow/branch certificate identity and bundle verification                                                                                                          | Preserve exact `deploy.yml@refs/heads/main` identity pinning; receipt schema changes do not replace registry signature verification.                                                                                    |
| Vite dependency optimization        | [Dependency pre-bundling](https://vite.dev/guide/dep-pre-bundling) and [`optimizeDeps` options](https://vite.dev/config/dep-optimization-options)                                                                                                                          | Vitest 4.1.9 uses Vite 8.0.16 browser server                          | Default crawling ignores test files; late discoveries rebundle and reload. Explicit `entries` covers nonstandard entrypoints, and `include` forces linked packages into the pre-bundle                      | Crawl the browser tests as entries and explicitly include the linked design package entrypoints. A semantic mutation test prevents a return to cold-run reloads; no coverage or worker change.                          |
| Vitest related selection            | [Vitest 4 CLI](https://v4.vitest.dev/guide/cli) and [`getRelevantTestSpecifications`](https://vitest.dev/api/advanced/vitest.html#getrelevanttestspecifications)                                                                                                           | Installed Vitest is 4.1.9                                             | `related` follows static imports; dynamic path imports are not covered. The relevant-specification API filters without executing tests and can return one specification per browser project                 | Generate a deduplicated shadow comparison against registry/import authority. Registry closure schedules; missing, stale, unknown, dynamic, or disagreeing graph state widens rather than narrows.                       |
| GitHub job dependencies/summaries   | [Workflow syntax: `needs`](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#jobsjob_idneeds) and [workflow command summaries](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-commands#adding-a-job-summary) | Current GitHub Actions service; pinned actions remain unchanged       | A failed/skipped dependency skips downstream jobs unless a status conditional overrides it. `GITHUB_STEP_SUMMARY` is retained per step and grouped into the completed job summary                           | Make CI verification depend on receipt-guard. Make deployment-complete an ordinary all-success dependency with no `always()`/continue-on-error path; publish its terminal summary only there.                           |
| Cloudflare structured deploy output | [Wrangler system environment variables](https://developers.cloudflare.com/workers/wrangler/system-environment-variables/) and [Workers versions/deployments](https://developers.cloudflare.com/workers/versions-and-deployments/)                                          | Deploy pins Wrangler 4.113.0                                          | `WRANGLER_OUTPUT_FILE_PATH` emits NDJSON; a successful `deploy` record contains `version_id` and targets. `wrangler deploy` creates a version and immediately assigns 100% traffic                          | Capture exactly one structured deploy record and propagate its version ID. Treat that live upload as a candidate until the external boundary probe succeeds and the terminal job runs.                                  |

## Stage H confirmation — 2026-07-28

| Area                         | Primary source                                                                                                  | Installed/current relevance | Conclusion                                                                                                                                                           | Plan effect                                                                                                                                                  |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------- | --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Vitest reporters and filters | [Vitest 4 reporters](https://v4.vitest.dev/guide/reporters) and [Vitest 4 CLI](https://v4.vitest.dev/guide/cli) | Installed Vitest is 4.1.9   | Vitest supports combined terminal/custom reporters, exact file filters, full-name regex filters, and an explicit browser-name selector; zero-test passing is opt-in. | Retain machine failure targets alongside terminal output, anchor full names, require an exact browser, and reject zero execution. No retry becomes evidence. |

## Stage I confirmation — 2026-07-28

| Area                         | Primary source                                                                                                     | Installed/current relevance                             | Conclusion                                                                                                                                                                                                                                                  | Plan effect                                                                                                                                                                                                                                          |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Turbo task and global inputs | [Configuring `turbo.json`](https://turborepo.com/docs/reference/configuration)                                     | Installed Turbo is 2.10.5                               | `globalDependencies` enter every task hash, while task `inputs` opt out of defaults unless `$TURBO_DEFAULT$` is restored; `$TURBO_ROOT$` is the supported way to bind repository-root inputs. Root package/lock/Turbo configuration remain implicit inputs. | Keep the blanket `tooling/**` dependency during shadow. Inventory direct package-script tools and transitive imports, report task-specific fingerprints, and do not remove the blanket until every root data/config dependency has a mutation proof. |
| Turbo dry-run observability  | [Turbo `run --dry=json` and `--summarize`](https://turborepo.com/docs/reference/run#--dry--dry-run)                | Installed Turbo is 2.10.5                               | JSON dry-runs expose task IDs, cache hashes, input lists, dependencies, and environment configuration; summaries can explain input/hash/timing differences.                                                                                                 | Retain current blanket hashes and proposed external-input hashes side by side. A shadow plan is measurement only, never cache authority.                                                                                                             |
| Turbo affected selection     | [Task inputs and affected future flag](https://turborepo.com/docs/reference/configuration#affectedusingtaskinputs) | Current official docs; flag is not enabled in this tree | Task-level affected selection depends on declared `inputs`; root package, lockfile, Turbo configuration, and global dependencies still select every task.                                                                                                   | Do not enable the future flag or task-specific inputs in Stage I. VegaStack's dependency/route authorities remain the scheduling oracle, unknowns widen, and any future Turbo-assisted selection must be cross-checked rather than trusted alone.    |

## Stage K confirmation — 2026-07-28

| Area                   | Primary source                                                                                                     | Installed/current relevance                                                                             | Conclusion                                                                                                                                                                                   | Plan effect                                                                                                                                                                                    |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| npm trusted publishing | [npm trusted publishers](https://docs.npmjs.com/trusted-publishers/)                                               | Workflow pins Node 24 and npm 11.16.0; current requirement is npm CLI 11.5.1+ and GitHub-hosted runners | GitHub Actions trusted publishing uses short-lived OIDC, needs `id-token: write`, does not support self-hosted runners, and does not require an npm token.                                   | Preserve the isolated hosted `publish` job and no-`NPM_TOKEN` posture. Do not move build/OIDC to a mini.                                                                                       |
| npm provenance         | [Generating provenance statements](https://docs.npmjs.com/generating-provenance-statements/)                       | Source repository is private; public packages are built in the hosted `package-build` job               | Trusted publishing automatically generates provenance where the repository/package visibility prerequisites are met; private-repository limitations remain.                                  | Preserve the hosted exact-byte producer and current documented visibility limitation. State selection changes when the producer runs, not its trust boundary.                                  |
| Changesets action      | [changesets/action upstream README](https://github.com/changesets/action)                                          | Pinned action is 1.9.0; CLI is 2.31.1                                                                   | With pending changesets the action creates or updates a Version Packages PR; custom publish commands must safely handle already-published versions and expose explicit publication outcomes. | Keep Version PR mutation separate from npm OIDC. Model open/update, versioned-unpublished, and already-published recovery states explicitly.                                                   |
| Changesets CLI         | [Changesets command-line options](https://github.com/changesets/changesets/blob/main/docs/command-line-options.md) | Installed CLI 2.31.1                                                                                    | `changeset version` consumes changesets and changes workspace versions/changelogs; `changeset publish` publishes workspace versions that are newer than registry versions.                   | Exact npm `name@version` lookups decide whether hosted publication work is required. All-empty changesets and lookup uncertainty block rather than relying on the action's green/no-op result. |

## Publication-trust clarification — accessed 2026-07-29

| Area                                  | Primary source                                                                                                                                                                                                                                                                   | Installed/current relevance                                                                          | Conclusion                                                                                                                                                                                                                                                                        | Plan effect                                                                                                                                                                                                                                |
| ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| npm trusted publishing and provenance | [npm trusted publishers](https://docs.npmjs.com/trusted-publishers/)                                                                                                                                                                                                             | Workflow pins Node 24 and npm 11.16.0; the repository is private and the two npm packages are public | GitHub trusted publishing requires a GitHub-hosted runner and supports tokenless OIDC. Automatic npm provenance additionally requires a public repository and a public package; npm explicitly states that a private repository receives no provenance even for a public package. | Keep hosted `publish`, no `NPM_TOKEN`, and the hosted exact-byte producer. Correct current prose to describe `package-build` as ephemeral exact-byte isolation rather than npm provenance. No workflow authority or MK checkpoint changes. |
| GitHub artifact attestations          | [GitHub artifact attestations](https://docs.github.com/en/actions/concepts/security/artifact-attestations) and [private-repository prerequisites](https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations#prerequisites) | Repository is private on GitHub Team; no attestation action is configured                            | GitHub artifact attestations are a separate facility from npm provenance, and private/internal repository use requires GitHub Enterprise Cloud.                                                                                                                                   | Do not relabel the existing Actions artifact as a GitHub attestation and do not add an unavailable trust dependency. Existing immutable digest verification remains the authority.                                                         |

## Clean package-artifact verification — accessed 2026-07-29

| Area              | Primary source                             | Installed/current relevance                                                | Conclusion                                                                                                                                           | Plan effect                                                                                                                                                                                                          |
| ----------------- | ------------------------------------------ | -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| pnpm package pack | [pnpm 11 `pack`](https://pnpm.io/cli/pack) | Workspace pins pnpm 11.7.0; public manifests export ignored `dist/*` files | `pnpm pack --json` exposes the archive file manifest; `--dry-run` is explicitly intended for inspecting contents. Pack lifecycle hooks are separate. | Consume explicitly builds both public packages, packs with JSON output, and independently checks every export/bin target. No `prepack`/`prepare` hook is added; hosted exact-byte publication topology is unchanged. |

## Dynamic dependency-aware verification — accessed 2026-07-30

| Area                               | Primary source                                                                                                                                             | Installed/current relevance                      | Conclusion                                                                                                                                                                                    | Plan effect                                                                                                                                                                                                                              |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Vitest related/list/reporters      | [Vitest 4 CLI](https://v4.vitest.dev/guide/cli) and [Vitest 4 reporters](https://v4.vitest.dev/guide/reporters)                                            | Installed Vitest is 4.1.9                        | `related` follows static imports but not computed dynamic imports; `list --json` exposes the selected tests before execution; repeated filters and structured/custom reporters are supported. | Use the repository AST/registry graph as an independent oracle, widen computed or unresolved imports, list before running, and reconcile exact nonempty planned/listed/executed leaves. Selected runs remain diagnostic-only.            |
| Playwright exact selection         | [Playwright test CLI](https://playwright.dev/docs/test-cli)                                                                                                | Installed `@playwright/test` is 1.61.0           | The supported CLI exposes `--list`, `--test-list`, `--project`, and structured reporters. `--pass-with-no-tests` is opt-in, so VegaStack can and does reject an empty selector.               | Keep contract selection behind `contracts-run.mjs`, pre-list exact route/project/title leaves, and reject stale, mismatched, or zero-execution scopes. Do not bypass wrapper-owned build, port, server identity, or cleanup.             |
| Turbo task/hash diagnostics        | [Turbo configuration](https://turborepo.com/docs/reference/configuration) and [Turbo run dry-run](https://turborepo.com/docs/reference/run#--dry--dry-run) | Installed Turbo is 2.10.5                        | Task `inputs` replace defaults unless `$TURBO_DEFAULT$` is included; global dependencies influence every task hash; JSON dry-runs expose the task graph and hashes for explanation.           | Continue reporting proposed task-specific inputs and dry-run hashes only in shadow. Keep the existing blanket `tooling/**` dependency and full oracle until the independent input inventory and later MK checkpoint are satisfied.       |
| TypeScript AST/module resolution   | [TypeScript compiler API](https://github.com/microsoft/TypeScript/wiki/Using-the-Compiler-API)                                                             | Workspace TypeScript is the locked catalog build | `SourceFile` exposes the parsed AST and the compiler API supports explicit module resolution. Computed runtime import paths still cannot be proven from a literal static graph.               | Parse import/export/re-export/literal `import()`/literal `require()` edges with the installed compiler, model repository aliases explicitly, and widen on computed or unresolved internal imports rather than claiming complete closure. |
| Node metadata and atomic reporting | [Node.js 24.18 filesystem API](https://nodejs.org/docs/latest-v24.x/api/fs.html)                                                                           | Runtime is Node 24.18.0                          | `lstat` examines the symlink itself and `Stats.mode` exposes file mode; concurrent promise writes are not synchronized or thread-safe.                                                        | Bind plans to content/type/mode/symlink facts and use the existing same-filesystem atomic report writer. Corrupt, partial, conflicting, missing, or changed-tree evidence can only cause a rerun/failure, never a pass.                  |

These sources refine implementation mechanics only. They do not enable dynamic pre-push, affected
reuse, selected consume replacement, task-specific Turbo cache inputs, or compositional production
evidence. The complete exact-final-tree `production-full` ship proof remains mandatory.

## VRT capture clarification — accessed 2026-07-30

| Area                                         | Primary source                                                                                                                                                                                           | Installed/current relevance                                                                                                        | Conclusion                                                                                                                                                                                                                                                                                                                                                       | Plan effect                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Playwright screenshot scrolling and clipping | [Playwright page assertions](https://playwright.dev/docs/api/class-pageassertions) and [element screenshots](https://playwright.dev/docs/api/class-elementhandle#element-handle-screenshot)              | Installed `@playwright/test` is 1.61.0; its bundled types expose `clip` on page screenshot assertions                              | Element screenshots scroll the element into view before capture. Page screenshot assertions support an explicit clipping rectangle. In OTP's nested overflow preview, the element-owned scroll intermittently clipped two already-rendered rows even after readiness and containment checks passed.                                                              | Keep locator screenshots for every ordinary fixture. For the exact OTP fixture only, first prove all five rows are rendered and contained, derive the non-null fixture bounding box, then use a page screenshot clipped to that exact rectangle. Mutation tests reject route widening, an invented/missing rectangle, a missing clip, locator fallback, or removal of the ordinary fixture path. This corrects capture mechanics only; it does not change component output, comparison thresholds, selection, receipt authority, or production policy. |
| Playwright clip/viewport geometry            | [Playwright page screenshot clip](https://playwright.dev/docs/api/class-page#page-screenshot-option-clip) and [locator bounding box](https://playwright.dev/docs/api/class-locator#locator-bounding-box) | Installed `@playwright/test` is 1.61.0; current page assertions delegate screenshot geometry to the page screenshot implementation | Page clipping uses an explicit x/y/width/height rectangle and defaults to the visible viewport rather than full-page capture. Locator bounding boxes are relative to the main-frame viewport, may be `null`, and may have negative x/y after scrolling. Therefore non-null alone does not prove that the requested rectangle can be captured without truncation. | Immediately before OTP capture, require the derived rectangle and viewport to exist, require every coordinate/dimension to be finite, require positive dimensions and nonnegative origin, and require right/bottom edges within the current viewport. A TypeScript-AST verifier enforces the exact ordered executable branch and exact OR-chain; 36 mutations include unreachable, early-return, overwritten, ignored, short-circuited, negative, zero-sized, and overflowing variants. No other fixture or evidence policy changes.                   |
